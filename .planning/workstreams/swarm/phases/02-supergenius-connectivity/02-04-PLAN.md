---
phase: 02-supergenius-connectivity
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02-PLAN.md
  - 02-03-PLAN.md
files_modified:
  - src/core/sgprocessing/SGProcessingBridge.hpp
  - src/core/sgprocessing/SGProcessingBridge.cpp
  - src/api/GeniusAPIServer.cpp
  - src/genius_slm_chat_c.cpp
autonomous: true
requirements:
  - SG-02
---

<objective>
Wire SuperGeniusClient into the existing dispatch pipeline. Replace the SubmitNetwork stub in SGProcessingBridge with real gRPC dispatch, integrate into GeniusAPIServer initialization, expose connectivity status, and implement auto-fallback to local MNN inference when SuperGenius is unreachable.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-supergenius-connectivity/02-CONTEXT.md
@src/core/sgprocessing/SGProcessingBridge.hpp
@src/core/sgprocessing/SGProcessingBridge.cpp
@src/api/GeniusAPIServer.hpp
@src/api/GeniusAPIServer.cpp
@src/genius_slm_chat_c.cpp
@src/genius_slm_chat_c.h
</context>

<tasks>

<task type="auto">
  <name>task 1: integrate SuperGeniusClient into GeniusAPIServer</name>
  <files>
    src/api/GeniusAPIServer.hpp
    src/api/GeniusAPIServer.cpp
  </files>
  <read_first>
  - src/api/GeniusAPIServer.hpp — Config struct, class declaration, member variables
  - src/api/GeniusAPIServer.cpp — Initialize() function (lines 45-153)
  - src/network/sg_client/SuperGeniusClient.hpp — client interface
  </read_first>
  <action>
Integrate SuperGeniusClient into GeniusAPIServer:
1. Add SuperGeniusClient forward declaration and member to GeniusAPIServer (via PIMPL or unique_ptr)
2. In Initialize():
   - If config.sg_endpoint_ is set (non-empty), create SuperGeniusClient
   - Call client->Initialize(identity) with the server's NodeIdentity
   - Call client->Connect() — log success or warn if failed
3. Wire config.sg_endpoint_, sg_tls_ca_, sg_tls_cert_ from Config into SuperGeniusClient::Config
4. Add connectivity status method: IsSuperGeniusConnected() → bool
</action>
  <verify><automated>grep -c "SuperGeniusClient" src/api/GeniusAPIServer.cpp</automated></verify>
  <acceptance_criteria>
  - SuperGeniusClient created during Initialize() when endpoint configured
  - Connect() called at startup — eagert connect (fail-fast)
  - Config fields flow from GeniusAPIServer::Config to SuperGeniusClient::Config
  - Connectivity status queryable
  </acceptance_criteria>
</task>

<task type="auto">
  <name>task 2: replace SubmitNetwork stub with real dispatch</name>
  <files>
    src/core/sgprocessing/SGProcessingBridge.hpp
    src/core/sgprocessing/SGProcessingBridge.cpp
  </files>
  <read_first>
  - src/core/sgprocessing/SGProcessingBridge.cpp — SubmitNetwork stub at line 340
  - src/core/sgprocessing/SGProcessingBridge.hpp — Config with network_mode_
  - src/network/sg_client/SuperGeniusClient.hpp — SubmitJob interface
  </read_first>
  <action>
Replace the SubmitNetwork stub in SGProcessingBridge:
1. Add SuperGeniusClient* member to SGProcessingBridge (set from GeniusAPIServer)
2. Add SetClient(SuperGeniusClient*) method
3. Replace SubmitNetwork body:

```cpp
outcome::result<std::vector<uint8_t>> SGProcessingBridge::SubmitNetwork(
    const std::string &jsondata) const
{
    if (!client_)
    {
        return outcome::failure(Error::NetworkError);
    }
    return client_->SubmitJob(jsondata);
}
```

4. Implement auto-fallback in SubmitJob():
   - If network_mode_ and client_: try SubmitNetwork first
   - On NetworkError or timeout: log WARN, call SubmitDirect instead
   - On auth failure (SignatureInvalid): DO NOT fallback — propagate error
</action>
  <verify><automated>grep -c "SubmitNetwork\|SubmitJob" src/core/sgprocessing/SGProcessingBridge.cpp</automated></verify>
  <acceptance_criteria>
  - SubmitNetwork calls SuperGeniusClient::SubmitJob()
  - Auto-fallback to SubmitDirect on network errors
  - Auth failures NOT silently swallowed — propagate to caller
  - NetworkError returned when client_ is null
  </acceptance_criteria>
</task>

<task type="auto">
  <name>task 3: expose connectivity status via FFI</name>
  <files>
    src/genius_slm_chat_c.cpp
  </files>
  <read_first>
  - src/genius_slm_chat_c.cpp — GeniusSlmGetStatus() implementation
  - src/api/GeniusAPIServer.hpp — IsSuperGeniusConnected()
  </read_first>
  <action>
Update GeniusSlmGetStatus() to include SuperGenius connectivity:
- Add "supergenius_connected": bool to the status JSON
- Add "fallback_active": bool — true when running in local mode despite network_mode_ being set
- Add "sg_endpoint": string — the configured SuperGenius endpoint
</action>
  <verify><automated>grep -c "supergenius_connected\|fallback_active" src/genius_slm_chat_c.cpp</automated></verify>
  <acceptance_criteria>
  - GeniusSlmGetStatus JSON includes supergenius_connected field
  - fallback_active field shows when using local mode as fallback
  - sg_endpoint shows configured endpoint
  </acceptance_criteria>
</task>

</tasks>
