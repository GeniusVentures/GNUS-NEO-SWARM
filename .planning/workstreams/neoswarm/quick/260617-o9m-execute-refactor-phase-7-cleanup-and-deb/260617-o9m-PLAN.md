# Quick Task 260617-o9m: Execute Refactor Phase 7 — Cleanup & Debt

**Source:** .planning/REFACTOR_ROADMAP.md → Phase 7

## Tasks

### Task 1: Delete orphaned test file + fix CMake target name mismatch

**Files:**
- `test/ffi/test_genius_slm_ffi.cpp` — DELETE (orphaned, not built, superseded by `test_genius_elm_ffi.cpp`)
- `test/CMakeLists.txt:62-63` — EDIT target name from `test_genius_slm_ffi` to `test_genius_elm_ffi`

**Action:**
1. `git rm test/ffi/test_genius_slm_ffi.cpp`
2. In `test/CMakeLists.txt` line 62: change target name `test_genius_slm_ffi` → `test_genius_elm_ffi`
3. In `test/CMakeLists.txt` line 63: update `target_sources(test_genius_slm_ffi` → `target_sources(test_genius_elm_ffi`

**Verify:** `git status` shows file deleted and CMakeLists modified. Build passes: `ninja -C build/OSX/Debug test_genius_elm_ffi`

### Task 2: Fix double target_compile_definitions for GENIUS_HAS_ROCKSDB

**Files:**
- `src/reputation/CMakeLists.txt` — EDIT remove duplicate line 20

**Action:**
1. In `src/reputation/CMakeLists.txt`, remove the duplicate `target_compile_definitions(neoswarm_reputation PUBLIC GENIUS_HAS_ROCKSDB)` on line 20 (keep line 17)

**Verify:** `grep -c "GENIUS_HAS_ROCKSDB" src/reputation/CMakeLists.txt` returns 1
