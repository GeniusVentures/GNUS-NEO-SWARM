---
phase: 13-sgfp4-v2-model-support
plan: 02
type: execute
wave: 2
depends_on: ["13-01", "13-03"]
files_modified:
  - "W:/gnus/GeniusNetwork/SuperGenius/build/Windows/Release/SuperGenius/lib/SGProcessingManager/SGProcessors.lib"
  - "W:/gnus/GeniusNetwork/SuperGenius/build/Windows/Release/SuperGenius/lib/SGProcessingManager/sgprocmanagerquant.lib"
  - "test/CMakeLists.txt"
  - "test/integration/test_sgprocessing_pipeline.cpp"
  - "test/integration/processing_datatypes/sgfp4_tiny.mnn"
  - "test/integration/processing_datatypes/sgfp4_tiny_input.raw"
  - "test/integration/processing_datatypes/sgfp4_tiny.recipe.md"
autonomous: true
requirements: [SGF-01]

must_haves:
  truths:
    - "D-01: the VulkanInitMutex deadlock status was checked in research before locking the verification approach (RESEARCH.md Finding 2: closed by sgproc-render Phase 18 commit 528a92a), so this plan targets real local E2E execution rather than a permanent skip-gate."
    - "Per SGF-01 and D-02, a real SGFP4-quantized .mnn fixture runs through SGProcessingBridge's direct-call path and returns non-empty output bytes on CPU and Vulkan when the environment allows."
    - "Per D-07, the end-to-end test fails loudly if the committed fixture contains zero `OpType_SGFP4Dequant` ops."
    - "Per D-03 and the updated Finding 2, Vulkan is skipped only when `HasUsableVulkanDevice()` reports no usable device; it is not permanently skip-gated for the old deadlock."
  artifacts:
    - path: "test/integration/processing_datatypes/sgfp4_tiny.mnn"
      provides: "small inline SGFP4 model fixture with at least one SGFP4Dequant op"
    - path: "test/integration/processing_datatypes/sgfp4_tiny_input.raw"
      provides: "input tensor bytes matching the SGFP4 fixture's declared input shape"
    - path: "test/integration/processing_datatypes/sgfp4_tiny.recipe.md"
      provides: "literal manual mnnconvert --sgfp4 provenance for the committed fixture"
    - path: "test/integration/test_sgprocessing_pipeline.cpp"
      provides: "CPU and Vulkan direct-path SGFP4 integration tests"
  key_links:
    - from: "test/CMakeLists.txt"
      to: "test/integration/processing_datatypes/sgfp4_tiny.mnn"
      via: "copy_if_different fixture staging into SUPERGENIUS_TEST_DATA_DIR"
      pattern: "sgfp4_tiny\.mnn"
    - from: "test/integration/test_sgprocessing_pipeline.cpp"
      to: "W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_tensor.cpp"
      via: "backend parameter injected into BuildSchemaJson output, then SubmitDirect() exercises ProcessingManager::Create()/Process()"
      pattern: "backend|SubmitDirect|OpType_SGFP4Dequant"
---

<objective>
Deliver the real direct-call proof for SGF-01 after the SuperGenius backend resolver exists. This plan commits the tiny inline SGFP4 fixture required by D-06, records the exact manual `mnnconvert --sgfp4` provenance beside it, stages that fixture through the existing `SUPERGENIUS_TEST_DATA_DIR` copy pattern, and adds two explicit end-to-end tests that exercise the CPU and Vulkan direct paths through `SGProcessingBridge::SubmitDirect()` with a schema parameter named `backend`. Per D-07, the tests must assert the fixture really contains at least one `OpType_SGFP4Dequant` op so a silently all-FP32 model cannot pass vacuously.

Purpose: convert the research conclusion into executable proof that SGFP4 models run through the real direct-call chain on both backends.
Output: committed SGFP4 fixture assets, recorded manual conversion recipe, and release-build integration coverage for explicit CPU and Vulkan direct-call execution.
</objective>

## Artifacts this phase produces

- Refreshed `W:/gnus/GeniusNetwork/SuperGenius/build/Windows/Release/SuperGenius/lib/SGProcessingManager/SGProcessors.lib`
- Refreshed `W:/gnus/GeniusNetwork/SuperGenius/build/Windows/Release/SuperGenius/lib/SGProcessingManager/sgprocmanagerquant.lib`
- `test/integration/processing_datatypes/sgfp4_tiny.mnn`
- `test/integration/processing_datatypes/sgfp4_tiny_input.raw`
- `test/integration/processing_datatypes/sgfp4_tiny.recipe.md`
- `SGProcessingPipeline.Sgfp4DirectCpu_EndToEnd`
- `SGProcessingPipeline.Sgfp4DirectVulkan_EndToEnd`
- Test-local helper `BuildBackendParameterizedSchemaJson(...)`
- Test-local helper `AssertModelContainsSgfp4DequantOp(...)`

<execution_context>
@C:/Users/Fuu/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Fuu/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/.planning/workstreams/neoswarm/ROADMAP.md
@W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/.planning/workstreams/neoswarm/REQUIREMENTS.md
@W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/.planning/workstreams/neoswarm/STATE.md
@W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/.planning/workstreams/neoswarm/phases/13-sgfp4-v2-model-support/13-CONTEXT.md
@W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/.planning/workstreams/neoswarm/phases/13-sgfp4-v2-model-support/13-RESEARCH.md
@W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/.planning/workstreams/neoswarm/phases/13-sgfp4-v2-model-support/13-PATTERNS.md
@test/CMakeLists.txt
@test/integration/test_sgprocessing_pipeline.cpp
@W:/gnus/GeniusNetwork/thirdparty/MNN/test/op/SGFP4ClassicAPITest.cpp
@W:/gnus/GeniusNetwork/thirdparty/MNN/include/MNN/SGFP4DequantUtils.hpp
@W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/include/processors/vulkan_gpu_probe.hpp
@W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_tensor.cpp
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rebuild the SuperGenius Release libraries after 13-01 lands</name>
  <files>W:/gnus/GeniusNetwork/SuperGenius/build/Windows/Release/SuperGenius/lib/SGProcessingManager/SGProcessors.lib, W:/gnus/GeniusNetwork/SuperGenius/build/Windows/Release/SuperGenius/lib/SGProcessingManager/sgprocmanagerquant.lib</files>
  <read_first>
    - W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/.planning/workstreams/neoswarm/phases/13-sgfp4-v2-model-support/13-01-PLAN.md
    - W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/.planning/workstreams/neoswarm/phases/13-sgfp4-v2-model-support/13-CONTEXT.md
    - W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/src/core/CMakeLists.txt
  </read_first>
  <action>Per locked decision D-10, perform an explicit SuperGenius Release rebuild after the 13-01 backend-resolver and null-check source edits land so NEO-SWARM never links stale prebuilt static libraries. Run the rebuild from `W:/gnus/GeniusNetwork/SuperGenius/build/Windows/Release`, build at minimum `SGProcessors` and `sgprocmanagerquant`, and capture timestamp evidence that both resulting `.lib` files postdate the `processing_processor_mnn_tensor.cpp` and `quantization.cpp` edits from 13-01 before any NEO-SWARM integration test result is interpreted.</action>
  <acceptance_criteria>
    - `W:/gnus/GeniusNetwork/SuperGenius/build/Windows/Release/SuperGenius/lib/SGProcessingManager/SGProcessors.lib` and `sgprocmanagerquant.lib` are rebuilt after the 13-01 source changes land.
    - The rebuild is executed from the SuperGenius Release build tree, not NEO-SWARM's local `build/` directory.
    - Verification records that the two rebuilt `.lib` timestamps are newer than `processing_processor_mnn_tensor.cpp` and `quantization.cpp` so linked binaries match source per D-10.
  </acceptance_criteria>
  <verify>
    <automated>cd "W:/gnus/GeniusNetwork/SuperGenius/build/Windows/Release" &amp;&amp; cmake --build . --target SGProcessors sgprocmanagerquant &amp;&amp; powershell -NoProfile -Command "$lib1 = Get-Item 'W:/gnus/GeniusNetwork/SuperGenius/build/Windows/Release/SuperGenius/lib/SGProcessingManager/SGProcessors.lib'; $lib2 = Get-Item 'W:/gnus/GeniusNetwork/SuperGenius/build/Windows/Release/SuperGenius/lib/SGProcessingManager/sgprocmanagerquant.lib'; $src1 = Get-Item 'W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_tensor.cpp'; $src2 = Get-Item 'W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/src/util/quantization.cpp'; if ($lib1.LastWriteTime -lt $src1.LastWriteTime -or $lib2.LastWriteTime -lt $src2.LastWriteTime) { throw 'Release libraries are older than 13-01 source changes'; }"</automated>
  </verify>
  <done>The SuperGenius Release libraries NEO-SWARM links are freshly rebuilt from the post-13-01 source tree, satisfying D-10 before any release-mode SGFP4 integration test runs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Commit the inline SGFP4 fixture and wire it into the existing test-fixture copy path</name>
  <files>test/CMakeLists.txt, test/integration/processing_datatypes/sgfp4_tiny.mnn, test/integration/processing_datatypes/sgfp4_tiny_input.raw, test/integration/processing_datatypes/sgfp4_tiny.recipe.md</files>
  <read_first>
    - test/CMakeLists.txt
    - test/integration/processing_datatypes/tensor_tiny.mnn
    - test/integration/processing_datatypes/tensor_input.raw
    - W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/.planning/workstreams/neoswarm/phases/13-sgfp4-v2-model-support/13-CONTEXT.md
    - W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/.planning/quick/260825-pgu-evaluate-mnn-sgfp4-pivot-fp4-implementat/260825-pgu-EVALUATION.md
  </read_first>
  <action>Create `test/integration/processing_datatypes/sgfp4_tiny.mnn` as the committed Phase 13 SGFP4 fixture and `test/integration/processing_datatypes/sgfp4_tiny_input.raw` as its matching FLOAT32 input tensor. The model must be a deliberately tiny conv-family network whose conv weights exceed 4096 elements so `mnnconvert --sgfp4` inserts at least one `OpType_SGFP4Dequant`, but the total model remains inline and far below `_largeModel`'s externalization threshold per D-06. Record the exact manual production command, source model provenance, input shape, and the explicit note `--sgfp4` in `test/integration/processing_datatypes/sgfp4_tiny.recipe.md`. Extend the existing `copy_if_different` block for `test_sgprocessing_pipeline` in `test/CMakeLists.txt` so all three new fixture files are staged into `SUPERGENIUS_TEST_DATA_DIR/processing_datatypes/` beside the existing tensor fixtures.</action>
  <acceptance_criteria>
    - `test/integration/processing_datatypes/sgfp4_tiny.mnn` exists in source control and is smaller than the externalized-model threshold by construction.
    - `test/integration/processing_datatypes/sgfp4_tiny.recipe.md` contains the literal string `--sgfp4` and names the committed output file `sgfp4_tiny.mnn`.
    - `test/CMakeLists.txt` copies `sgfp4_tiny.mnn`, `sgfp4_tiny_input.raw`, and `sgfp4_tiny.recipe.md` into the `processing_datatypes` output directory for `test_sgprocessing_pipeline`.
    - After building `test_sgprocessing_pipeline`, the three files exist under `$<TARGET_FILE_DIR:test_sgprocessing_pipeline>/processing_datatypes/`.
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build/Windows/Release --target test_sgprocessing_pipeline &amp;&amp; ctest --test-dir build/Windows/Release -R SGProcessingBridge.BuildSchemaJson_Fp4Ultra --output-on-failure</automated>
  </verify>
  <done>The SGFP4 fixture and its input/provenance files are committed and staged automatically with the existing integration-test data copy rule.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add explicit CPU and Vulkan SGFP4 direct-path end-to-end tests</name>
  <files>test/integration/test_sgprocessing_pipeline.cpp</files>
  <read_first>
    - test/integration/test_sgprocessing_pipeline.cpp
    - src/core/sgprocessing/sg_processing_bridge.cpp
    - W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/include/processors/vulkan_gpu_probe.hpp
    - W:/gnus/GeniusNetwork/thirdparty/MNN/test/op/SGFP4ClassicAPITest.cpp
    - W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/.planning/workstreams/neoswarm/phases/13-sgfp4-v2-model-support/13-RESEARCH.md
  </read_first>
  <behavior>
    - The CPU test builds schema JSON for the SGFP4 fixture, injects `backend=cpu`, runs the full `SGProcessingBridge::SubmitDirect()` path, and returns non-empty output bytes.
    - The Vulkan test injects `backend=vulkan`, runs the same direct path, and returns non-empty output bytes when a usable Vulkan device exists.
    - The Vulkan test uses `HasUsableVulkanDevice()` only to skip hosts with no device present; on this machine it should execute real assertions rather than the old deadlock skip path.
    - Both tests assert the fixture contains at least one `OpType_SGFP4Dequant` op before dispatch.
  </behavior>
  <action>In `test_sgprocessing_pipeline.cpp`, add two new tests named `SGProcessingPipeline.Sgfp4DirectCpu_EndToEnd` and `SGProcessingPipeline.Sgfp4DirectVulkan_EndToEnd`. Reuse `TestDataPath()` and `FileExists()` for fixture lookup, then build a base schema with `SGProcessingBridge::BuildSchemaJson(..., sgns::InputFormat::FLOAT32, ...)` so the fixture's declared FLOAT32 input tensor schema stays distinct from the SGFP4 weight format per D-08. Inject a `parameters` entry named `backend` with values `cpu` or `vulkan`, and call `SGProcessingBridge::SubmitDirect(json, ioc)` so the exact direct-call path `ProcessingManager::Create()` -> `Process()` is exercised rather than a parallel MNN-only shortcut. Add a small helper `AssertModelContainsSgfp4DequantOp(...)` that loads `sgfp4_tiny.mnn` using the same public MNN classic-API access pattern used by `SGFP4ClassicAPITest.cpp` and asserts at least one op type equals `OpType_SGFP4Dequant` before dispatch. For the Vulkan case, guard only the absence-of-device condition with `if ( !sgns::sgprocessing::HasUsableVulkanDevice() ) { GTEST_SKIP() << "No usable Vulkan device present"; }`; do not cite the old VulkanInitMutex deadlock, because research verified it is closed on the current SGProcessingManager ancestor chain.</action>
  <acceptance_criteria>
    - `test_sgprocessing_pipeline.cpp` contains `Sgfp4DirectCpu_EndToEnd` and `Sgfp4DirectVulkan_EndToEnd` exactly.
    - Both tests call `SubmitDirect(...)` with schema JSON carrying a `backend` parameter; neither test silently falls back to the old default-only path.
    - Both tests assert at least one `OpType_SGFP4Dequant` op exists in `sgfp4_tiny.mnn` before dispatch.
    - CPU execution passes on a Release build without requiring Vulkan.
    - Vulkan execution passes on hosts with a usable Vulkan device and skips only on hosts without one.
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build/Windows/Release --target test_sgprocessing_pipeline &amp;&amp; ctest --test-dir build/Windows/Release -R "SGProcessingPipeline.Sgfp4Direct(Cpu|Vulkan)_EndToEnd" --output-on-failure</automated>
  </verify>
  <done>NEO-SWARM has real SGFP4 direct-path integration coverage for explicit CPU and Vulkan sessions, backed by a committed fixture that proves `OpType_SGFP4Dequant` is present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture model -> MNN loader | Committed test model bytes are parsed by MNN and then passed through SGProcessingManager's direct path. |
| Test JSON -> backend resolver | Test-created schema parameters control CPU vs Vulkan execution mode. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Tampering | SGFP4 fixture provenance | mitigate | Commit `sgfp4_tiny.recipe.md` beside the fixture and require the literal `--sgfp4` command/provenance in source control. |
| T-13-04 | Tampering | False-positive all-FP32 model fixture | mitigate | Assert at least one `OpType_SGFP4Dequant` op exists before dispatch per D-07. |
| T-13-05 | Denial of Service | Vulkan-path environment mismatch | mitigate | Skip only when `HasUsableVulkanDevice()` reports no device; otherwise execute the real Vulkan assertions. |
| T-13-SC | Tampering | package/build inputs | accept | No new package installs; uses existing MNN and SGProcessingManager builds. |
</threat_model>

<verification>
First perform the explicit SuperGenius Release rebuild and timestamp check from Task 1, then build the NEO-SWARM Release test target, run the two new SGFP4 integration tests, and confirm the staged fixture files exist in the build output directory. Because this plan depends on 13-01, do not interpret any NEO-SWARM integration result until the linked SGProcessingManager static libraries have been proven newer than the 13-01 source edits per D-10.
</verification>

<success_criteria>
- [ ] The SGFP4 fixture, matching input, and literal manual recipe are committed.
- [ ] `test_sgprocessing_pipeline` stages those files automatically.
- [ ] The CPU direct-path SGFP4 test passes.
- [ ] The Vulkan direct-path SGFP4 test passes when a usable Vulkan device exists, otherwise skips only for missing-device hosts.
- [ ] Both tests prove the fixture contains at least one `OpType_SGFP4Dequant` op.
</success_criteria>

<output>
Create `.planning/workstreams/neoswarm/phases/13-sgfp4-v2-model-support/13-02-SUMMARY.md` when done
</output>
