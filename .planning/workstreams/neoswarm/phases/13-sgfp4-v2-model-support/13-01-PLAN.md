---
phase: 13-sgfp4-v2-model-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/include/util/quantization.hpp"
  - "W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/src/util/quantization.cpp"
  - "W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_tensor.cpp"
  - "W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/test/util/quantization_test.cpp"
  - "W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/test/processors/mnn_tensor_fp4_test.cpp"
autonomous: true
requirements: [SGF-01, SGF-02, SGF-04]

must_haves:
  truths:
    - "D-09: cross-repo commits in this plan land innermost-first (SGProcessingManager → SuperGenius → GeniusNetwork root → NEO-SWARM), with `git status --short` verified at each level before moving on."
    - "Per D-04 and D-05, the MNN_Tensor processor can create either an explicit CPU session or an explicit Vulkan session from a schema parameter instead of silently hardcoding Vulkan."
    - "Per D-11, malformed or incompatible model bytes return a structured ProcessingError from StartProcessing() instead of crashing on procresults->host<float>()."
    - "Per SGF-04a, the FP4_ULTRA unit coverage no longer asserts decode is unavailable; it proves the format passes the input-format gate and any failure is downstream model/session handling."
  artifacts:
    - path: "W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/include/util/quantization.hpp"
      provides: "ResolveMnnBackend declaration with backend parameter contract"
    - path: "W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/src/util/quantization.cpp"
      provides: "ResolveMnnBackend implementation with Vulkan fallback"
    - path: "W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_tensor.cpp"
      provides: "parameter-driven MNN_Tensor session selection and null-safe error propagation"
    - path: "W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/test/processors/mnn_tensor_fp4_test.cpp"
      provides: "negative regression coverage for malformed model buffers and non-stale FP4_ULTRA expectations"
  key_links:
    - from: "W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_tensor.cpp"
      to: "W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/src/util/quantization.cpp"
      via: "StartProcessing() resolves backend from parameters before Process() builds ScheduleConfig"
      pattern: "ResolveMnnBackend\(|config\.type"
    - from: "W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/test/processors/mnn_tensor_fp4_test.cpp"
      to: "W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_tensor.cpp"
      via: "direct StartProcessing() regression coverage"
      pattern: "MalformedModelBufferReturnsCleanErrorNoCrash|Fp4Ultra"
---

<objective>
Implement the SuperGenius-side prerequisite work that makes Phase 13's real SGFP4 direct-path validation possible. Per D-04 and D-05, `MNN_Tensor::Process()` must stop hardcoding `MNN_FORWARD_VULKAN` and accept an explicit schema-selected backend so Phase 13 can prove both CPU and Vulkan execution. Per D-11 and SGF-02, `StartProcessing()` must honor `Process()` failure and return a structured `ProcessingError` instead of dereferencing a null tensor pointer. Per SGF-04a, the stale FP4_ULTRA unit test must be rewritten so it no longer asserts decode is unavailable.

Purpose: make CPU/Vulkan backend selection real before the NEO-SWARM end-to-end tests are written, and close the malformed-model crash in the same file while the control flow is open.
Output: backend resolver utility, null-safe `MNN_Tensor` processing flow, fresh SuperGenius unit coverage proving the malformed-model path errors cleanly and the FP4_ULTRA expectation is no longer stale.
</objective>

## Artifacts this phase produces

- `sgns::sgprocmanagerquant::ResolveMnnBackend( const std::vector<sgns::Parameter> *parameters )`
- Updated `sgns::sgprocessing::MNN_Tensor::StartProcessing(...)` backend-resolution call site
- Updated `sgns::sgprocessing::MNN_Tensor::Process(...)` signature/body to consume the resolved backend
- `QuantizationTest.ResolveMnnBackendFallsBackOnNullParameters`
- `QuantizationTest.ResolveMnnBackendUsesCpuWhenRequested`
- `QuantizationTest.ResolveMnnBackendUsesVulkanWhenRequested`
- `QuantizationTest.ResolveMnnBackendFallsBackOnInvalidString`
- `MnnTensorFp4Test.MalformedModelBufferReturnsCleanErrorNoCrash`
- `MnnTensorFp4Test.Fp4UltraRecognizedAndMalformedModelReturnsCleanError`

<execution_context>
@C:/Users/Fuu/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Fuu/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/.planning/workstreams/neoswarm/ROADMAP.md
@W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/.planning/workstreams/neoswarm/REQUIREMENTS.md
@W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/.planning/workstreams/neoswarm/STATE.md
@W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/.planning/workstreams/neoswarm/phases/13-sgfp4-v2-model-support/13-CONTEXT.md
@W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/.planning/workstreams/neoswarm/phases/13-sgfp4-v2-model-support/13-RESEARCH.md
@W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/.planning/workstreams/neoswarm/phases/13-sgfp4-v2-model-support/13-PATTERNS.md
@W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_tensor.cpp
@W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/include/util/quantization.hpp
@W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/src/util/quantization.cpp
@W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/test/processors/mnn_tensor_fp4_test.cpp
@W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/test/util/quantization_test.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a schema-driven backend resolver for CPU vs Vulkan sessions</name>
  <files>W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/include/util/quantization.hpp, W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/src/util/quantization.cpp, W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/test/util/quantization_test.cpp</files>
  <read_first>
    - W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/include/util/quantization.hpp
    - W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/src/util/quantization.cpp
    - W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/test/util/quantization_test.cpp
    - W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_tensor.cpp
    - W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/.planning/workstreams/neoswarm/phases/13-sgfp4-v2-model-support/13-RESEARCH.md
  </read_first>
  <behavior>
    - `parameters == nullptr` returns `MNN_FORWARD_VULKAN` exactly, preserving today's default behavior.
    - A `Parameter` named `backend` with type `STRING` and default `"cpu"` resolves to `MNN_FORWARD_CPU`.
    - A `Parameter` named `backend` with type `STRING` and default `"vulkan"` resolves to `MNN_FORWARD_VULKAN`.
    - Missing entries, wrong types, or unsupported strings fall back to `MNN_FORWARD_VULKAN`.
  </behavior>
  <action>Add `ResolveMnnBackend( const std::vector<sgns::Parameter> *parameters )` to `include/util/quantization.hpp` and implement it in `src/util/quantization.cpp`, following the same find-by-name pattern as `ResolveQuantScale(parameters)` and `ResolveByteQuantMode(parameters)`. The resolver must look for a parameter named `backend` of type `sgns::ParameterType::STRING`, lowercase the declared default string, accept only `cpu` and `vulkan`, and return `MNN_FORWARD_VULKAN` for null, missing, wrong-type, or unsupported values so the current additive behavior is unchanged per D-04. Extend `test/util/quantization_test.cpp` with explicit unit cases for null, missing, cpu, vulkan, and invalid-string fallback so the new helper is proven before it is threaded into the processor.</action>
  <acceptance_criteria>
    - `quantization.hpp` declares `ResolveMnnBackend( const std::vector<sgns::Parameter> *parameters )` alongside the existing `ResolveQuantScale` helpers.
    - `quantization.cpp` returns `MNN_FORWARD_CPU` only for a schema parameter named `backend` with string default `cpu`, and `MNN_FORWARD_VULKAN` for `vulkan`, null, missing, invalid, or unsupported values.
    - `quantization_test.cpp` contains four new `ResolveMnnBackend...` test cases covering null fallback, explicit CPU, explicit Vulkan, and invalid-string fallback.
    - `ctest --test-dir build/Windows/Debug -R QuantizationTest --output-on-failure` passes in SGProcessingManager.
  </acceptance_criteria>
  <verify>
    <automated>cd "W:/gnus/GeniusNetwork/SuperGenius/build/Windows/Debug" &amp;&amp; cmake --build . --target quantization_test &amp;&amp; ctest --test-dir . -R QuantizationTest --output-on-failure</automated>
  </verify>
  <done>The backend resolver exists, defaults to Vulkan, recognizes explicit CPU/Vulkan schema values, and is covered by the quantization unit-test target.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread the resolved backend through MNN_Tensor and harden the null-return path</name>
  <files>W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_tensor.cpp, W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/test/processors/mnn_tensor_fp4_test.cpp</files>
  <read_first>
    - W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_tensor.cpp
    - W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/test/processors/mnn_tensor_fp4_test.cpp
    - W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/include/util/quantization.hpp
    - W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager/src/util/quantization.cpp
    - W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/.planning/workstreams/neoswarm/phases/13-sgfp4-v2-model-support/13-CONTEXT.md
  </read_first>
  <behavior>
    - `StartProcessing()` resolves a backend once per invocation and passes it into `Process()`.
    - CPU requests create an MNN session with `config.type = MNN_FORWARD_CPU` and do not take `VulkanInitMutex()`.
    - Vulkan requests keep `config.type = MNN_FORWARD_VULKAN` and preserve the existing `VulkanInitMutex()` locking around `createSession()`.
    - Malformed model bytes that make `Process()` return `nullptr` produce a structured `ProcessingError` instead of crashing.
  </behavior>
  <action>In `processing_processor_mnn_tensor.cpp`, call `ResolveMnnBackend(parameters)` beside the existing `ResolveQuantScale(parameters)` call, thread the resolved `MNNForwardType` into `Process(...)`, and replace the hardcoded `config.type = MNN_FORWARD_VULKAN` assignment with the resolved value. Keep the current locked `createSession()` branch for Vulkan, but create CPU sessions without taking `VulkanInitMutex()` so the CPU path is genuinely distinct per D-05. Immediately after `auto procresults = Process(...)`, add a null check that returns `ProcessingResult{ {}, nullptr, {}, ProcessingError{ ProcessingErrorStage::FORMAT_UNSUPPORTED, "MNN_Tensor::Process returned null (malformed or incompatible model)" } }` before any `procresults->host<float>()` dereference. Rewrite the stale `Fp4UltraRecognizedButDecodeUnavailable` test into `Fp4UltraRecognizedAndMalformedModelReturnsCleanError`, asserting that FP4_ULTRA input reaches the downstream model/session failure path instead of a `FORMAT_UNSUPPORTED` decode-unavailable assertion, and add `MalformedModelBufferReturnsCleanErrorNoCrash` to prove D-11's malformed-model regression explicitly.</action>
  <acceptance_criteria>
    - `processing_processor_mnn_tensor.cpp` no longer hardcodes `config.type = MNN_FORWARD_VULKAN` without a resolver-controlled override.
    - The only `VulkanInitMutex()` lock remains on the Vulkan branch; CPU session creation bypasses that lock.
    - `StartProcessing()` checks `procresults` before every dereference and returns the exact structured error message `MNN_Tensor::Process returned null (malformed or incompatible model)` on null.
    - `mnn_tensor_fp4_test.cpp` contains `MalformedModelBufferReturnsCleanErrorNoCrash` and a rewritten FP4_ULTRA regression that no longer expects `decode unavailable` or `MNN_Ultra` in the message.
    - `ctest --test-dir build/Windows/Debug -R MNNTensorFp4Test --output-on-failure` passes after a fresh SGProcessingManager rebuild per D-10.
  </acceptance_criteria>
  <verify>
    <automated>cd "W:/gnus/GeniusNetwork/SuperGenius/build/Windows/Debug" &amp;&amp; cmake --build . --target mnn_tensor_fp4_test SGProcessors sgprocmanagerquant &amp;&amp; ctest --test-dir . -R MNNTensorFp4Test --output-on-failure</automated>
  </verify>
  <done>`MNN_Tensor` can be driven to explicit CPU or Vulkan sessions from schema parameters, the null dereference is removed, and the SGF-02/SGF-04a unit regressions pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model bytes -> `MNN_Tensor::Process()` | Untrusted or malformed model data crosses into interpreter/session creation. |
| Job schema parameters -> backend selection | Untrusted schema values choose CPU or Vulkan execution policy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Denial of Service | `processing_processor_mnn_tensor.cpp` null return path | mitigate | Per SGF-02 and D-11, null-check `procresults` and return a structured `ProcessingError` before dereferencing. |
| T-13-02 | Tampering | `backend` schema parameter | mitigate | Accept only lowercase-normalized `cpu` and `vulkan`; all other values fall back to Vulkan rather than selecting an unintended backend. |
| T-13-SC | Tampering | package/build inputs | accept | No new packages are installed in this plan; rebuild uses the already-present SGProcessingManager toolchain. |
</threat_model>

<verification>
Run the SGProcessingManager Debug build after both tasks, then execute `QuantizationTest` and `MNNTensorFp4Test`. Before leaving the submodule, verify `git -C W:/gnus/GeniusNetwork/SuperGenius/SGProcessingManager status --short` shows only the expected source/test changes, then verify `git -C W:/gnus/GeniusNetwork/SuperGenius status --short` shows only the SGProcessingManager pointer update per D-09.
</verification>

<success_criteria>
- [ ] `ResolveMnnBackend(...)` exists and defaults to Vulkan when the schema is missing or invalid.
- [ ] `MNN_Tensor::StartProcessing()` can select CPU or Vulkan explicitly from parameters.
- [ ] A malformed model buffer no longer crashes `StartProcessing()`.
- [ ] The stale FP4_ULTRA test no longer asserts decode is unavailable.
- [ ] SGProcessingManager Debug targeted tests pass and the innermost-first git status checks are clean.
</success_criteria>

<output>
Create `.planning/workstreams/neoswarm/phases/13-sgfp4-v2-model-support/13-01-SUMMARY.md` when done
</output>
