---
phase: 13-sgfp4-v2-model-support
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/core/CMakeLists.txt"
  - "src/core/fp4/fp4_codec.hpp"
  - "src/core/fp4/fp4_codec.cpp"
  - "test/CMakeLists.txt"
  - "test/core/test_fp4_codec.cpp"
  - "test/integration/test_sg_connectivity.cpp"
autonomous: true
requirements: [SGF-03, SGF-04]

must_haves:
  truths:
    - "Per SGF-03, the orphaned NF4 `fp4_codec` implementation and its standalone test target are removed entirely from NEO-SWARM."
    - "Per SGF-04b, the FP4_ULTRA schema regression now asserts `type=tensor` and `format=FP4_ULTRA` rather than the stale lowercase literal."
    - "Per D-08, no new code or tests in this cleanup plan call SGFP4 by the wrong `Ultra FP4` name."
  artifacts:
    - path: "src/core/CMakeLists.txt"
      provides: "neoswarm_core source list without fp4/fp4_codec.cpp"
    - path: "test/CMakeLists.txt"
      provides: "test target list without test_fp4_codec"
    - path: "test/integration/test_sg_connectivity.cpp"
      provides: "correct FP4_ULTRA schema assertions"
  key_links:
    - from: "src/core/CMakeLists.txt"
      to: "src/core/fp4/fp4_codec.cpp"
      via: "source list removal"
      pattern: "fp4/fp4_codec\.cpp"
    - from: "test/CMakeLists.txt"
      to: "test/core/test_fp4_codec.cpp"
      via: "test target removal"
      pattern: "test_fp4_codec"
    - from: "test/integration/test_sg_connectivity.cpp"
      to: "test/integration/test_sgprocessing_pipeline.cpp"
      via: "mirrors the canonical FP4_ULTRA schema assertions"
      pattern: "format\":\"FP4_ULTRA\"|type\":\"tensor\""
---

<objective>
Remove the dead NF4 code that Research Finding 7 confirmed is self-contained and unused, and fix the stale NEO-SWARM FP4_ULTRA schema assertion in the same cleanup slice. SGF-03 is intentionally self-contained: delete `src/core/fp4/fp4_codec.hpp`, `src/core/fp4/fp4_codec.cpp`, and `test/core/test_fp4_codec.cpp`, then remove the two owning CMake entries. SGF-04b is equally local: update `SGConnectivity.BuildSchemaJsonFP4UltraFormatEmitsFP4Type` so it matches the already-correct `BuildSchemaJson_Fp4Ultra` assertions in `test_sgprocessing_pipeline.cpp`.

Purpose: remove now-misleading dead code and stale assertions before the new SGFP4 work lands, without entangling this cleanup with the backend-resolver or end-to-end fixture tasks.
Output: no `fp4_codec` sources or test target remain in NEO-SWARM, and the connectivity regression asserts the real schema contract for FP4_ULTRA.
</objective>

## Artifacts this phase produces

- No new runtime symbols; this plan's deliverable is deletion plus one corrected existing GTest case.
- Updated `SGConnectivity.BuildSchemaJsonFP4UltraFormatEmitsFP4Type`
- Deleted `src/core/fp4/fp4_codec.hpp`
- Deleted `src/core/fp4/fp4_codec.cpp`
- Deleted `test/core/test_fp4_codec.cpp`

<execution_context>
@C:/Users/Fuu/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Fuu/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/.planning/workstreams/neoswarm/ROADMAP.md
@W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/.planning/workstreams/neoswarm/REQUIREMENTS.md
@W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/.planning/workstreams/neoswarm/phases/13-sgfp4-v2-model-support/13-CONTEXT.md
@W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/.planning/workstreams/neoswarm/phases/13-sgfp4-v2-model-support/13-RESEARCH.md
@W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/.planning/workstreams/neoswarm/phases/13-sgfp4-v2-model-support/13-PATTERNS.md
@src/core/CMakeLists.txt
@src/core/fp4/fp4_codec.hpp
@src/core/fp4/fp4_codec.cpp
@test/CMakeLists.txt
@test/core/test_fp4_codec.cpp
@test/integration/test_sg_connectivity.cpp
@test/integration/test_sgprocessing_pipeline.cpp
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete the orphaned NF4 codec and remove its owning CMake entries</name>
  <files>src/core/CMakeLists.txt, src/core/fp4/fp4_codec.hpp, src/core/fp4/fp4_codec.cpp, test/CMakeLists.txt, test/core/test_fp4_codec.cpp</files>
  <read_first>
    - src/core/CMakeLists.txt
    - src/core/fp4/fp4_codec.hpp
    - src/core/fp4/fp4_codec.cpp
    - test/CMakeLists.txt
    - test/core/test_fp4_codec.cpp
    - W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/.planning/workstreams/neoswarm/phases/13-sgfp4-v2-model-support/13-RESEARCH.md
  </read_first>
  <action>Delete `src/core/fp4/fp4_codec.hpp`, `src/core/fp4/fp4_codec.cpp`, and `test/core/test_fp4_codec.cpp` outright. In `src/core/CMakeLists.txt`, remove the `fp4/fp4_codec.cpp` source-list entry from `add_library(neoswarm_core STATIC ...)`. In `test/CMakeLists.txt`, remove the `neoswarm_test(test_fp4_codec core/test_fp4_codec.cpp "neoswarm_core")` line and nothing else. After deletion, confirm no remaining source or test reference to `fp4_codec` or `FP4Codec` exists anywhere under `src/` or `test/` outside phase-planning documents.</action>
  <acceptance_criteria>
    - The three `fp4_codec` files are deleted from the working tree.
    - `src/core/CMakeLists.txt` contains no `fp4/fp4_codec.cpp` entry.
    - `test/CMakeLists.txt` contains no `test_fp4_codec` target.
    - `git grep -n "fp4_codec\|FP4Codec" -- src test` returns no matches.
    - `cmake --build build/Windows/Release --target neoswarm_core test_sg_connectivity` succeeds after the deletions.
  </acceptance_criteria>
  <verify>
    <automated>git grep -n "fp4_codec\|FP4Codec" -- src test &amp;&amp; cmake --build build/Windows/Release --target neoswarm_core test_sg_connectivity</automated>
  </verify>
  <done>The orphaned NF4 codec implementation, its unit test, and both owning CMake entries are gone with no residual references in source or test code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Correct the stale FP4_ULTRA schema assertion in the connectivity regression</name>
  <files>test/integration/test_sg_connectivity.cpp</files>
  <read_first>
    - test/integration/test_sg_connectivity.cpp
    - test/integration/test_sgprocessing_pipeline.cpp
    - src/core/sgprocessing/sg_processing_bridge.cpp
  </read_first>
  <behavior>
    - The connectivity regression asserts `"type":"tensor"` is present for FP4_ULTRA schema JSON.
    - The connectivity regression asserts `"format":"FP4_ULTRA"` is present.
    - The connectivity regression asserts lowercase `fp4_ultra` does not appear anywhere in the generated schema.
  </behavior>
  <action>Rewrite `SGConnectivity.BuildSchemaJsonFP4UltraFormatEmitsFP4Type` in `test/integration/test_sg_connectivity.cpp` so it mirrors the canonical assertions already present in `SGProcessingBridge.BuildSchemaJson_Fp4Ultra` inside `test_sgprocessing_pipeline.cpp`. Replace the stale comment about a dedicated `fp4_ultra` type string with the concrete contract: FP4_ULTRA jobs emit `"type":"tensor"`, emit `"format":"FP4_ULTRA"`, and never contain the lowercase literal `fp4_ultra` anywhere in the schema body.</action>
  <acceptance_criteria>
    - `test_sg_connectivity.cpp` asserts `"type":"tensor"` is present for FP4_ULTRA JSON.
    - `test_sg_connectivity.cpp` asserts `"format":"FP4_ULTRA"` is present.
    - `test_sg_connectivity.cpp` asserts `fp4_ultra` is absent.
    - `ctest --test-dir build/Windows/Release -R SGConnectivity.BuildSchemaJsonFP4UltraFormatEmitsFP4Type --output-on-failure` passes.
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build/Windows/Release --target test_sg_connectivity &amp;&amp; ctest --test-dir build/Windows/Release -R SGConnectivity.BuildSchemaJsonFP4UltraFormatEmitsFP4Type --output-on-failure</automated>
  </verify>
  <done>The connectivity regression matches the real FP4_ULTRA schema contract and no longer contradicts the pipeline test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test assertions -> schema contract | Source assertions must reflect the actual JSON emitted by SGProcessingBridge. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Tampering | stale FP4 schema assertions | mitigate | Mirror the exact canonical assertions from `test_sgprocessing_pipeline.cpp` so the connectivity test cannot validate the inverse contract. |
| T-13-07 | Denial of Service | dead code surface | mitigate | Delete the unused NF4 codec entirely so future changes cannot accidentally route through an unsupported parallel implementation. |
| T-13-SC | Tampering | package/build inputs | accept | No package changes; build verifies only the existing local toolchain. |
</threat_model>

<verification>
After the deletions and assertion rewrite, build the narrowed NEO-SWARM targets and run the single FP4_ULTRA connectivity regression. Confirm `git status --short` shows only the expected source deletions and test/CMake edits before moving to the dependent SGFP4 fixture plan.
</verification>

<success_criteria>
- [ ] No `fp4_codec` source or test file remains in NEO-SWARM.
- [ ] `neoswarm_core` and `test_sg_connectivity` still build.
- [ ] The FP4_ULTRA connectivity regression matches the canonical tensor/format assertions.
- [ ] There are no residual `fp4_codec` references in `src/` or `test/`.
</success_criteria>

<output>
Create `.planning/workstreams/neoswarm/phases/13-sgfp4-v2-model-support/13-03-SUMMARY.md` when done
</output>
