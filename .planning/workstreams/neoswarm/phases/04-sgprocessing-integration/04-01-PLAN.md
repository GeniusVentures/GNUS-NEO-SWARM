---
phase: 04-sgprocessing-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - GNUS-NEO-SWARM/src/core/CMakeLists.txt
  - GNUS-NEO-SWARM/test/CMakeLists.txt
  - GNUS-NEO-SWARM/src/core/sgprocessing/sg_processing_bridge.cpp
  - GNUS-NEO-SWARM/test/integration/test_sgprocessing_pipeline.cpp
  - GNUS-NEO-SWARM/.planning/workstreams/neoswarm/codebase/CONCERNS.md
  - GNUS-NEO-SWARM/.planning/workstreams/neoswarm/PROJECT.md
autonomous: true
requirements: [PROC-03, FIX-04]

must_haves:
  truths:
    - "neoswarm_core links against GeniusNetwork's SuperGenius build output (12 SGProcessingManager libs), not the old nonexistent ../SuperGenius sibling path"
    - "SGProcessingBridge::SubmitDirect() compiles against the current ProcessOutput-returning ProcessingManager::Process() signature"
    - "A FP4_ULTRA-format job schema declares JSON type=tensor (not the invalid fp4_ultra), so it can pass DataType::from_json and reach a registered processor"
    - "CONCERNS.md and PROJECT.md no longer claim an active SentencePiece/SGProcessing protobuf conflict that doesn't exist in the codebase"
  artifacts:
    - "GNUS-NEO-SWARM/src/core/CMakeLists.txt (GENIUSNETWORK_SUPERGENIUS_DIR cache variable, repointed SGProcessingManager block, 6 new find_library calls)"
    - "GNUS-NEO-SWARM/test/CMakeLists.txt (SUPERGENIUS_TEST_DATA_DIR repointed)"
  key_links:
    - "src/core/CMakeLists.txt's _SGPROC_LIB_DIR -> GeniusNetwork/SuperGenius/build/<Platform>/<BuildType>/SuperGenius/lib/SGProcessingManager (12 libs)"
    - "SGProcessingBridge::InputFormatToTypeString(FP4_ULTRA) -> tensor -> DataType::TENSOR dispatch (ProcessingManager.cpp SetProcessorByName)"
---

<objective>
This plan is the NEO-SWARM-repo half of Phase 4's genuinely cross-repo scope (D-01) — its sibling plans 04-02/04-03 land in `GeniusNetwork/SuperGenius` (`dev_childwallet` branch, the sole canonical SuperGenius checkout now that `W:\gnus\SuperGenius`'s stale `dev_persisprocresults` checkout has been deleted — D-03). Fix the three concrete, pre-existing defects that block NEO-SWARM from ever successfully linking against and calling into the real (dev_childwallet) SGProcessingManager: (1) the CMake link path still points at a deleted/nonexistent sibling `../SuperGenius` checkout instead of `GeniusNetwork/SuperGenius`'s prebuilt output (D-02, D-12, Pitfall 5), (2) `SGProcessingBridge::SubmitDirect()` will not compile against the current `ProcessingManager::Process()` signature, which now returns `ProcessOutput` instead of a bare `std::vector<uint8_t>` (Pitfall 1), and (3) `InputFormatToTypeString(FP4_ULTRA)` emits an invalid `DataType` JSON string ("fp4_ultra") that fails `sgns::from_json` and would make every default-config SGProcessing job return `Error::INVALID_JSON` today (Pitfall 2). Also corrects two stale documentation claims (D-11) describing an active SentencePiece/protobuf conflict that does not exist in this codebase.

This plan resolves PROC-03 (protobuf conflict resolved by consolidation, not a code fix — D-11) and FIX-04 (test binaries link cleanly against SGProcessingManager) for the platform(s) available on this machine (Windows).

Purpose: Every other plan in this phase (04-02, 04-03, 04-04) depends on NEO-SWARM actually being able to compile and link against SGProcessingManager's real headers/libs. Without this plan, `sg_processing_bridge.cpp` does not compile once real headers are on the include path, making the rest of the phase unverifiable.
Output: `neoswarm_core` configures, compiles, and links against `GeniusNetwork/SuperGenius`'s real build output; `SGProcessingBridge` compiles cleanly and builds a valid FP4_ULTRA schema; CONCERNS.md/PROJECT.md read correctly for a `dev_childwallet`-consolidated build.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@W:\gnus\GeniusCognitiveSystem\GNUS-NEO-SWARM\.planning\workstreams\neoswarm\phases\04-sgprocessing-integration\04-CONTEXT.md
@W:\gnus\GeniusCognitiveSystem\GNUS-NEO-SWARM\.planning\workstreams\neoswarm\phases\04-sgprocessing-integration\04-RESEARCH.md
@W:\gnus\GeniusCognitiveSystem\GNUS-NEO-SWARM\.planning\workstreams\neoswarm\phases\04-sgprocessing-integration\04-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Repoint SGProcessingManager CMake link path at GeniusNetwork's build output</name>
  <files>GNUS-NEO-SWARM/src/core/CMakeLists.txt, GNUS-NEO-SWARM/test/CMakeLists.txt</files>
  <read_first>
    - src/core/CMakeLists.txt:63-145 (current self-contained SGProcessingManager block — `_SGPROC_INCLUDE`/`_SGPROC_GEN_INCLUDE`/`_SGPROC_LIB_DIR` hardcode `${PROJECT_ROOT}/../SuperGenius/...`, resolving to `W:\gnus\GeniusCognitiveSystem\SuperGenius\...` — confirmed nonexistent)
    - cmake/CommonBuildParameters.cmake:380-396 (the existing `SUPERGENIUS_BUILD_DIR`/`SGPROCESSING_DIR` auto-detection is a DIFFERENT, wider mechanism whose `PROJECT_SUPER_ROOT` resolves via `get_third_party_dir()`'s sibling-`thirdparty` walk-up — confirmed this walk-up cannot reach `GeniusNetwork` either post-app-restructure, since the real `thirdparty` lives at `GeniusNetwork/thirdparty`, a location the walk-up algorithm structurally cannot check. Do NOT fix this wider mechanism here — it is a separate, larger, out-of-phase-scope build-configuration risk affecting every module (network, storage, core), not just SGProcessingManager. Flag it in a code comment; fix only the local, self-contained SGProcessingManager block.)
    - test/CMakeLists.txt:37-39 (`SUPERGENIUS_TEST_DATA_DIR` hardcodes the same broken `../SuperGenius/test/src` sibling path)
    - build/cmake/functions.cmake:158-168 (`get_default_root()` — confirms `PROJECT_ROOT` = GNUS-NEO-SWARM's own repo root, set once from `build/Windows/CMakeLists.txt`)
  </read_first>
  <action>
    In `src/core/CMakeLists.txt`, immediately before the `# SGProcessingManager integration` comment (current line 63), add a new overridable cache variable: `GENIUSNETWORK_SUPERGENIUS_DIR`, defaulted to `${PROJECT_ROOT}/../../GeniusNetwork/SuperGenius` (CACHE PATH), normalized via `cmake_path(SET ... NORMALIZE ...)`. Add a comment directly above it stating this is deliberately self-contained and does NOT reuse `SUPERGENIUS_BUILD_DIR`/`SGPROCESSING_DIR` from `cmake/CommonBuildParameters.cmake`, because that mechanism's `PROJECT_SUPER_ROOT` resolution also cannot reach `GeniusNetwork` post-app-restructure — a separate, wider, out-of-phase pre-existing issue this phase's PROC-03/FIX-04 scope does not absorb.

    Replace the existing `_SGPROC_INCLUDE`/`_SGPROC_GEN_INCLUDE`/`_SGPROC_LIB_DIR` definitions (current lines 67-69) to derive from `GENIUSNETWORK_SUPERGENIUS_DIR` instead of `${PROJECT_ROOT}/../SuperGenius` and `${THIRDPARTY_BUILD_DIR}`:
    - `_SGPROC_INCLUDE` = `${GENIUSNETWORK_SUPERGENIUS_DIR}/SGProcessingManager/include`
    - `_SGPROC_GEN_INCLUDE` = `${GENIUSNETWORK_SUPERGENIUS_DIR}/SGProcessingManager/generated`
    - `_SGPROC_LIB_DIR` = `${GENIUSNETWORK_SUPERGENIUS_DIR}/build/${BUILD_PLATFORM_NAME}/${CMAKE_BUILD_TYPE}${ABI_SUBFOLDER_NAME}/SuperGenius/lib/SGProcessingManager` (confirmed exact path for Windows Release: `GeniusNetwork/SuperGenius/build/Windows/Release/SuperGenius/lib/SGProcessingManager/`, containing 12 `.lib` files)

    Add 6 new `find_library(... PATHS "${_SGPROC_LIB_DIR}" NO_DEFAULT_PATH)` calls immediately after the existing 6 (current lines 71-76), following the exact one-line-per-library idiom already used, for the libraries confirmed present but not yet linked: `SGArtifacts`, `SGCapability`, `SGShaderCompiler`, `sgproccapture`, `sgprocmanagerdiff`, `sgprocmanagerquant` (variables `_SGPROC_ARTIFACTS`, `_SGPROC_CAPABILITY`, `_SGPROC_SHADERCOMPILER`, `_SGPROC_CAPTURE`, `_SGPROC_DIFF`, `_SGPROC_QUANT`). Add all 6 new variables to the existing `target_link_libraries(neoswarm_core PUBLIC ...)` call (current lines 83-90) alongside the original 6 — `SGCapability` and `SGArtifacts` are structurally required transitively by `ProcessingManager` itself; `sgprocmanagerquant` is required by every MNN processor's quantization-capture call; link all 6 rather than guessing which are unreachable, since an unused static lib link is harmless but a missing one produces unresolved-symbol linker errors.

    Do not change the `if(_SGPROC_PROCESSING_BASE AND EXISTS ...)` guard structure, the `-ld_classic`/`APPLE` block (current lines 130-132), or the `else()` stub-mode fallback (current lines 134-144) — only the path variables and the linked-library list change. Preserve the `if(TARGET ...)`-unconditional-link-or-stub pattern per CLAUDE.md's no-`#ifdef` rule.

    In `test/CMakeLists.txt`, change the `SUPERGENIUS_TEST_DATA_DIR` default (current line 38) from `"${CMAKE_SOURCE_DIR}/../SuperGenius/test/src"` to `"${GENIUSNETWORK_SUPERGENIUS_DIR}/test/src"`. `test/CMakeLists.txt` is processed after `src/core/CMakeLists.txt` in this project's `add_subdirectory` order, so `GENIUSNETWORK_SUPERGENIUS_DIR` is already a defined CACHE variable by the time this line runs; if configure fails with an undefined-variable warning, add a fallback `if(NOT DEFINED GENIUSNETWORK_SUPERGENIUS_DIR)` block here with the same default. Confirmed real fixture path once repointed: `GeniusNetwork/SuperGenius/test/src/processing_datatypes/` (contains `bert-tiny.mnn`, `audio-processing-definition.json`, etc.).
  </action>
  <acceptance_criteria>
    Fresh CMake configure in `build/Windows/Release` (or `Debug`) succeeds and its `-- SGProcessingManager: linked (...)` status message shows the new `GeniusNetwork/SuperGenius/...` path (not the old `../SuperGenius` path or "stub mode"). `ninja neoswarm_core` compiles and links without unresolved-symbol errors referencing `CapabilityValidator`, `ExecutionManifest`, or `sgprocmanagerquant::*`.
  </acceptance_criteria>
  <verify>
    <automated>cd "W:\gnus\GeniusCognitiveSystem\GNUS-NEO-SWARM" && cmake -S build/Windows -B build/Windows/Release -G Ninja -DCMAKE_BUILD_TYPE=Release 2>&1 | grep -i "SGProcessingManager: linked" && cmake --build build/Windows/Release --target neoswarm_core</automated>
  </verify>
  <done>CMake configure reports SGProcessingManager linked against the GeniusNetwork/SuperGenius path; neoswarm_core builds with zero unresolved-symbol errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix SGProcessingBridge compile-break and type/format field confusion</name>
  <files>GNUS-NEO-SWARM/src/core/sgprocessing/sg_processing_bridge.cpp, GNUS-NEO-SWARM/test/integration/test_sgprocessing_pipeline.cpp</files>
  <read_first>
    - src/core/sgprocessing/sg_processing_bridge.cpp:39-62 (`InputFormatToTypeString` — `FP4_ULTRA` case returns `"fp4_ultra"`, an invalid `DataType` string)
    - src/core/sgprocessing/sg_processing_bridge.cpp:292-344 (`SubmitDirect` — `pm->Process(...)` now returns `outcome::result&lt;ProcessOutput&gt;`; line 338's `outcome::success( process_result.value() )` will not compile since `ProcessOutput` has no implicit conversion to `std::vector&lt;uint8_t&gt;`; lines 340-344 are dead/unreachable code after the real `return` on line 338)
    - GeniusNetwork/SuperGenius/SGProcessingManager/include/processingbase/ProcessingManager.hpp:46-60 (`ProcessOutput` struct — has `size()`/`empty()`/`begin()`/`end()` delegating to its `combinedHash` member, but no conversion operator)
    - GeniusNetwork/SuperGenius/SGProcessingManager/generated/Generators.hpp:598-618 (`DataType::from_json` recognizes `"tensor"` among 15 valid names; `"fp4_ultra"` is not one of them)
  </read_first>
  <behavior>
    - `InputFormatToTypeString(sgns::InputFormat::FP4_ULTRA)` returns `"tensor"` (matching the `default:` case's existing behavior for other numeric formats) — not `"fp4_ultra"`.
    - `InputFormatToFormatString(sgns::InputFormat::FP4_ULTRA)` is unchanged — still returns `"FP4_ULTRA"` (already correct).
    - `SubmitDirect()` compiles and, on a successful `Process()` call, returns `outcome::success(std::vector&lt;uint8_t&gt;(...))` constructed from `process_result.value()`'s `begin()`/`end()` iterators — not the `ProcessOutput` object itself.
    - The unreachable dead code after the real `return` in `SubmitDirect()` is removed.
  </behavior>
  <action>
    In `InputFormatToTypeString` (sg_processing_bridge.cpp, current lines 39-62), change the `case sgns::InputFormat::FP4_ULTRA:` body from returning the literal type string tied to a dedicated processor, to returning the same string the `default:` case already returns for other numeric/tensor-ish formats, updating the trailing comment to note FP4_ULTRA is communicated via the separate `"format"` field (`InputFormatToFormatString`), not the `"type"`/`DataType` selector field — `InputFormat` (wire encoding) must not be conflated with `DataType` (processor selector).

    In `SubmitDirect()` (current lines 292-344), replace line 338's return statement so it wraps an explicitly-constructed `std::vector&lt;uint8_t&gt;` built from `process_result.value()`'s `begin()`/`end()` iterators, instead of passing the `ProcessOutput` value directly to `outcome::success(...)`. Delete the now-fully-unreachable code after that return (current lines 340-344: the `(void) jsondata; (void) ioc;` discards, the stub-mode warn log, and its own `return outcome::success(...)`) — dead code that predates this fix and was never reachable even before this change.
  </action>
  <acceptance_criteria>
    `git grep -n fp4_ultra src/core/sgprocessing/sg_processing_bridge.cpp` returns zero matches (the literal is gone from the type-string mapping). `git grep -c "process_result.value().begin()" src/core/sgprocessing/sg_processing_bridge.cpp` returns 1. A new GTest case in `test/integration/test_sgprocessing_pipeline.cpp` asserts `BuildSchemaJson(..., sgns::InputFormat::FP4_ULTRA, ...)`'s output JSON contains `"type":"tensor"` and `"format":"FP4_ULTRA"` (both present; `"fp4_ultra"` is not present anywhere in the JSON) — this test needs no Vulkan/`ProcessingManager::Create()` call since `BuildSchemaJson` is pure JSON construction, so it runs deterministically on every machine including this one.
  </acceptance_criteria>
  <verify>
    <automated>cd "W:\gnus\GeniusCognitiveSystem\GNUS-NEO-SWARM" && cmake --build build/Windows/Release --target test_sgprocessing_pipeline && ctest --test-dir build/Windows/Release -R "SGProcessingBridge.BuildSchemaJson_Fp4Ultra" --output-on-failure</automated>
  </verify>
  <done>InputFormatToTypeString(FP4_ULTRA) returns "tensor"; SubmitDirect() compiles against the current ProcessOutput-returning Process() signature; the new schema-generation regression test passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Correct stale SentencePiece/protobuf-conflict claims in project docs (D-11)</name>
  <files>GNUS-NEO-SWARM/.planning/workstreams/neoswarm/codebase/CONCERNS.md, GNUS-NEO-SWARM/.planning/workstreams/neoswarm/PROJECT.md</files>
  <read_first>
    - .planning/workstreams/neoswarm/codebase/CONCERNS.md:79-90 (dated 2026-05-27, "Known Bugs" section: "Test Binaries Fail to Link with SGProcessingManager" and "SentencePiece/SGProcessing Protobuf Conflict" — both describe `GENIUS_HAS_SGPROCESSING`/`GENIUS_HAS_SENTENCEPIECE` compile flags that no longer exist under the current no-`#ifdef` architecture, and a protobuf conflict RESEARCH.md confirmed does not exist: zero references to SentencePiece anywhere in the current source tree)
    - .planning/workstreams/neoswarm/codebase/CONCERNS.md:188-191 ("Dependencies at Risk" section, "SentencePiece / Protobuf Conflict" — same stale claim)
    - .planning/workstreams/neoswarm/PROJECT.md:82-88 ("Known Issues" bullet: "SentencePiece and SGProcessing have protobuf symbol conflict (cannot link both)")
  </read_first>
  <action>
    In CONCERNS.md, replace the "SentencePiece/SGProcessing Protobuf Conflict" subsection under "Known Bugs" (current lines 87-90) with a short corrected note: SentencePiece does not exist anywhere in the current source tree (no implementation file, no compile flag) as of Phase 4 (2026-08-18) research; there is no active protobuf conflict to resolve. The remaining protobuf-consolidation concern is that NEO-SWARM must link a single, consistent SGProcessingManager build (resolved by Phase 4's CMake relink to GeniusNetwork's SuperGenius build output, plan 04-01). Update the "Test Binaries Fail to Link with SGProcessingManager" subsection (current lines 81-85) to note the `-ld_classic` workaround is a PUBLIC/INTERFACE `target_link_options` on `neoswarm_core`, which propagates transitively to every `neoswarm_test()`-built test binary via normal CMake target-property propagation (re-verify empirically per D-12, not fixed status). In "Dependencies at Risk" (current lines 188-191), replace the "SentencePiece / Protobuf Conflict" entry the same way — either delete the subsection entirely (if the file's structure allows a clean removal) or replace its Risk/Impact/Migration-plan body with the corrected note above.

    In PROJECT.md, correct the "Known Issues" bullet (current line 85) "SentencePiece and SGProcessing have protobuf symbol conflict (cannot link both)" to state that this conflict does not exist in the current codebase (confirmed by Phase 4 research, 2026-08-18) — SentencePiece was never actually integrated; the real historical concern was informal, undocumented consolidation, now resolved by linking a single SGProcessingManager build. Do not touch any other line in PROJECT.md — it contains many other stale claims unrelated to this phase's scope (compile-time feature flags, RocksDB persistence, etc.) that are out of scope for Phase 4's PROC-01/02/03/FIX-04 requirements.
  </action>
  <acceptance_criteria>
    `git grep -n "protobuf symbol conflict" .planning/workstreams/neoswarm/PROJECT.md` returns zero matches for the old uncorrected phrasing (a corrected sentence describing no active conflict is fine and expected). `git grep -in "SentencePiece.*protobuf.*conflict" .planning/workstreams/neoswarm/codebase/CONCERNS.md` no longer matches the old "when both GENIUS_HAS_SENTENCEPIECE and GENIUS_HAS_SGPROCESSING are active" phrasing.
  </acceptance_criteria>
  <verify>
    <automated>cd "W:\gnus\GeniusCognitiveSystem\GNUS-NEO-SWARM" && ! grep -q "GENIUS_HAS_SENTENCEPIECE" .planning/workstreams/neoswarm/codebase/CONCERNS.md .planning/workstreams/neoswarm/PROJECT.md</automated>
  </verify>
  <done>CONCERNS.md and PROJECT.md no longer describe an active SentencePiece/SGProcessing protobuf conflict or reference the removed GENIUS_HAS_* compile flags for this concern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| NEO-SWARM job schema -> SGProcessingManager | JSON schema built by `SGProcessingBridge::BuildSchemaJson()` crosses into SGProcessingManager's `ProcessingManager::Create()`/`from_json` parsing — untrusted in the sense that a malformed `type`/`format` field must fail closed, not silently misdispatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-04-01 | Tampering | `SGProcessingBridge::InputFormatToTypeString` | medium | mitigate | Fix confirmed today: FP4_ULTRA no longer emits an invalid `"fp4_ultra"` DataType string that silently fails `from_json` and returns a generic `INVALID_JSON` error with no actionable diagnostic — corrected to emit `"tensor"`, the valid, intended dispatch key (Task 2) |
| T-04-02 | Denial of Service | CMake build configuration | low | accept | Pointing at a wrong/missing `GENIUSNETWORK_SUPERGENIUS_DIR` fails loudly at CMake configure/link time (missing libs -> unresolved symbols), not silently at runtime — acceptable fail-closed behavior, no mitigation beyond the EXISTS-guarded stub-mode fallback already in place |
</threat_model>

<verification>
Fresh CMake configure + `neoswarm_core` build succeeds against the GeniusNetwork/SuperGenius link path. `test_sgprocessing_pipeline` compiles and its schema-generation tests (including the new FP4_ULTRA case) pass. CONCERNS.md/PROJECT.md no longer describe a nonexistent protobuf conflict.
</verification>

<success_criteria>
- [ ] `src/core/CMakeLists.txt` links `neoswarm_core` against `GeniusNetwork/SuperGenius`'s prebuilt output (12 libs), not the old nonexistent sibling path
- [ ] `test/CMakeLists.txt`'s `SUPERGENIUS_TEST_DATA_DIR` points at the real fixture directory
- [ ] `SGProcessingBridge::SubmitDirect()` compiles against the current `ProcessOutput`-returning `Process()` signature
- [ ] `InputFormatToTypeString(FP4_ULTRA)` returns `"tensor"`, verified by a passing regression test
- [ ] CONCERNS.md and PROJECT.md no longer claim an active SentencePiece/protobuf conflict
</success_criteria>

<output>
Create `.planning/phases/04-sgprocessing-integration/04-01-SUMMARY.md` when done
</output>
