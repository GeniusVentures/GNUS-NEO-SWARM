---
phase: 01-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/security/CMakeLists.txt
  - src/security/NodeIdentity.cpp
  - src/security/MessageSigning.cpp
autonomous: true
requirements:
  - SEC-01
  - SEC-06

must_haves:
  truths:
    - "Node can generate a secp256k1 keypair, derive a PeerId, and save the key"
    - "Security stub code paths return false (fail-close) in release builds — missing crypto is a hard rejection"
  artifacts:
    - path: "src/security/CMakeLists.txt"
      provides: "secp256k1 + OpenSSL library linkage with compile definitions"
      contains: "GENIUS_HAS_SECP256K1"
    - path: "src/security/NodeIdentity.cpp"
      provides: "fail-close stub Verify at line 282"
      grep: "return false"
    - path: "src/security/MessageSigning.cpp"
      provides: "fail-close stub Verify at line 59"
      grep: "return false"
  key_links:
    - from: "src/security/CMakeLists.txt"
      to: "thirdparty secp256k1 target"
      via: "if(TARGET secp256k1)"
      pattern: "target_link_libraries.*secp256k1"
---

<objective>
Activate the real secp256k1 cryptographic identity library and convert all security stubs to fail-close (reject rather than accept). This is the prerequisite for every subsequent security task — without real secp256k1 linkage, all Verify paths silently accept any signature.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/codebase/CONVENTIONS.md
@src/security/CMakeLists.txt
@src/security/NodeIdentity.cpp
@src/security/MessageSigning.cpp
@src/common/Error.hpp
@src/common/Logging.hpp
</context>

<tasks>

<task type="auto">
  <name>task 1: verify and harden secp256k1 linkage</name>
  <files>src/security/CMakeLists.txt</files>
  <read_first>
  - src/security/CMakeLists.txt — current CMake configuration for security library
  - .planning/codebase/STACK.md — thirdparty lib layout and secp256k1 path
  </read_first>
  <action>
Verify secp256k1 is linkable. The current CMakeLists.txt uses `if(TARGET secp256k1)` at line 13 which conditionally defines GENIUS_HAS_SECP256K1. Check whether the build system actually creates the `secp256k1` target from thirdparty:

1. Run `cmake --build build/OSX/Debug --target help 2>&1 | grep secp256k1` to check if the target exists in the build system.
2. If the target does NOT exist, examine how the thirdparty build exposes secp256k1 (likely via find_package or as an IMPORTED library). Look at `cmake/CommonBuildParameters.cmake` and `build/CommonCompilerOptions.cmake` for the pattern used to expose other thirdparty libraries to the project.
3. Add a fallback `find_library` or `find_package` block BEFORE the `if(TARGET secp256k1)` check so the target always gets defined when the library exists on disk. Model this after the existing `find_package(OpenSSL QUIET)` pattern already in the file.
4. Ensure the GENIUS_HAS_OPENSSL path is also verified — PeerId() relies on SHA256 from OpenSSL at line 141-144 of NodeIdentity.cpp.

**CRITICAL constraint from research:** The project must remain C++17. Do NOT change CMAKE_CXX_STANDARD.
</action>
  <verify>
    <automated>grep -c "GENIUS_HAS_SECP256K1" src/security/CMakeLists.txt — must be ≥1</automated>
  </verify>
  <acceptance_criteria>
  - `GENIUS_HAS_SECP256K1` compile definition is reachable in a real build (not just conditionally gated behind a missing target)
  - `GENIUS_HAS_OPENSSL` compile definition is reachable (needed for SHA-256 in PeerId and Sign/Verify hashing)
  - Building with `cmake .. -G Ninja -DCMAKE_BUILD_TYPE=Debug` from build/OSX/Debug succeeds with the security library linked against secp256k1
  </acceptance_criteria>
  <done>secp256k1 library target is available and GENIUS_HAS_SECP256K1 is defined at compile time</done>
</task>

<task type="auto">
  <name>task 2: fail-close all security stub fallbacks</name>
  <files>
    src/security/NodeIdentity.cpp
    src/security/MessageSigning.cpp
  </files>
  <read_first>
  - src/security/NodeIdentity.cpp — current Verify implementation (lines 247-284) with stub at lines 278-283
  - src/security/MessageSigning.cpp — current Verify implementation (lines 49-60) with stub at line 59
  - src/common/Logging.hpp — logger factory pattern, spdlog usage
  - .planning/research/PITFALLS.md — Pitfall #1 (Stub Security Paths), Pitfall #6 (Deterministic Nonces)
  </read_first>
  <action>
Convert both security stubs from fail-open (return true / accept) to fail-closed (return false / reject):

**NodeIdentity::Verify (line 247-284):**
Change line 282 from:
```
return true;
```
to:
```
IdentityLogger()->error( "NodeIdentity::Verify — secp256k1 not compiled in, REJECTING signature" );
return false;
```

Rationale: The current behavior silently accepts any signature when secp256k1 is unavailable. Per PITFALLS.md §Pitfall 1: "Stub fallbacks in security code must fail-close, not fail-open." This applies to the `#else` branch of `#ifdef GENIUS_HAS_SECP256K1` (lines 278-283).

**MessageSigning::Verify (line 49-60):**
Replace the entire function body (lines 52-59):
```cpp
// TODO(SECURITY): reconstruct NodeIdentity from pub_key_hex and call
// identity.Verify() once secp256k1 is compiled in.
( void )payload;
( void )signature;
( void )pub_key_hex;
SigningLogger()->warn( "MessageSigning::Verify — stub, signature not checked" );
return true;
```
with:
```cpp
( void )payload;
( void )signature;
( void )pub_key_hex;
SigningLogger()->error( "MessageSigning::Verify — secp256k1 not available, REJECTING signature" );
return false;
```

Change log level from `warn` to `error` for fail-close paths — these are security-critical failures, not warnings.

**Do NOT modify any `#ifdef GENIUS_HAS_SECP256K1` code paths** — only the `#else` stub fallback paths.
**Do NOT modify NodeIdentity::Generate stub** — the stub keygen warning mode is acceptable for development.
**Preserve all existing Doxygen comments, section separators, and code style** (Allman braces, 4-space indent, trailing underscores).
</action>
  <verify>
    <automated>grep -n "return false" src/security/NodeIdentity.cpp | grep -c "282\|283\|284" — must be ≥1; grep -n "return false" src/security/MessageSigning.cpp | grep -c "59\|60\|61" — must be ≥1</automated>
  </verify>
  <acceptance_criteria>
  - `NodeIdentity::Verify` in the `#else` (non-secp256k1) path returns `false` — not `true`
  - `MessageSigning::Verify` in the unguarded path returns `false` — not `true`
  - Both use `SigningLogger()->error()` (not `warn`)
  - No other code paths or `#ifdef` blocks modified
  - All existing 46 tests in `test/` continue to pass: `cd build/OSX/Debug && ctest --output-on-failure`
  </acceptance_criteria>
  <done>Both security stubs reject when real crypto is unavailable; 46 existing tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build system → secp256k1 library | Compile-time dependency — a missing library reverts to stub |
| Node process → cryptographic subsystem | Runtime crypto operations depend on properly initialized secp256k1 context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | `NodeIdentity::Verify` stub (line 278-283) | mitigate | Return `false` instead of `true` in `#else` branch — fail-close |
| T-01-02 | Spoofing | `MessageSigning::Verify` stub (line 49-60) | mitigate | Return `false` when secp256k1 unavailable — fail-close |
| T-01-03 | Denial of Service | Build system | accept | Missing secp256k1 target is a build configuration issue — detectable at cmake time. No runtime vector since stubs now fail-close |
| T-01-04 | Elevation of Privilege | Stub identity `Generate` | accept | Stub keygen for development-only path. Production builds with secp256k1 linked never hit this path |
</threat_model>

<verification>
1. Build: `cd build/OSX/Debug && cmake .. -G Ninja -DCMAKE_BUILD_TYPE=Debug && ninja` — must succeed
2. Tests: `cd build/OSX/Debug && ctest --output-on-failure` — all 46 existing tests pass
3. Compile check: `grep -r "GENIUS_HAS_SECP256K1" build/OSX/Debug/CMakeCache.txt build/OSX/Debug/compile_commands.json 2>/dev/null || echo "check compile_commands.json for -DGENIUS_HAS_SECP256K1"`
4. Stub check: `grep "return false" src/security/NodeIdentity.cpp src/security/MessageSigning.cpp | grep -v "^#" | grep -v "loaded_"` — must find fail-close returns
</verification>

<success_criteria>
- [ ] secp256k1 library target is found/linkable by the build system
- [ ] `GENIUS_HAS_SECP256K1` is defined at compile time in non-stub builds
- [ ] `NodeIdentity::Verify` stub returns `false` (fail-close)
- [ ] `MessageSigning::Verify` stub returns `false` (fail-close)
- [ ] All 46 existing tests pass with zero regressions
- [ ] Build succeeds in Debug configuration on OSX
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-hardening/01-01-SUMMARY.md`
</output>
