---
phase: 01-foundation-bug-fixes
plan: 01
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - gnus-poc/training/tokenizer_utils.py
  - gnus-poc/training/train_specialists_mlx.py
  - gnus-poc/data/scripts/prepare_datasets.py
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02

must_haves:
  truths:
    - "Running prepare_datasets.py and then training on its output produces coherent completions — the chat template used during data prep matches what train_specialists_mlx.py consumes"
    - "A partially-trained specialist (e.g., iteration 200/1000) is NOT recognized as complete by the skip-on-existing check"
    - "Only a specialist with training_metadata.json reporting iters == configured_iters passes the skip check"
    - "The --force-retrain flag bypasses the skip check and re-executes training regardless of existing adapters"
    - "prepare_datasets.py validates Common Pile metadata schema at startup and aborts with a clear error if the expected field is missing (Pitfall #4)"
  artifacts:
    - path: "gnus-poc/training/tokenizer_utils.py"
      provides: "Shared chat template formatting using HuggingFace tokenizer.apply_chat_template()"
      exports: ["load_tokenizer", "format_chat"]
      min_lines: 40
    - path: "gnus-poc/data/scripts/prepare_datasets.py"
      provides: "Dataset preparation using tokenizer-based chat template (not hand-rolled format strings)"
      contains: "from training.tokenizer_utils import"
    - path: "gnus-poc/training/train_specialists_mlx.py"
      provides: "Training with fixed skip logic checking milestone file and metadata iters field"
      contains: "--force-retrain"
  key_links:
    - from: "gnus-poc/data/scripts/prepare_datasets.py"
      to: "gnus-poc/training/tokenizer_utils.py"
      via: "import sys; sys.path.insert(0, str(PROJECT_ROOT)); from training.tokenizer_utils import"
      pattern: "from training\\.tokenizer_utils import (load_tokenizer|format_chat)"
    - from: "gnus-poc/training/train_specialists_mlx.py"
      to: "gnus-poc/training/tokenizer_utils.py"
      via: "direct import (same package)"
      pattern: "from \\.tokenizer_utils import|from training\\.tokenizer_utils import"
    - from: "gnus-poc/training/train_specialists_mlx.py skip check"
      to: "training_metadata.json iters field"
      via: "comparison: metadata['iters'] == configured_iters"
      pattern: "metadata.*\\['iters'\\]|metadata.*\\.get\\(.*iters"
---

<objective>
Fix two silent data-corrupting bugs in the existing Python training pipeline. First, fix the chat template mismatch (Pitfall #1) where prepare_datasets.py hardcodes Qwen2.5 `<|im_start|>` format but training uses Qwen3-30B-A3B models — by extracting a shared tokenizer_utils.py that uses the actual HuggingFace tokenizer's `apply_chat_template()`. Second, fix the skip-on-existing logic (Pitfall #2) where a crashed training run at iteration 200/1000 is incorrectly treated as "complete" — by checking the milestone file matching configured iters and validating training_metadata.json. Also add metadata field validation (Pitfall #4) and fix hardcoded niche format strings (Pitfall #16). Per FOUND-01 and FOUND-02.

Purpose: These bugs silently corrupt every specialist trained on this codebase. Fixing them is the absolute prerequisite for all subsequent phases — Phase 2-7 train specialists, and building on broken training produces garbage at every stage.

Output: tokenizer_utils.py (new), prepare_datasets.py (fixed formatting), train_specialists_mlx.py (fixed skip logic + force-retrain).
</objective>

<execution_context>
@/Users/khurley/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/khurley/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md (Phase 1 success criteria)
@.planning/REQUIREMENTS.md (FOUND-01, FOUND-02)
@.planning/research/PITFALLS.md (Pitfall #1: chat template mismatch, lines 13-31; Pitfall #2: skip-on-existing, lines 34-51; Pitfall #4: metadata field drift, lines 76-91; Pitfall #16: hardcoded niche formats, lines 304-318)
@.planning/phases/01-CONTEXT.md (Implementation Decisions: chat template fix, skip fix)
@gnus-poc/data/scripts/prepare_datasets.py (lines 115-165: format_for_training with hardcoded <|im_start|>; lines 70-72: metadata field extraction)
@gnus-poc/training/train_specialists_mlx.py (lines 214-240: skip-on-existing logic; lines 186-202: training_metadata.json structure; lines 1-22: imports and docstring)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tokenizer_utils.py and fix chat template in prepare_datasets.py</name>
  <files>
    gnus-poc/training/tokenizer_utils.py
    gnus-poc/data/scripts/prepare_datasets.py
  </files>
  <behavior>
    - Test 1: load_tokenizer("mlx-community/Qwen3-30B-A3B-Instruct-2507-bf16") returns a HuggingFace tokenizer object with apply_chat_template method
    - Test 2: format_chat([{"role": "system", "content": "You are helpful."}, {"role": "user", "content": "Hello"}], tokenizer) returns a string that does NOT contain `<|im_start|>` (Qwen3 template uses different tokens)
    - Test 3: format_chat output, when decoded, produces coherent tokens matching Qwen3's expected template format (contains model-specific special tokens, not Qwen2.5 ones)
  </behavior>
  <action>
    Create gnus-poc/training/tokenizer_utils.py with two functions per CONTEXT.md "Specific Ideas":

    Function 1 — `load_tokenizer(model_path: str)`:
    - Import `AutoTokenizer` from `transformers` (NOT mlx_lm — data prep should not require MLX)
    - Load tokenizer with `trust_remote_code=True` (matching existing convention in train_specialists_mlx.py line 173)
    - Return the tokenizer object
    - If loading fails, raise a clear RuntimeError with the model_path that failed

    Function 2 — `format_chat(messages: list[dict], tokenizer)`:
    - Accept a list of message dicts with `role` and `content` keys (standard chat format)
    - Call `tokenizer.apply_chat_template(messages, tokenize=False, add_generation_prompt=False)`
    - Return the formatted string
    - assert that the returned string is non-empty

    Then modify gnus-poc/data/scripts/prepare_datasets.py:

    A) Add import at top (after existing imports, before function definitions):
    ```python
    import sys
    from pathlib import Path
    PROJECT_ROOT = Path(__file__).resolve().parent.parent.parent
    sys.path.insert(0, str(PROJECT_ROOT))
    from training.tokenizer_utils import load_tokenizer, format_chat
    ```

    B) Replace the ENTIRE `format_for_training()` function (lines 115-165) with a version that uses tokenizer-based formatting. The new function should:
    - Load the tokenizer ONCE at module level (outside the function, to avoid reloading per sample):
      `_TOKENIZER = load_tokenizer("mlx-community/Qwen3-30B-A3B-Instruct-2507-bf16")`
    - Use `format_chat(messages, _TOKENIZER)` instead of hand-rolled `<|im_start|>` strings
    - Per niche, construct proper messages list with system/user/assistant roles
    - For `medical`: system="You are a medical research specialist.", user=f"Provide medical or biomedical information based on the following research:\n{text[:context_end]}", assistant=text[context_end:response_end]
    - For `qa_technical`: Use same pattern. Fix Pitfall #16 — use source metadata to determine format. If sample has StackExchange `question`/`answer` keys (from meta), use those as user/assistant. Only fall back to 'Q:'/'A:' string splitting if metadata is absent.
    - For `code`: system="You are a programming and code documentation specialist.", user="Explain or document this code:", assistant=text[:2000]
    - For `encyclopedic`: system="You are an encyclopedic knowledge specialist.", user=f"Provide information about {title}:", assistant=content[:2000]
    - For `patents`: system="You are a patent and technical innovation specialist.", user="Explain this invention or technical innovation:", assistant=text[:2000]
    - The key change: EVERY niche uses `format_chat()` instead of f-string with `<|im_start|>`.

    C) Add metadata field validation per Pitfall #4 — in `extract_niche_samples()`, after the first example is loaded (around line 63-72), add:
    - Inspect `example.get('meta', {})` keys
    - Verify that at least one of `pile_set_name`, `source`, or `dataset` exists in the meta keys
    - If NONE of these keys exist, print all available meta keys and raise RuntimeError with message: "Common Pile metadata schema changed — expected 'pile_set_name', 'source', or 'dataset' but found: {list(meta.keys())}"
    - After extraction completes, log the count of samples with source='unknown' and warn if >10% of total.

    After modification, verify: running `python3 -c "from training.tokenizer_utils import load_tokenizer, format_chat; print('import OK')"` succeeds (tokenizer loading is lazy — only on first call).
  </action>
  <verify>
    <automated>python3 -c "
import sys
sys.path.insert(0, 'gnus-poc')
from training.tokenizer_utils import load_tokenizer, format_chat
# Lazy-load test: verify functions exist without triggering model download
assert callable(load_tokenizer), 'load_tokenizer not callable'
assert callable(format_chat), 'format_chat not callable'
# Verify prepare_datasets.py has no remaining <|im_start|> hardcodes
import ast
with open('gnus-poc/data/scripts/prepare_datasets.py') as f:
    tree = ast.parse(f.read())
source = open('gnus-poc/data/scripts/prepare_datasets.py').read()
# Count occurrences of the old pattern (should be zero, or only in comments)
import re
matches = re.findall(r'<\|im_start\|>', source)
# Allow in docstrings/comments only — check if any are outside comments
lines = source.split('\n')
in_docstring = False
bad_lines = []
for i, line in enumerate(lines, 1):
    stripped = line.strip()
    if stripped.startswith('\"\"\"') or stripped.startswith(\"'''\"):
        in_docstring = not in_docstring
        continue
    if in_docstring or stripped.startswith('#'):
        continue
    if '<|im_start|>' in stripped:
        bad_lines.append(f'  Line {i}: {stripped[:80]}')
if bad_lines:
    print('ERROR: Hardcoded <|im_start|> found outside comments/docstrings:')
    for bl in bad_lines:
        print(bl)
    sys.exit(1)
print('OK: No <|im_start|> hardcodes remain in code')
print('OK: tokenizer_utils.py imports successfully')
print('OK: format_for_training uses tokenizer_utils.format_chat')
"</automated>
    <human-check>The smoke test in Plan 01-03 will do a full end-to-end template check using a real (small) tokenizer. For manual verification: `python3 -c "import sys; sys.path.insert(0,'gnus-poc'); from training.tokenizer_utils import load_tokenizer, format_chat; tok = load_tokenizer('Qwen/Qwen2.5-0.5B-Instruct'); print(format_chat([{'role':'user','content':'test'}], tok))"` and verify no `<|im_start|>` appears.</human-check>
  </verify>
  <done>tokenizer_utils.py exists with load_tokenizer and format_chat; prepare_datasets.py imports and uses them for ALL 5 niches; no <|im_start|> hardcodes remain in non-comment code; metadata field validation added to extract_niche_samples.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix skip-on-existing logic and add --force-retrain</name>
  <files>
    gnus-poc/training/train_specialists_mlx.py
  </files>
  <behavior>
    - Test 1: When adapters.safetensors exists but milestone file (e.g., 0001000_adapters.safetensors) is missing, training is NOT skipped — it resumes/re-trains
    - Test 2: When adapters.safetensors exists AND milestone file exists but training_metadata.json has iters=200 (vs configured 1000), training is NOT skipped — metadata validation catches incomplete run
    - Test 3: When adapters.safetensors exists AND milestone file exists AND training_metadata.json has iters=1000 (matching configured), training IS skipped with clear log message
    - Test 4: When --force-retrain is passed, training executes regardless of any existing adapters (deletes existing adapters first)
  </behavior>
  <action>
    Modify gnus-poc/training/train_specialists_mlx.py `main()` function (lines 214-240) to fix the skip-on-existing logic per Pitfall #2 and CONTEXT.md "Specific Ideas":

    A) Replace the existing skip check (lines 222-230) which only tests `final_adapter.exists()` with a three-phase check:

    Phase 1 — Check for force-retrain flag:
    - Add `import argparse` at top
    - In `main()`, parse `--force-retrain` flag: `parser = argparse.ArgumentParser(); parser.add_argument('--force-retrain', action='store_true'); args = parser.parse_args()`
    - If `args.force_retrain`: log "Force retrain enabled — deleting existing adapters" and `shutil.rmtree(adapter_path)` if it exists, then proceed to training without skip check.

    Phase 2 — Check for milestone file (if not force-retrain):
    - Construct expected milestone filename: `f"{args.iters:07d}_adapters.safetensors"` (e.g., "0001000_adapters.safetensors")
    - Check `(adapter_path / milestone_file).exists()`
    - If milestone file does NOT exist: log "No milestone file found — training from scratch (or resuming)" and proceed to training.

    Phase 3 — Validate metadata (if milestone exists):
    - Load `adapter_path / "training_metadata.json"`
    - Check `metadata.get('iters') == OVERRIDES['iters']`
    - Check `metadata.get('status') == 'complete'` (add this status field to metadata output in train_specialist() — see below)
    - If BOTH conditions pass: log "✓ Skipping {niche} — training complete at iteration {metadata['iters']}" and continue to next specialist.
    - If metadata missing or iters mismatch: log "⚠ Existing adapters appear incomplete (metadata iters={metadata.get('iters')} vs configured {OVERRIDES['iters']}) — retraining" and proceed to training.

    B) In `train_specialist()` function (lines 186-202), add a `"status": "complete"` field to the metadata dict:
    ```python
    metadata = {
        "niche": niche_name,
        "base_model": base_model,
        "training_duration_minutes": duration,
        "trained_at": datetime.now().isoformat(),
        "iters": args.iters,
        "batch_size": args.batch_size,
        "num_layers": args.num_layers,
        "lora_parameters": args.lora_parameters,
        "status": "complete",           # NEW — Phase 2 skip check depends on this
        "dataset_hash": None,           # Placeholder for Plan 01-02 versioning
    }
    ```

    C) Also add milestone file check after training completes — verify that the expected milestone file (e.g., `0001000_adapters.safetensors`) was actually written by MLX. If not present, log a warning that training may have been interrupted.

    D) Add `import shutil` at top for `rmtree` in force-retrain path.
  </action>
  <verify>
    <automated>python3 -c "
import ast, sys

with open('gnus-poc/training/train_specialists_mlx.py') as f:
    source = f.read()
    tree = ast.parse(source)

# Check: --force-retrain argument exists
assert '--force-retrain' in source, 'Missing --force-retrain argument'
print('OK: --force-retrain argument present')

# Check: milestone file pattern exists (e.g. f-string with :07d)
assert '{args.iters:07d}' in source or '{:07d}' in source, 'Missing milestone file pattern'
print('OK: Milestone file pattern present')

# Check: metadata validation references 'iters' and 'status'
meta_lines = [l.strip() for l in source.split('\n') if 'metadata' in l.lower() and ('iters' in l.lower() or 'status' in l.lower())]
assert len(meta_lines) >= 1, 'Missing metadata iters/status validation'
print('OK: Metadata iters/status validation present')
for ml in meta_lines:
    print(f'  {ml[:100]}')

# Check: status field added to metadata dict
assert '\"status\": \"complete\"' in source or \"'status': 'complete'\" in source, 'Missing status=complete in metadata'
print('OK: status field in metadata')

# Check: no bare final_adapter.exists() as sole skip condition
# The new logic should check milestone file, not just adapter existence
assert 'milestone' in source.lower(), 'Missing milestone file check'
print('OK: Milestone file check present')
print('All skip-logic fixes verified')
"</automated>
  </verify>
  <done>Skip check uses 3-phase logic (force-retrain → milestone file → metadata validation); training_metadata.json includes status field; --force-retrain deletes existing adapters before training; milestone file verified after training completes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HuggingFace → tokenizer_utils.py | `trust_remote_code=True` loads arbitrary code from model repos (Pitfall #13) |
| Common Pile dataset → prepare_datasets.py | External streaming dataset with potentially changing schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | tokenizer_utils.load_tokenizer() | accept | trust_remote_code=True is required by tokenizer config — same pattern as existing train_specialists_mlx.py. Risk accepted for POC (see Pitfall #13); pin model versions with commit hashes in Phase 3. |
| T-01-02 | Denial of Service | prepare_datasets.py metadata validation | mitigate | Pitfall #4 fix: validate metadata schema at first example, abort with clear error if expected fields missing, warn if >10% unknown sources. |
| T-01-03 | Information Disclosure | skip-on-existing logic | mitigate | False completions (Pitfall #2) are a data integrity threat — the skip logic now validates metadata iters field before treating a specialist as complete. |
</threat_model>

<verification>
<automated>
# End-to-end: verify both fixes are present and no regressions
python3 -c "
import ast, sys, re

# 1. Verify prepare_datasets.py has tokenizer_utils import + metadata validation
with open('gnus-poc/data/scripts/prepare_datasets.py') as f:
    prep_source = f.read()
assert 'from training.tokenizer_utils import' in prep_source, 'MISSING: tokenizer_utils import in prepare_datasets.py'
assert 'format_chat' in prep_source, 'MISSING: format_chat usage in prepare_datasets.py'
assert 'RuntimeError' in prep_source or 'raise' in prep_source, 'MISSING: metadata validation error handling'
# No bare <|im_start|> in non-comment code
lines = prep_source.split('\n')
in_comment = False
for i, line in enumerate(lines, 1):
    s = line.strip()
    if s.startswith('\"\"\"'): in_comment = not in_comment; continue
    if in_comment or s.startswith('#'): continue
    if '<|im_start|>' in s:
        print(f'ERROR: <|im_start|> in line {i}: {s[:80]}')
        sys.exit(1)

# 2. Verify train_specialists_mlx.py has fixed skip logic
with open('gnus-poc/training/train_specialists_mlx.py') as f:
    train_source = f.read()
assert '--force-retrain' in train_source, 'MISSING: --force-retrain'
assert 'milestone' in train_source.lower() or '000' in train_source, 'MISSING: milestone file check'
assert '\"status\"' in train_source and 'complete' in train_source, 'MISSING: status field in metadata'

# 3. Verify no regressions — key functions still exist
train_tree = ast.parse(train_source)
func_names = [n.name for n in ast.walk(train_tree) if isinstance(n, ast.FunctionDef)]
for required in ['prepare_dataset_for_mlx', 'build_args_for_niche', 'train_specialist', 'main']:
    assert required in func_names, f'MISSING function: {required}'

prep_tree = ast.parse(prep_source)
prep_funcs = [n.name for n in ast.walk(prep_tree) if isinstance(n, ast.FunctionDef)]
for required in ['extract_niche_samples', 'create_splits', 'format_for_training', 'save_datasets', 'main']:
    assert required in prep_funcs, f'MISSING function: {required}'

print('ALL CHECKS PASSED')
print('  - Chat template: tokenizer_utils.py imported, no <|im_start|> hardcodes')
print('  - Skip logic: --force-retrain, milestone check, metadata validation')
print('  - Metadata: status field, iters comparison')
print('  - No regressions: all required functions preserved')
"
</automated>
</verification>

<success_criteria>
- [ ] tokenizer_utils.py exists with load_tokenizer() and format_chat() functions
- [ ] prepare_datasets.py imports and uses format_chat() for ALL 5 niches
- [ ] Zero hardcoded `<|im_start|>` strings in non-comment code in prepare_datasets.py
- [ ] Metadata field validation catches schema drift (RuntimeError if no expected field)
- [ ] Skip-on-existing checks milestone file (e.g., 0001000_adapters.safetensors) not just adapters.safetensors
- [ ] Skip check validates training_metadata.json iters field matches configured iters
- [ ] --force-retrain flag bypasses skip check and deletes existing adapters
- [ ] training_metadata.json includes `"status": "complete"` field
- [ ] All existing functions (prepare_dataset_for_mlx, build_args_for_niche, etc.) preserved
</success_criteria>

<output>
Create `.planning/phases/01-foundation-bug-fixes/01-01-SUMMARY.md` when done
</output>
