---
context: phase
phase: 03-gcs-globaldb-integration
plan: 01
task: null
total_tasks: 3
status: between_waves
last_updated: 2026-08-11T02:02:17.735Z
---

# Continue Here — Phase 3 (GCS GlobalDB Integration), Between Waves 1 & 2

<current_state>
Phase 3 Wave 1 (03-01: GCS GlobalDB component skeleton) is complete — 3 tasks, 4 files created, 4/4 tests pass, committed and pushed to `feature/phase3-gcs-globaldb`.

Between waves, a cross-repo restructuring was performed (not yet committed):
- Flutter UI code extracted from GNUS-NEO-SWARM into parent GeniusCogntiveSystem repo
- `flutter_slm_bridge` → `neoswarm_ffi` renamed inside submodule
- `flutter_app` moved to `app/src/`, chat_gnus merged in
- `app/src/scaffold/` submodule added (openapi-client-scaffold)
- `app/ffi/` stub created
- `app/ui/` deleted

The restructuring changes are UNCOMMITTED and need to be committed to new branches before proceeding to Wave 2.
</current_state>

<completed_work>

Wave 1 (03-01) — all 3 tasks done, pushed:
- Task 1: CMake target + error codes (commit `0bb1d7e`)
- Task 2: GcsGlobalDb implementation (commit `bc6ab3c`)
- Task 3: Lifecycle tests (commit `7b98fce`)

Inter-wave restructuring (NOT committed yet):
- `flutter_slm_bridge` → `neoswarm_ffi` renamed (inside GNUS-NEO-SWARM)
- `flutter_app` moved to parent `app/src/`, chat_gnus merged (single Flutter project)
- `app/src/scaffold/` submodule added
- `app/ffi/` stub created, `app/ui/` removed
- All pubspec.yaml files updated
</completed_work>

<remaining_work>

Phase 3 remaining waves (nothing started):
- 03-02: GCS reputation publisher (publishes reputation deltas to GlobalDB)
- 03-03: Rewire api_server.cpp to GcsGlobalDb (was: delete flutter_slm_bridge — plan needs updating)
- 03-04: Integration tests + Phase 3 verification

03-03-PLAN.md still references deleting `flutter_slm_bridge/` — needs updating to reflect rename to `neoswarm_ffi`.
</remaining_work>

<decisions_made>

- chat_gnus merged into flutter_app (single Flutter project under `app/src/`). Rationale: Flutter won't depend on flutter_ai_toolkit, flutter_chat_ui, flutter_chat_core directly — frontend_scaffold Phase 6 will provide widget toolkit.
- neoswarm_ffi stays inside GNUS-NEO-SWARM for CMake access. Dart side consumed via path dependency from `app/src/pubspec.yaml`.
- No restructuring commits pushed yet (user wanted build verification first — now committing for cross-machine pickup).
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)

1. `03-01-SUMMARY.md` — Wave 1 completion details, auto-fixes, architecture decisions
1. `03-CONTEXT.md` — Phase design decisions (D-01..D-17) that govern remaining waves
1. `03-RESEARCH.md` — Technical research underpinning GCS GlobalDB integration
1. `03-02-PLAN.md` — GCS reputation publisher (next wave to execute)

## Critical Anti-Patterns (do NOT repeat these)

- GeniusSDK install-prefix dylib predates `GeniusSDKGetNode` (d550800) — CMake redirects to build-tree dylib in `src/storage/CMakeLists.txt`. Don't remove this redirect unless the install prefix has been updated.
- Header paths for ipfs-lite graphsync: `impl/network/network.hpp` not `impl/network.hpp`; `impl/local_requests.hpp` not `impl/request_id_generator.hpp`.
- GeniusSDK.hpp not in install include dir — consumed from `${GENIUS_SDK_DIR}/src` source tree.

## Infrastructure State

- Branch (submodule): `feature/phase3-gcs-globaldb` — has Wave 1 commits
- Branch (parent): `develop` — needs new `feature/app-restructure` branch for restructuring
- Build: C++ tests pass (4/4 GcsGlobalDb + 16 reputation). Flutter not yet verified.
- GeniusSDK: requires build-tree dylib at `${GENIUS_SDK_BUILD_DIR}/src/libGeniusSDK_shared.dylib` (not install-prefix)

<context>
Wave 1 executor completed in 22 min with 7 auto-fixed deviations. The component is solid — init-style lifecycle with injected pubsub test seam. Between waves, we restructured the repo to extract Flutter UI code from the C++ submodule. The restructuring follows the pattern already established by genius-tube (`src/ffi/` + `src/scaffold/`) and genius-ai-boss (`frontend/scaffold` + `backend/scaffold`).

The key architectural insight: the parent repo is Flutter UI with Cubit state management. The C++ business logic stays in GNUS-NEO-SWARM. The OpenAI-compatible API the app connects to is a future HTTP server — NOT an FFI concern.

Next wave (03-02) publishes reputation data to the GCS CRDT layer using the GcsGlobalDb component from Wave 1.
</context>

<next_action>
Start with: commit the restructuring to new `feature/app-restructure` branches in both repos, push to remote. Then `flutter pub get` in `app/src/`. Then proceed to Wave 2: `/gsd-execute-phase 3 --wave 2 --ws neoswarm`.
</next_action>
