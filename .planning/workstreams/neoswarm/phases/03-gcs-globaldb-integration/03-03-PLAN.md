---
phase: 03-gcs-globaldb-integration
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - src/api/api_server.hpp
  - src/api/api_server.cpp
  - src/api/CMakeLists.txt
  - src/reputation/CMakeLists.txt
  - src/memory/CMakeLists.txt
  - src/core/CMakeLists.txt
  - src/main.cpp
  - test/reputation/test_reputation.cpp
  - test/CMakeLists.txt
  - src/reputation/reputation_storage.hpp   # DELETED
  - src/reputation/reputation_storage.cpp   # DELETED
  - src/reputation/reputation_crdt.hpp      # DELETED
  - src/reputation/reputation_crdt.cpp      # DELETED
  - flutter_slm_bridge/                     # DELETED (entire directory)
autonomous: false
approval_gate: "Execution does NOT proceed to wave 4 without explicit user approval of this wave's SUMMARY (Phase 8 post-mortem workflow rule)."
requirements: [PERS-01, PERS-02, PERS-03, NET-02]

must_haves:
  truths:
    - "api_server.cpp constructs and uses GcsGlobalDb (m_gcsDb) instead of ReputationStorage (m_repStorage) and ReputationCRDT (m_repCrdt)"
    - "UpdateReputation reads/writes reputation through GcsGlobalDb::GetReputation/PutReputation — the P2PNode BroadcastCRDT path is removed (GlobalDB autosyncs, D-19)"
    - "src/reputation/reputation_storage.{hpp,cpp} and reputation_crdt.{hpp,cpp} no longer exist on disk (D-09, D-19)"
    - "flutter_slm_bridge/ no longer exists on disk (D-24)"
    - "No #include <rocksdb/ and no rocksdb::DB anywhere under src/ (grep gate, ROADMAP constraint #9)"
    - "No direct RocksDB linkage in any NEO-SWARM CMakeLists (D-10)"
    - "ApiServer::Config exposes m_gcsDbPath default ./gcs.db replacing m_reputationDbPath (D-02)"
    - "The full build passes and the reputation test suite runs against the GCS component"
  artifacts:
    - path: "src/api/api_server.hpp"
      provides: "m_gcsDb member replacing m_repStorage/m_repCrdt; Config::m_gcsDbPath replacing m_reputationDbPath"
      contains: "GcsGlobalDb"
    - path: "src/api/api_server.cpp"
      provides: "Rewired construction (line ~124), UpdateReputation (~323-351), peer-task reputation lookup (~466), Stop (~708)"
      contains: "PutReputation"
    - path: "src/reputation/CMakeLists.txt"
      provides: "neoswarm_reputation with reputation_storage.cpp/reputation_crdt.cpp removed and RocksDB block stripped"
    - path: "test/reputation/test_reputation.cpp"
      provides: "Scoring/consensus tests retained; storage/CRDT tests rewritten against GcsGlobalDb"
  key_links:
    - from: "src/api/api_server.cpp"
      to: "src/storage/gcs_global_db.hpp"
      via: "m_gcsDb->PutReputation(updated) in UpdateReputation"
      pattern: "PutReputation\\("
    - from: "src/api/api_server.cpp"
      to: "src/storage/gcs_global_db.hpp"
      via: "m_gcsDb->GetReputation(from_peer) in the P2P task handler"
      pattern: "GetReputation\\("
    - from: "src/api/CMakeLists.txt"
      to: "neoswarm_storage"
      via: "target_link_libraries(neoswarm_api ... neoswarm_storage)"
      pattern: "neoswarm_storage"
---

<objective>
Rewire the sole production consumer — `api_server.cpp` — from `ReputationStorage`/`ReputationCRDT` to `GcsGlobalDb`, then delete the replaced code outright: `reputation_storage.{hpp,cpp}`, `reputation_crdt.{hpp,cpp}` (D-09, D-19), all direct RocksDB CMake linkage (D-10), and the abandoned `flutter_slm_bridge/` directory (D-24). Rename the config knob `m_reputationDbPath` → `m_gcsDbPath` with default `./gcs.db` (D-02).

Purpose: `ReputationStorage` IS the architectural bug — it wraps RocksDB directly and can diverge across nodes. The fix is replacement, not wrapping (D-09). After this wave, every persistent reputation read/write flows through the CRDT-backed GCS GlobalDB and NEO-SWARM source contains zero direct RocksDB usage (ROADMAP constraint #9).

Output: Consumer rewired, dead code deleted, grep gates green, build + reputation tests passing.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/neoswarm/phases/03-gcs-globaldb-integration/03-CONTEXT.md
@.planning/workstreams/neoswarm/phases/03-gcs-globaldb-integration/03-RESEARCH.md
@.planning/workstreams/neoswarm/phases/03-gcs-globaldb-integration/03-02-SUMMARY.md
@src/api/api_server.hpp
@src/api/api_server.cpp
@src/api/CMakeLists.txt
@src/storage/gcs_global_db.hpp
@src/reputation/reputation_storage.hpp
@src/reputation/reputation_storage.cpp
@src/reputation/reputation_crdt.hpp
@src/reputation/reputation_crdt.cpp
@src/reputation/CMakeLists.txt
@src/memory/CMakeLists.txt
@src/core/CMakeLists.txt
@src/main.cpp
@test/reputation/test_reputation.cpp
@test/CMakeLists.txt

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

Current consumer surface (api_server.hpp:137-138):
```cpp
std::unique_ptr<reputation::ReputationStorage> m_repStorage;
std::unique_ptr<reputation::ReputationCRDT>    m_repCrdt;
// Config (api_server.hpp:60): std::string m_reputationDbPath = "./reputation.db";
```

Current consumer usage sites (api_server.cpp — ALL must be handled):
```cpp
// :123-129  construction + Open (warns and continues on failure)
// :327      UpdateReputation guard: if ( !m_repStorage || !m_repStorage->IsOpen() ) return;
// :332      auto get_res = m_repStorage->Get( resp.m_nodeId );
// :344      (void)m_repStorage->Put( updated );
// :345      m_repCrdt->Merge( updated );
// :347-350  if ( m_p2pNode && m_p2pNode->IsRunning() ) { (void)m_p2pNode->BroadcastCRDT( m_repCrdt->Serialize() ); }
// :466-473  P2P task handler: m_repStorage->Get( from_peer ) → out.reputation_ = rep.m_globalScore
// :708-709  Stop(): if ( m_repStorage ) m_repStorage->Close();
```

Replacement surface (src/storage/gcs_global_db.hpp — delivered in Waves 1-2):
```cpp
struct Config { std::string m_dbPath = kDefaultDbPath; };  // kDefaultDbPath = "./gcs.db"
static constexpr const char* kReputationTopic;             // "gcs-reputation"
explicit GcsGlobalDb( Config cfg ) noexcept;
outcome::result<void> Initialize();                         // production: pulls pubsub via GeniusSDKGetNode
void Shutdown() noexcept;
bool IsRunning() const noexcept;
outcome::result<void> PutReputation( const NodeReputation& rep );
outcome::result<NodeReputation> GetReputation( const std::string& identityKey );
outcome::result<std::vector<NodeReputation>> QueryReputations();
outcome::result<void> RemoveReputation( const std::string& identityKey );
```

main.cpp:307: `cfg.m_reputationDbPath = args.db_path_;` — CLI flag `--reputation-db-path` becomes `--gcs-db-path` (D-02).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire api_server from ReputationStorage/ReputationCRDT to GcsGlobalDb</name>
  <files>
    src/api/api_server.hpp
    src/api/api_server.cpp
    src/api/CMakeLists.txt
    src/main.cpp
  </files>
  <read_first>
    src/api/api_server.hpp
    src/api/api_server.cpp
    src/api/CMakeLists.txt
    src/main.cpp
    src/storage/gcs_global_db.hpp
  </read_first>
  <behavior>
    - Test 1 (build): neoswarm_api compiles with GcsGlobalDb member and no ReputationStorage/ReputationCRDT references
    - Test 2: UpdateReputation with a running GcsGlobalDb persists the updated record (observable via GetReputation after UpdateReputation in a component-level test or the existing pipeline test)
    - Test 3: UpdateReputation when GcsGlobalDb is not running → early return, no crash, warn logged (preserves the old not-open guard semantics)
    - Test 4: The P2PNode BroadcastCRDT call path is gone — reputation convergence is GlobalDB's job now (D-19)
  </behavior>
  <action>
    **api_server.hpp edits:**
    1. Replace `#include` of reputation_storage/reputation_crdt headers (find them at the top — api_server.hpp includes reputation headers for the member declarations; keep reputation_scoring.hpp, weighted_consensus.hpp, node_reputation.hpp includes — those classes survive) with `#include "storage/gcs_global_db.hpp"`.
    2. Line 60: rename `std::string m_reputationDbPath = "./reputation.db";` → `std::string m_gcsDbPath = "./gcs.db";` (D-02). Check for other Config references to m_reputationDbPath anywhere under src/ and test/ (`grep -rn "m_reputationDbPath" src test`) and update each.
    3. Lines 137-138: replace the two members with `std::unique_ptr<storage::GcsGlobalDb> m_gcsDb;`.

    **api_server.cpp edits (all five usage sites):**
    1. **Construction (:123-129):** replace the m_repCrdt/m_repStorage make_unique pair and the Open block with:
       - `storage::GcsGlobalDb::Config gcsCfg; gcsCfg.m_dbPath = m_cfg.m_gcsDbPath; m_gcsDb = std::make_unique<storage::GcsGlobalDb>( gcsCfg );`
       - **Initialization gating:** GcsGlobalDb::Initialize() requires the GeniusSDK to be up (D-20 ordering: ApiServer → SGClient → GCS GlobalDB). In this wave SGClient init happens in InitializeNetwork() at step 6 — AFTER step 5 where reputation is constructed. Minimal change: construct m_gcsDb at step 5 (cheap, config-only per D-13) but call `m_gcsDb->Initialize()` only when `m_cfg.m_enableNetwork && m_sgClient && m_sgClient->IsConnected()` after InitializeNetwork() returns — insert that block immediately after the `InitializeNetwork();` call at :132. When the network path is off (local-only mode), m_gcsDb stays uninitialized and every op fails closed — matching the old behavior where ReputationStorage::Open failing logged a warn and continued. Log at warn when skipping init in local-only mode.
       - Remove the `(void)`-style warn-only failure swallow: on Initialize() failure log error via ServerLogger and continue in degraded mode (same resilience contract as before — the server still serves single-node inference without reputation).
    2. **UpdateReputation (:327-350):** guard becomes `if ( !m_gcsDb || !m_gcsDb->IsRunning() ) { return; }`. Get becomes `m_gcsDb->GetReputation( resp.m_nodeId )` — on failure (ReputationNotFound) start from a fresh `NodeReputation{}` with `rep.m_identityKey = resp.m_nodeId` (same as today's else branch). Put becomes `m_gcsDb->PutReputation( updated )` — CHECK the result; on failure log error (no `(void)` discard — project rule; note api_server.cpp has pre-existing `(void)` discards elsewhere which are OUT OF SCOPE per the Outcome Error Handling Rules unless on a touched line — you are touching these lines, so the new lines must not discard). DELETE the `m_repCrdt->Merge( updated );` line and the entire `BroadcastCRDT` block (:345-350) — GlobalDB autosyncs via the gcs-reputation topic (D-19; P2PNode itself is untouched per D-18 — only this call site goes away).
    3. **P2P task handler (:466-473):** guard becomes `if ( m_gcsDb && m_gcsDb->IsRunning() )`; Get becomes `m_gcsDb->GetReputation( from_peer )`; on success `out.reputation_ = rep_res.value().m_globalScore;` (unchanged semantics).
    4. **Stop (:708-709):** replace with `if ( m_gcsDb ) { m_gcsDb->Shutdown(); }`.

    **src/api/CMakeLists.txt:** add `neoswarm_storage` to neoswarm_api's target_link_libraries (read the file first; add alongside neoswarm_reputation).

    **src/main.cpp:307:** `cfg.m_gcsDbPath = args.db_path_;` and rename the CLI flag registration for `--reputation-db-path` → `--gcs-db-path` (find the flag definition in main.cpp's argument parsing; update help text to "GCS GlobalDB path"). Keep `args.db_path_` member name or rename to `args.gcs_db_path_` for clarity — rename it, it's the same minimal diff size and removes a stale name (check the args struct definition in main.cpp).

    **Acceptance criteria:**
    - `grep -c "ReputationStorage\|ReputationCRDT" src/api/api_server.cpp src/api/api_server.hpp` == 0
    - `grep -c "GcsGlobalDb" src/api/api_server.cpp src/api/api_server.hpp` >= 3
    - `grep -c "PutReputation\|GetReputation" src/api/api_server.cpp` >= 3
    - `grep -c "BroadcastCRDT" src/api/api_server.cpp` == 0
    - `grep -c "m_gcsDbPath" src/api/api_server.hpp src/main.cpp` >= 2
    - `grep -c "m_reputationDbPath" -r src/` == 0
    - `grep -c "neoswarm_storage" src/api/CMakeLists.txt` >= 1
  </action>
  <verify>
    <automated>bash -c 'grep -c "ReputationStorage\|ReputationCRDT" src/api/api_server.cpp src/api/api_server.hpp | grep -q ":0" && grep -c "GcsGlobalDb" src/api/api_server.cpp | grep -q "[1-9]" && grep -c "BroadcastCRDT" src/api/api_server.cpp | grep -q "^0$" && cd build/OSX/Debug && ninja neoswarm_api'</automated>
  </verify>
  <done>
    ApiServer owns a GcsGlobalDb constructed from m_gcsDbPath, initialized only on the network path after SGClient is up (D-20 ordering). UpdateReputation and the P2P handler read/write reputation through the typed ops; the manual CRDT merge/broadcast path is deleted. Config knob renamed end-to-end including the CLI flag.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete reputation_storage/reputation_crdt, strip direct RocksDB linkage, delete flutter_slm_bridge (D-09, D-10, D-19, D-24)</name>
  <files>
    src/reputation/reputation_storage.hpp
    src/reputation/reputation_storage.cpp
    src/reputation/reputation_crdt.hpp
    src/reputation/reputation_crdt.cpp
    src/reputation/CMakeLists.txt
    src/memory/CMakeLists.txt
    src/core/CMakeLists.txt
    flutter_slm_bridge/
  </files>
  <read_first>
    src/reputation/CMakeLists.txt
    src/memory/CMakeLists.txt
    src/core/CMakeLists.txt
  </read_first>
  <behavior>
    - Test 1: the four files no longer exist on disk
    - Test 2: `grep -rn "ReputationStorage\|ReputationCRDT" src/ test/` returns zero matches (after Task 3's test rewrite — sequence the grep gate accordingly: this task removes src/ references; test/ references are removed in Task 3)
    - Test 3: `grep -rn "rocksdb" src/*/CMakeLists.txt` returns zero matches
    - Test 4: `grep -rn "rocksdb::DB\|#include <rocksdb/" src/` returns zero matches
    - Test 5: flutter_slm_bridge/ directory no longer exists
    - Test 6 (build): full `ninja` succeeds — nothing referenced the deleted code that Task 1/3 didn't already rewire
  </behavior>
  <action>
    **Reference sweep FIRST (before any deletion):** run `grep -rln "ReputationStorage\|ReputationCRDT\|reputation_storage\|reputation_crdt" src/ test/ cmake/ CMakeLists.txt` and enumerate every hit. Expected remaining hits after Task 1: `src/reputation/CMakeLists.txt` (source list), `test/reputation/test_reputation.cpp` (Task 3 handles), `test/CMakeLists.txt` (libs line if it names the deleted sources — it names only the target, verify), the four files themselves. Any UNEXPECTED hit (another consumer) → STOP and surface to the user before deleting (project rule: fix root cause, no surprises).

    **Deletions (D-09, D-19):**
    - `rm src/reputation/reputation_storage.hpp src/reputation/reputation_storage.cpp src/reputation/reputation_crdt.hpp src/reputation/reputation_crdt.cpp`

    **src/reputation/CMakeLists.txt:** remove `reputation_storage.cpp` and `reputation_crdt.cpp` from the add_library source list (lines 4-5); delete the entire RocksDB conditional block (lines 18-22). Result: neoswarm_reputation = reputation_scoring.cpp + weighted_consensus.cpp linking neoswarm_common + neoswarm_proto only.

    **src/memory/CMakeLists.txt (D-10):** delete the RocksDB conditional block (lines 17-21). memory_init.cpp has no direct rocksdb usage (verify with `grep -n rocksdb src/memory/memory_init.cpp` — expected 0; if non-zero, STOP and surface).

    **src/core/CMakeLists.txt (D-10):** line 113 — remove `find_library(_ROCKSDB_LIB ...)` and remove `_ROCKSDB_LIB` from the foreach at line 122. Verify `_SNAPPY_LIB` (line 114) is still needed by another transitive dep before touching it — snappy is rocksdb's compressor; check whether anything else links it (`grep -n snappy src/core/CMakeLists.txt`); if _SNAPPY_LIB only served rocksdb, remove both lines together; if unsure, keep snappy and note it in the SUMMARY (RocksDB still arrives transitively via crdt_globaldb — that's permitted).

    **flutter_slm_bridge/ deletion (D-24):** confirm zero CMake references outside the directory itself (`grep -rn "flutter_slm_bridge" --include=CMakeLists.txt . | grep -v "^./flutter_slm_bridge"` — expected 0; the dir's internal CMakeLists reference each other only). Then `git rm -r flutter_slm_bridge/` (tracked files) plus remove any untracked residue with `rm -rf flutter_slm_bridge`. Per D-24: fresh ffigen bindings will be generated against genius_elm_chat_completions.h when the Flutter wrapper work happens — no replacement in this phase.

    **Acceptance criteria:**
    - `test ! -f src/reputation/reputation_storage.hpp && test ! -f src/reputation/reputation_crdt.hpp` (all four gone)
    - `grep -c "reputation_storage\|reputation_crdt" src/reputation/CMakeLists.txt` == 0
    - `grep -rn "rocksdb" src/reputation/CMakeLists.txt src/memory/CMakeLists.txt src/core/CMakeLists.txt` == 0 hits
    - `test ! -d flutter_slm_bridge`
    - `grep -rn "#include <rocksdb/\|rocksdb::DB" src/` == 0 hits
    - Full `ninja` build passes
  </action>
  <verify>
    <automated>bash -c 'test ! -f src/reputation/reputation_storage.cpp && test ! -f src/reputation/reputation_crdt.cpp && test ! -d flutter_slm_bridge && ! grep -rq "rocksdb" src/reputation/CMakeLists.txt src/memory/CMakeLists.txt src/core/CMakeLists.txt && ! grep -rn "#include <rocksdb/" src/ && cd build/OSX/Debug && cmake .. -G "Ninja" -DCMAKE_BUILD_TYPE=Debug && ninja'</automated>
  </verify>
  <done>
    The four replaced files are deleted, all three direct RocksDB linkages are stripped (RocksDB still arrives transitively via crdt_globaldb — allowed), flutter_slm_bridge/ is gone, zero rocksdb includes or symbols remain under src/, and the full build is green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewrite reputation test suite against GcsGlobalDb</name>
  <files>
    test/reputation/test_reputation.cpp
    test/CMakeLists.txt
  </files>
  <read_first>
    test/reputation/test_reputation.cpp
    test/storage/test_gcs_global_db.cpp
    test/CMakeLists.txt
    src/storage/gcs_global_db.hpp
  </read_first>
  <behavior>
    - Test 1: ReputationScoring and WeightedConsensus tests are byte-for-byte preserved (they test pure logic untouched by this phase)
    - Test 2: Former ReputationStorage tests (Put/Get/Remove/GetAll round-trips) now exercise GcsGlobalDb via the injected-pubsub seam — same assertions, new subject
    - Test 3: Former ReputationCRDT merge tests are DELETED, not ported (D-19 — merge semantics are GlobalDB's; the Wave 2 convergence suite covers the behavior end-to-end)
    - Test 4: The suite uses the wait-condition template; zero sleep_for
  </behavior>
  <action>
    **Read the full test file first** and partition it into three groups: (a) ReputationScoring tests, (b) WeightedConsensus tests, (c) ReputationStorage + ReputationCRDT tests.

    **Groups (a) and (b):** keep verbatim — these classes are untouched. Only the includes at the top change.

    **Group (c) rewrite:**
    - Replace includes `reputation/reputation_storage.hpp` + `reputation/reputation_crdt.hpp` with `storage/gcs_global_db.hpp`.
    - DELETE all ReputationCRDT tests (Merge/Get/GetAll/Serialize/DeserializeAndMerge) outright per D-19.
    - Rewrite ReputationStorage tests against GcsGlobalDb using the Wave 1/2 test seam: per-test temp dir, `KeyPairFileStorage`, `GossipPubSub` on port 0, `Initialize( pubsub )`. Reuse the wait-condition helper pattern from test/storage/test_gcs_global_db.cpp (duplicate the small anonymous-namespace helper into this file — do NOT extract a shared header in this wave; two copies is the smaller diff and the consolidation is a separate cleanup).
    - Map old assertions to new API: `Put/Get` → `PutReputation/GetReputation`; `GetAll` → `QueryReputations`; `Remove` → `RemoveReputation`; not-found → `Error::ReputationNotFound`; `IsOpen` → `IsRunning`.
    - Drop the old `PutBatch` test (no batch op on GcsGlobalDb in Phase 3 — callers loop; if an old test depended on atomicity, note the semantic change in the SUMMARY).

    **test/CMakeLists.txt:** update the test_reputation libs line — add `neoswarm_storage` (test now constructs GcsGlobalDb): `"neoswarm_reputation;neoswarm_storage;neoswarm_common"`. If pubsub fixture symbols need it, mirror the include-dir customization pattern used for test_gcs_global_db.

    **Acceptance criteria:**
    - `grep -c "reputation_storage\|reputation_crdt\|ReputationStorage\|ReputationCRDT" test/reputation/test_reputation.cpp` == 0
    - `grep -c "GcsGlobalDb" test/reputation/test_reputation.cpp` >= 1
    - `grep -c "sleep_for" test/reputation/test_reputation.cpp` == 0
    - `grep -c "TEST" test/reputation/test_reputation.cpp` — scoring + consensus test count unchanged from before the rewrite (count and compare)
    - `cd build/OSX/Debug && ninja test_reputation && ./test/test_reputation` green
    - Full suite: `ctest --test-dir build/OSX/Debug --output-on-failure` green
  </action>
  <verify>
    <automated>bash -c 'grep -c "ReputationStorage\|ReputationCRDT" test/reputation/test_reputation.cpp | grep -q "^0$" && grep -c "GcsGlobalDb" test/reputation/test_reputation.cpp | grep -q "[1-9]" && grep -c "sleep_for" test/reputation/test_reputation.cpp | grep -q "^0$" && cd build/OSX/Debug && ninja && ctest --output-on-failure'</automated>
  </verify>
  <done>
    The reputation suite tests scoring and consensus verbatim, exercises storage behavior through GcsGlobalDb with real port-0 pubsubs under the wait-condition template, contains no ReputationCRDT tests, and the entire CTest suite passes with zero sleep_for.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ApiServer → GcsGlobalDb | Internal component boundary; reputation writes now cross into CRDT storage |
| Deleted code → remaining build | Dangling references would surface as compile errors (fail-safe) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | Reputation convergence path | mitigate | Manual ReputationCRDT merge + BroadcastCRDT removed (D-19); convergence now inherits GlobalDB's CRDT machinery with tombstones — strictly stronger than the deleted LWW register |
| T-03-10 | Denial of Service | Local-only mode without GCS init | mitigate | UpdateReputation/peer-handler guards fail closed on !IsRunning(); single-node inference unaffected; degraded mode logged |
| T-03-11 | Information Disclosure | Deleted flutter_slm_bridge | accept | Abandoned code referenced nonexistent GeniusSlm* symbols (D-24); deletion removes misleading surface area; regeneration path documented in CONTEXT |
| T-03-12 | Elevation of Privilege | CLI flag rename | mitigate | --reputation-db-path no longer accepted; operators must use --gcs-db-path — fail-fast at argument parse rather than silently writing to an unexpected path |
</threat_model>

<verification>
Build verification:
```bash
cd build/OSX/Debug && cmake .. -G "Ninja" -DCMAKE_BUILD_TYPE=Debug && ninja
```

Grep gates (all must pass — ROADMAP constraint #9 + research acceptance tests):
```bash
# Deleted code is gone
test ! -f src/reputation/reputation_storage.hpp
test ! -f src/reputation/reputation_storage.cpp
test ! -f src/reputation/reputation_crdt.hpp
test ! -f src/reputation/reputation_crdt.cpp
test ! -d flutter_slm_bridge

# Zero direct RocksDB in NEO-SWARM source
! grep -rn "#include <rocksdb/" src/
! grep -rn "rocksdb::DB" src/
! grep -rq "rocksdb" src/reputation/CMakeLists.txt src/memory/CMakeLists.txt src/core/CMakeLists.txt

# Zero dead symbols
! grep -rn "ReputationStorage\|ReputationCRDT" src/ test/
! grep -rn "m_reputationDbPath" src/ test/

# Rewiring present
grep -c "GcsGlobalDb" src/api/api_server.cpp src/api/api_server.hpp    # >= 3 combined
grep -c "PutReputation" src/api/api_server.cpp                         # >= 1
grep -c "GetReputation" src/api/api_server.cpp                         # >= 2
```

Test verification:
```bash
cd build/OSX/Debug && ctest --output-on-failure
```
</verification>

<success_criteria>
- [ ] api_server uses GcsGlobalDb at all five former usage sites; m_repStorage/m_repCrdt members and BroadcastCRDT call deleted (D-09, D-19)
- [ ] GCS init gated on the network path after SGClient (D-20 ordering); local-only mode degrades gracefully with fail-closed guards
- [ ] Config::m_gcsDbPath (default ./gcs.db) replaces m_reputationDbPath; CLI flag --gcs-db-path (D-02)
- [ ] reputation_storage.{hpp,cpp} + reputation_crdt.{hpp,cpp} deleted; neoswarm_reputation builds without them
- [ ] RocksDB conditional blocks removed from reputation/memory/core CMakeLists; _ROCKSDB_LIB find_library removed from core (D-10)
- [ ] flutter_slm_bridge/ deleted from disk and git (D-24)
- [ ] Grep gates: zero `#include <rocksdb/`, zero `rocksdb::DB`, zero ReputationStorage/ReputationCRDT symbols under src/ and test/
- [ ] Reputation tests rewritten against GcsGlobalDb; full CTest suite green; zero sleep_for
</success_criteria>

<output>
After completion, create `.planning/workstreams/neoswarm/phases/03-gcs-globaldb-integration/03-03-SUMMARY.md` and STOP — wave 4 requires explicit user approval per the Phase 8 post-mortem workflow rule.
</output>
