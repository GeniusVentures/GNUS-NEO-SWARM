---
phase: 02-training-distillation-quality
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - gnus-poc/training/validation.py
  - gnus-poc/training/train_specialists_mlx.py
  - gnus-poc/tests/test_training_validation.py
  - gnus-poc/tests/test_training_config.py
autonomous: true
requirements: [TRAIN-01, TRAIN-02]

must_haves:
  truths:
    - "LoRA adapter weights load cleanly via MLX after training — loadability check catches corrupt adapters before pipeline continues"
    - "Post-training validation loss on held-out test set is below the per-specialist validation_loss_threshold"
    - "Adapter produces different inference output from the base model (behavioral difference: Jaccard < 0.95)"
    - "Objective errors (wrong facts, wrong code, wrong math) are tracked separately from subjective differences (style, tone, phrasing)"
    - "Training metadata (loss curve, hyperparameters, duration) is recorded in training_metadata.json for each specialist"
    - "All training hyperparameters from YAML config are respected at runtime; invalid combinations produce clear validation errors"
    - "Phase 1 interfaces (TeacherClient, cascade) are importable; held-out test splits exist per specialist"
  artifacts:
    - path: "gnus-poc/training/validation.py"
      provides: "validate_adapter function — multi-prong adapter validity check per D-08"
      min_lines: 80
    - path: "gnus-poc/training/train_specialists_mlx.py"
      provides: "Extended train_specialist() with post-training validation pass on held-out test set"
      contains: "validate_adapter"
    - path: "gnus-poc/tests/test_training_validation.py"
      provides: "Unit tests for adapter loadability, validation loss, behavioral diff, objective error tracking"
      min_lines: 120
    - path: "gnus-poc/tests/test_training_config.py"
      provides: "Unit tests for hyperparameter validation from config: override respect, invalid combo rejection"
      min_lines: 60
  key_links:
    - from: "gnus-poc/training/train_specialists_mlx.py"
      to: "gnus-poc/training/validation.py"
      via: "import validate_adapter"
      pattern: "from.*validation.*import.*validate_adapter"
    - from: "gnus-poc/training/validation.py"
      to: "mlx_lm.utils.load"
      via: "MLX model loading with adapter_path"
      pattern: "mlx_lm.*utils.*load.*adapter_path"
    - from: "gnus-poc/tests/test_training_config.py"
      to: "gnus-poc/training/train_specialists_mlx.py"
      via: "pytest tests hyperparameter config override and validation"
      pattern: "hyperparameter.*config"
---

<objective>
Add post-training adapter validation to the MLX LoRA trainer. Implement multi-prong adapter validity checks (loadability, validation loss, behavioral difference) on a held-out test set. Record training metadata completeness. Ensure all hyperparameters from YAML config are respected at runtime. Pre-flight: verify Phase 1 interfaces are available and test splits exist.

Purpose: Transform prototype training from "produces adapter files" to "produces verified adapters." Catch corrupt/invalid adapters at training time rather than downstream. Per D-06, use a separate held-out test set (not the training val_batches) for an independent quality signal. BLOCKER 2 resolution: verify Phase 1 completion and test data availability before implementation.

Output: validation.py, extended train_specialists_mlx.py, 2 test files
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/workstreams/poc/phases/02-training-distillation-quality/02-RESEARCH.md
@.planning/workstreams/poc/phases/02-training-distillation-quality/02-PATTERNS.md
@.planning/workstreams/poc/phases/02-training-distillation-quality/02-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs, extracted from codebase. -->

From gnus-poc/training/train_specialists_mlx.py:
```python
PROJECT_ROOT = Path(__file__).resolve().parent.parent

def train_specialist(niche_name: str):
    """Trains a LoRA adapter for a specialist niche using MLX.
    
    Loads data from data/specialists/{niche}_mlx/{train,valid}.jsonl
    Calls mlx_lora.train_model()
    Produces: adapter_config.json, adapter_model.safetensors, training_metadata.json
    Returns: metadata dict with niche, base_model, trained_at, status
    """
    ...

# Key patterns:
# MLX loading: model, tokenizer = mlx_utils.load(model_id, tokenizer_config={"trust_remote_code": True})
# Metadata writing: json.dump(metadata, f, indent=2)
# Error handling: try/except block in main() catches per-niche failures
```

From gnus-poc/distill/cascade.py (result object pattern):
```python
class CascadeResult:
    def __init__(self, final_content, confidence, escalated, attempts, ...):
        self.final_content = final_content
        ...
    def to_dict(self):
        return { ... }  # JSON-serializable
```

From gnus-poc/eval/metric_store.py (path resolution pattern):
```python
def __init__(self, project_root: Optional[Path] = None):
    if project_root is None:
        project_root = Path(__file__).resolve().parent.parent
    self._project_root = project_root
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight verification — Phase 1 interfaces and test split availability (BLOCKER 2 resolution)</name>
  <files>gnus-poc/tests/test_training_validation.py</files>
  <read_first>
    gnus-poc/distill/teacher.py — read lines 1-30 for TeacherClient class definition and top-level imports
    gnus-poc/distill/cascade.py — read lines 1-30 for TeacherCascade class definition and top-level imports
    gnus-poc/training/train_specialists_mlx.py — read lines 1-35 for PROJECT_ROOT pattern and specialist config loading
    gnus-poc/config/specialists/code.yaml — read for base_model and existing training section
    .planning/workstreams/poc/phases/02-training-distillation-quality/02-RESEARCH.md — read lines 745-759 (Open Questions section)
  </read_first>
  <action>
    Pre-flight verification addressing RESEARCH.md Open Questions 1 and 2. Per revision checker BLOCKER 2: Verify Phase 1 interfaces are importable and test splits exist before Phase 2 implementation proceeds.

    **Verify Phase 1 interface availability (Open Question 1):**
    1. Attempt import: `from distill.teacher import TeacherClient` — assert import succeeds
    2. Attempt import: `from distill.cascade import TeacherCascade` — assert import succeeds
    3. If either import fails, log a clear warning: "Phase 1 interface not available — Phase 2 depends on TeacherClient and TeacherCascade from Phase 1. Verify Phase 1 is complete before proceeding."
    4. Do NOT fail the task — Phase 1 may be complete but paths may differ. Log the result as pre-flight state.

    **Verify test split existence (Open Question 2):**
    1. For each specialist niche in ["code", "encyclopedic", "medical", "patents", "qa_technical"]:
       - Check if `data/specialists/{niche}/test.jsonl` exists
       - If exists: log "test split found for {niche}" with line count
       - If missing: create a minimal test split from the valid.jsonl
         - Read `data/specialists/{niche}_mlx/valid.jsonl` (MLX-formatted data)
         - Take the last max(10, 20% of total) samples as the test split
         - Write to `data/specialists/{niche}/test.jsonl`
         - Log: "created test split for {niche} from valid.jsonl — {N} samples"
       - If neither valid.jsonl exists: log warning and skip (niche has no training data yet)
    2. Verify each test split has at least min_test_samples from specialist config (default 50). Log warning if below threshold.

    **Update RESEARCH.md Open Questions:**
    - Mark question 1 as RESOLVED with note: "Phase 1 interface availability verified in [DATE] — pre-flight task 02-03 Task 0."
    - Mark question 2 as RESOLVED with note: "Test splits verified/created in [DATE] — pre-flight task 02-03 Task 0. Split sources logged."

    **Create scaffolding test in test_training_validation.py:**
    - Add `test_phase1_interfaces_importable()`: asserts TeacherClient and TeacherCascade importable
    - Add `test_test_splits_exist()`: asserts test.jsonl exists per specialist or is created

    Use PROJECT_ROOT = Path(__file__).resolve().parent.parent.parent / "gnus-poc" for path resolution.
    Use importlib for safe imports: `import importlib; importlib.import_module("distill.teacher")`.
  </action>
  <verify>
    <automated>pytest tests/test_training_validation.py::test_phase1_interfaces_importable tests/test_training_validation.py::test_test_splits_exist -x -v</automated>
  </verify>
  <acceptance_criteria>
    Pre-flight verification complete. Phase 1 interfaces confirmed importable (or missing interfaces logged). Test splits exist or are created for all 5 specialists. RESEARCH.md Open Questions 1 and 2 marked RESOLVED.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Create post-training validation module with multi-prong adapter checks</name>
  <files>gnus-poc/training/validation.py</files>
  <read_first>
    gnus-poc/training/train_specialists_mlx.py — read all lines, focusing on:
      - line 36: PROJECT_ROOT pattern
      - lines 148-201: train_specialist() function — MLX loading + metadata writing
      - lines 298-305: try/except error handling pattern
      - line 187: mlx_lora.train_model() call (where validation runs AFTER)
    gnus-poc/eval/evaluator.py — read lines 17-50 for SpecialistEvaluator.__init__ and evaluate() API (validation loss computation)
    gnus-poc/eval/metric_store.py — read lines 49-57 for project_root default pattern
    .planning/workstreams/poc/phases/02-training-distillation-quality/02-RESEARCH.md — read lines 374-395 (Pattern 2: Post-Training Validation Pass) and lines 674-715 (code example)
  </read_first>
  <behavior>
    - Test 1 (test_validate_adapter_loadability_fails_on_bad_path): validate_adapter with nonexistent adapter_path returns loadable=False and load_error message. Overall valid is False.
    - Test 2 (test_validate_adapter_loadability_succeeds_with_mock): Mocked mlx_utils.load returns model+tokenizer. validate_adapter sets loadable=True.
    - Test 3 (test_validation_loss_below_threshold_passes): Mocked validation loss = 2.0, threshold = 3.0. loss_valid=True.
    - Test 4 (test_validation_loss_above_threshold_fails): Mocked validation loss = 4.0, threshold = 3.0. loss_valid=False.
    - Test 5 (test_behavioral_diff_jaccard_below_095_passes): Jaccard similarity = 0.7 (< 0.95). behavioral_diff=True.
    - Test 6 (test_behavioral_diff_jaccard_above_095_fails): Jaccard similarity = 0.97 (>= 0.95). behavioral_diff=False.
    - Test 7 (test_overall_valid_only_when_all_three_pass): loadable=False -> overall_valid=False even if other checks would pass. All three must pass for overall_valid=True.
    - Test 8 (test_objective_error_tracking): Test samples with known wrong answers report objective_errors count > 0 when predictions differ from ground truth.
  </behavior>
  <action>
    Create gnus-poc/training/validation.py implementing the multi-prong adapter validity check per D-08 (loadability, validation loss, behavioral diff) and D-06 (held-out test set, NOT training val_batches).

    Module-level function:
    ```python
    def validate_adapter(model_id: str, adapter_path: str, test_samples: list,
                         loss_threshold: float, behavioral_diff_threshold: float = 0.95) -> dict:
    ```

    Returns dict with keys: loadable (bool), load_error (str|None), validation_loss (float|None), loss_valid (bool), jaccard_similarity (float|None), behavioral_diff (bool), objective_errors (int), subjective_diffs (int), overall_valid (bool).

    Three checks per D-08:
    (a) Loadability: try: mlx_utils.load(model_id, adapter_path=adapter_path); except -> loadable=False, return early with load_error
    (b) Validation loss: compute cross-entropy loss on test_samples. loss <= loss_threshold -> loss_valid=True. Use SpecialistEvaluator pattern for loss computation.
    (c) Behavioral difference: load base model without adapter, run inference on first 5 test_samples. Compute Jaccard token-level similarity between base and adapter outputs. Jaccard < behavioral_diff_threshold -> behavioral_diff=True.

    Objective error tracking (per D-08): If test_samples contain "label" or "ground_truth" keys, compare adapter output to ground truth. Count mismatches as objective_errors. Track subjective differences separately (when outputs differ but no ground truth to verify).

    Follow training/train_specialists_mlx.py patterns:
    - Use PROJECT_ROOT = Path(__file__).resolve().parent.parent
    - import logging; logger = logging.getLogger(__name__)
    - Try/except around MLX operations (fail-open — return result dict with error info, never raise)
    - No print() in library code — use logger.info/warning/error

    Helper functions:
    - _compute_validation_loss(model, tokenizer, test_samples) -> float
    - _compute_jaccard_similarity(base_model, adapter_model, tokenizer, samples) -> float
    - _track_objective_errors(adapter_model, tokenizer, test_samples) -> dict
  </action>
  <verify>
    <automated>pytest tests/test_training_validation.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    validate_adapter function in gnus-poc/training/validation.py. All 8 tests passing. Returns structured result dict with loadable, loss_valid, behavioral_diff, overall_valid, and error tracking.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Integrate post-training validation into train_specialist() and add comprehensive test coverage including hyperparameter config tests</name>
  <files>gnus-poc/training/train_specialists_mlx.py, gnus-poc/tests/test_training_validation.py, gnus-poc/tests/test_training_config.py</files>
  <read_first>
    gnus-poc/training/train_specialists_mlx.py — read lines 148-227 (train_specialist function — insert validation after line 187 mlx_lora.train_model())
    gnus-poc/training/validation.py — Task 1 output (validate_adapter API)
    gnus-poc/tests/test_distillation.py — read all 73 lines for class-based test pattern
    gnus-poc/tests/test_benchmarker.py — read lines 1-80 for tmp_path + MagicMock test patterns
    .planning/workstreams/poc/phases/02-training-distillation-quality/02-RESEARCH.md — read lines 548-558 (Pitfall 2: Adapter Loadability Fails Silently) and lines 560-564 (Pitfall 4: Validation Set Leakage)
  </read_first>
  <action>
    Per TRAIN-01 and TRAIN-02: Integrate post-training validation into the existing train_specialist() function. Do NOT rewrite — surgical insertion only. Create hyperparameter config validation test file (WARNING 2 resolution).

    **Extend gnus-poc/training/train_specialists_mlx.py train_specialist():**

    After `mlx_lora.train_model()` call (line 187):
    1. Load test split (D-06: NOT the training val_batches):
       - Read test_split_path from specialist config (default: data/specialists/{niche}/test.jsonl)
       - If test file missing, log warning and skip validation (non-blocking per Pitfall 4 avoidance)
       - Load test_samples from JSONL file (json.loads per line)
    2. Import and call validate_adapter():
       ```python
       from training.validation import validate_adapter
       val_result = validate_adapter(
           model_id=base_model,
           adapter_path=str(adapter_path),
           test_samples=test_samples,
           loss_threshold=validation_loss_threshold,
       )
       ```
    3. Record validation result in metadata:
       ```python
       metadata["validation"] = {
           "overall_valid": val_result["overall_valid"],
           "loadable": val_result["loadable"],
           "loss_valid": val_result["loss_valid"],
           "validation_loss": val_result["validation_loss"],
           "behavioral_diff": val_result["behavioral_diff"],
           "jaccard_similarity": val_result["jaccard_similarity"],
           "objective_errors": val_result["objective_errors"],
           "subjective_diffs": val_result["subjective_diffs"],
       }
       ```
    4. Set training status based on validation:
       - If val_result["overall_valid"] -> status = "complete"
       - If not val_result["overall_valid"] -> status = "training_complete_validation_failed"
       - If load_error -> status = "training_complete_adapter_not_loadable"
    5. Write updated metadata to training_metadata.json

    **Hyperparameter configurability (TRAIN-02):**
    - Read specialist config YAML via existing config loading mechanism
    - Validate hyperparameter combinations at load: batch_size > 0, learning_rate > 0, lora_rank >= 1, iterations > 0
    - Invalid combos -> raise clear ValueError with field name and current value
    - Existing argparser CLI already reads from config — verify it's wired correctly

    **Test file verification — test_training_validation.py:**
    Ensure gnus-poc/tests/test_training_validation.py (created in Task 1) is comprehensive:
    - Class TestValidateAdapter with 8 test methods from Task 1 behaviors
    - Use @patch("training.validation.mlx_utils.load") for MLX mocking
    - Use MagicMock for model/tokenizer mocks
    - Follow test_benchmarker.py pattern: class-based, pytest, tmp_path
    - Add import tests: verify validate_adapter is importable from training.validation

    **Create gnus-poc/tests/test_training_config.py (WARNING 2 resolution — TRAIN-02):**
    - Class TestTrainingConfig with test methods:
      test_hyperparameter_override_from_config: verify specialist YAML values (lora_rank, iterations, batch_size) are read and applied at runtime
      test_invalid_batch_size_rejected: batch_size <= 0 raises ValueError with clear message
      test_invalid_learning_rate_rejected: learning_rate <= 0 raises ValueError
      test_invalid_lora_rank_rejected: lora_rank < 1 raises ValueError
      test_invalid_iterations_rejected: iterations <= 0 raises ValueError
      test_valid_config_accepted: all valid values pass validation without error
    - Follow test_distillation.py class-based pytest pattern with tmp_path
    - Import hyperparameter validation from training.train_specialists_mlx or config loader

    Run all tests to green:
    ```
    pytest tests/test_training_validation.py tests/test_training_config.py -x -v
    ```
  </action>
  <verify>
    <automated>pytest tests/test_training_validation.py tests/test_training_config.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    train_specialist() runs post-training validation after mlx_lora.train_model(). Validation results recorded in training_metadata.json. Status reflects validation outcome. All hyperparameter validation checks in place. test_training_config.py created with 6+ tests for config validation. All tests passing.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Training data -> Trainer | Test split loaded from disk — must validate file exists and is parseable |
| Adapter weights -> Pipeline | Validation must catch corrupt/invalid adapters before downstream stages consume them |
| Specialist config -> Trainer | Validation loss threshold and test_split_path from YAML — must validate at load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Denial of Service | training/validation.py validate_adapter | mitigate | Try/except around MLX load — load failure returns result dict with error info rather than crashing. Pipeline can handle "training_complete_validation_failed" status gracefully. |
| T-02-11 | Tampering | training/validation.py test split loading | mitigate | Test split path validated against project root (no path traversal). File missing -> log warning, skip validation (non-blocking per Pitfall 4). JSONL parse errors -> log error, skip that sample. |
| T-02-12 | Information Disclosure | training/validation.py validation loss | accept | Validation loss values are training metrics — not sensitive. No PII or secrets exposed in validation result dict. |
| T-02-13 | Spoofing | training/train_specialists_mlx.py status flag | mitigate | training_metadata.json includes validation results with objective_errors count. Pipeline downstream stages can check overall_valid before trusting adapter. Status reflects actual validation outcome (not just file existence). |
| T-02-14 | Elevation of Privilege | training/train_specialists_mlx.py hyperparameter config | mitigate | Hyperparameter validation at load: batch_size > 0, learning_rate > 0, lora_rank >= 1. Invalid combos raise clear ValueError — blocked before mlx_lora.train_model() is called with invalid params. |
| T-02-SC | Tampering | pip install | mitigate | No new package installs in this plan. Existing dependencies (mlx-lm 0.28.4) already verified. |
</threat_model>

<verification>
**Per-wave merge check:**
```
pytest tests/test_training_validation.py tests/test_training_config.py -x -v
```

**Phase gate check:**
```
pytest tests/ -x -v
```
All training validation and config tests pass; no regression in existing tests.
</verification>

<success_criteria>
- validate_adapter function exists in gnus-poc/training/validation.py with multi-prong checks
- train_specialist() runs validation after mlx_lora.train_model() on held-out test set
- Validation results recorded in training_metadata.json with overall_valid and objective_errors
- Training status reflects validation outcome (complete vs validation_failed vs not_loadable)
- Hyperparameter validation catches invalid combos at load time
- Pre-flight verification confirms Phase 1 interfaces importable and test splits exist
- All tests passing: test_training_validation.py (8+ tests), test_training_config.py (6+ tests)
</success_criteria>

<output>
Create .planning/workstreams/poc/phases/02-training-distillation-quality/02-03-SUMMARY.md when done
</output>
