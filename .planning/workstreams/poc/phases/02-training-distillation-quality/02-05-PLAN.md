---
phase: 02-training-distillation-quality
plan: 05
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - router/__init__.py
  - router/plan.py
  - router/rules.py
  - router/state_machine.py
  - config/router_rules.yaml
autonomous: true
requirements: [ROUTE-01, ROUTE-02]
---

<objective>
Create a greenfield rules-based router module that classifies input queries using YAML-driven keyword, regex, and syntax density rules. Uses a GQHSM-compatible JSON state machine (wrapping transitions) to produce execution plans with fallback chaining.

Purpose: First query routing layer for specialist selection. Rules are deterministic, inspectable, YAML-defined. GQHSM-compatible JSON ensures future C++ portability.
Output: router/__init__.py, router/plan.py, router/rules.py, router/state_machine.py (all new), config/router_rules.yaml (new)
</objective>

<context>
From distill/cascade.py (Phase 1 pattern): Teacher cascade uses confidence threshold + fallback chaining — router mirrors this.
From 02-RESEARCH.md: Pattern 2 (GQHSM JSON Schema), Pattern 3 (RouterStateMachine wrapping transitions), Pitfall 3 (priority conflicts).
From 02-CONTEXT.md: D-09 through D-13 (Python SM + JSON contract, states/transitions/triggers/guards/actions, fallback chaining, GQHSM submodule).
</context>

<tasks>
- Task 1 (tdd): Create router module — ExecutionPlan, RuleEngine, and router_rules.yaml — 8 behavior tests
- Task 2 (auto): Create GQHSM-compatible state machine runtime wrapping transitions
</tasks>

<threat_model>
| T-02-11 | Denial of Service | RuleEngine regex matching | mitigate | Input size limit 100KB |
| T-02-12 | Tampering | router_rules.yaml priority conflicts | mitigate | Duplicate priorities raise ConfigValidationError |
| T-02-13 | Information Disclosure | Classification path logging | accept | Classification path logged for inspectability |
| T-02-14 | Denial of Service | Infinite fallback chaining | mitigate | Bounded to specialists list (max 5 iterations) |
</threat_model>

<verification>
- pytest tests/test_router.py -x -m "not slow" passes (8 behavior tests)
- Code queries route to code specialist; math queries route to math path; no-match → encyclopedic
- router_rules.yaml is sole rule definition — no hardcoded rules in Python
- ExecutionPlan contains primary_specialist, execution_mode, confidence
- RouterStateMachine consumes GQHSM-compatible JSON with 7 states and 9 transitions
</verification>

<success_criteria>
1. RuleEngine classifies queries via keyword, regex, and syntax density rules from YAML config
2. Code/function queries → code specialist; >30% numeric tokens → math path; no-match → encyclopedic
3. Adding a rule requires only a YAML entry — no Python code change
4. ExecutionPlan includes primary_specialist, execution_mode, confidence, matched_rules
5. Fallback chaining tries next-best match when primary confidence below threshold
6. RouterStateMachine consumes GQHSM-compatible JSON definition
7. All tests pass without GPU dependency
</success_criteria>
