---
phase: 01-pipeline-hardening
plan: 04
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - distill/teacher.py
  - distill/teacher_errors.py
  - distill/cascade.py
autonomous: true
requirements: [TEACH-01, TEACH-04, TEACH-05]

must_haves:
  truths:
    - "Level 1 teacher (deepseek-v4-fast) always runs first for every generation request"
    - "When Level 1 logprobs mean confidence is below threshold, the best Level 2 teacher for the detected domain is selected from the benchmark table"
    - "If the first Level 2 teacher's confidence is still below threshold, the next-best Level 2 teacher is tried"
    - "Budget state persists to disk (`artifacts/.budget_state.json`) and is loaded on TeacherClient startup"
    - "When cumulative spend exceeds budget_cap_usd, all teacher API calls raise BudgetExceededError"
    - "Circuit breaker opens after N consecutive failures, stays open for recovery_timeout seconds, then allows one half-open probe"
  artifacts:
    - path: "distill/cascade.py"
      provides: "Multi-teacher cascade orchestrator with benchmark-routed Level 2 escalation"
      exports: ["TeacherCascade", "CascadeResult"]
      min_lines: 80
    - path: "distill/teacher.py"
      provides: "TeacherClient with cascade integration, budget persistence, half-open circuit breaker"
      min_lines: 180
  key_links:
    - from: "distill/teacher.py TeacherClient.generate()"
      to: "distill/cascade.py TeacherCascade.execute()"
      via: "generate() optionally routes through cascade when multi-teacher mode enabled"
      pattern: "TeacherCascade|cascade\.execute"
    - from: "distill/teacher.py TeacherClient._check_budget()"
      to: "artifacts/.budget_state.json"
      via: "JSON file read on startup, written after each API call"
      pattern: "budget_state\.json|_budget_state_path"
    - from: "distill/teacher.py TeacherClient._check_circuit()"
      to: "circuit breaker state machine"
      via: "half-open probe after recovery_timeout expires"
      pattern: "half.open|recovery_timeout|_circuit_opened_at"
---

<objective>
Implement multi-teacher cascade orchestration with benchmark-routed Level 2 escalation (per D-01/D-02/D-03), persistent budget enforcement loading cumulative spend from disk on startup (per D-13/D-14), and a circuit breaker with half-open recovery (per TEACH-05).

Purpose: Enable the three-tier benchmark-routed teacher cascade, enforce budget persistence across runs, and implement a production-grade circuit breaker with half-open state.
Output: `distill/cascade.py` (new), updated `distill/teacher.py`, updated `distill/teacher_errors.py`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-pipeline-hardening/01-CONTEXT.md

<interfaces>
<!-- Current TeacherClient after Plan 02 refactor -->
From distill/teacher.py (after Plan 02):
```python
class TeacherClient:
    def __init__(self, config_path=None, project_root=None):
        # Loads config, builds backend registry from endpoints/models
        # Has: self._backends, self._models, self._config["teacher"]
        # Budget: self._total_cost, self._budget_cap (in-memory only — Plan 04 adds persistence)
        # Circuit: self._circuit_open, self._consecutive_failures, self._max_consecutive_failures

    def _resolve_backend(self, model_name: str):
        # Returns backend instance for model_name

    def _call_api(self, model_name: str, messages, **kwargs) -> dict:
        # Retry loop, circuit check, budget check, backend.generate()

    def generate(self, model_name: str, messages, **kwargs) -> Response:
        # Dispatches to _call_api, resets failures, logs cost

    def generate_with_logprobs(self, model_name: str, messages, **kwargs) -> Response:
        # logprobs=True wrapper
```

<!-- Current error types -->
From distill/teacher_errors.py:
```python
class TeacherConfigError(Exception): pass
class BudgetExceededError(Exception): pass
class CircuitBreakerOpenError(Exception): pass
class SyntheticDataError(Exception): pass      # Phase 1
class BackendNotFoundError(TeacherConfigError): pass  # Added in Plan 02
```

<!-- Config structure (from Plan 01) -->
```yaml
teacher:
  level1: "deepseek-v4-fast"
  confidence_threshold: 0.7
  max_retries: 3
  backoff_base_seconds: 2.0
  budget_cap_usd: 5.0
  circuit_breaker_failure_threshold: 5
  circuit_breaker_recovery_timeout: 60

teacher_benchmark:
  coding: {"deepseek-v4-pro[1m]": 0.82, "glm-5.2": 0.91}
  orchestration: {"deepseek-v4-pro[1m]": 0.88, "glm-5.2": 0.75}
  medical: {"deepseek-v4-pro[1m]": 0.79, "glm-5.2": 0.85}
  qa_technical: {"deepseek-v4-pro[1m]": 0.85, "glm-5.2": 0.78}
  encyclopedic: {"deepseek-v4-pro[1m]": 0.90, "glm-5.2": 0.72}
  patents: {"deepseek-v4-pro[1m]": 0.83, "glm-5.2": 0.70}

models:
  deepseek-v4-fast:
    endpoint: litellm
    model_id: "deepseek-v4-fast"
  deepseek-v4-pro[1m]:
    endpoint: litellm
    model_id: "deepseek-v4-pro[1m]"
  glm-5.2:
    endpoint: litellm
    model_id: "glm-5.2"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement multi-teacher cascade with benchmark-routed Level 2 escalation</name>
  <files>distill/cascade.py, distill/teacher.py</files>
  <read_first>
    distill/teacher.py — current state after Plan 02 (multi-backend refactor); read to understand the `_call_api` and `generate` method structure.
    CONTEXT.md D-01, D-02, D-03 — the cascade design: Level 1 always Fast, Level 2 benchmark-routed, confidence threshold trigger, GLM 5.2 not mandatory.
    config/pipeline.yaml — the `teacher`, `teacher_benchmark`, `models` sections that drive cascade behavior.
  </read_first>
  <action>
    Create `distill/cascade.py` with:

    **Class `CascadeResult`:**
    - Fields: `final_content` (str), `level1_confidence` (float or None), `level2_confidence` (float or None), `level1_model` (str), `level2_model` (str or None), `escalated` (bool), `attempts` (list of dicts with model_name, confidence, error)
    - Method `to_dict(self) -> dict` for logging

    **Class `TeacherCascade`:**
    - Constructor `__init__(self, teacher_client, benchmark_table: dict, level1_model: str, confidence_threshold: float)`:
      - `teacher_client` is the TeacherClient instance (for calling `generate_with_logprobs`)
      - `benchmark_table` is the `teacher_benchmark` dict from config
      - `level1_model` is the always-first teacher (e.g., "deepseek-v4-fast")
      - `confidence_threshold` is the logprobs mean threshold for escalation

    - Method `execute(self, messages: list, domain: str, **kwargs) -> CascadeResult`:
      Implements the escalation flow per D-02:

      1. **Level 1 (always):** Call `self._teacher.generate_with_logprobs(self._level1_model, messages, **kwargs)`. Extract logprobs from response. Compute confidence as mean token probability across all tokens with logprobs data: `confidence = exp(mean(logprob.token_logprob for each token position))`. If Level 1 fails with an exception, record it and proceed to Level 2 immediately (treat as confidence=0).

      2. **Confidence check:** If `confidence >= self._confidence_threshold`, return CascadeResult with `escalated=False`, `level1_confidence=confidence`, `final_content=level1_response.content`. Do NOT run Level 2.

      3. **Benchmark routing (if confidence < threshold):** Look up `domain` in `self._benchmark_table`. If domain not found, fall back to the first model in the benchmark table's first domain entry (a reasonable default). Sort Level 2 models by strength_score descending. Iterate through sorted Level 2 models:
         - Call `self._teacher.generate_with_logprobs(model_name, messages, **kwargs)` for each Level 2 model
         - Compute confidence as above
         - If `confidence >= self._confidence_threshold`: return CascadeResult with Level 2 results, `escalated=True`
         - If confidence still below threshold: try next Level 2 model
         - If all Level 2 models exhausted: return the result with the best (highest) confidence among Level 2 attempts, even if below threshold

      4. **Return best result:** The cascade always returns a response — it never fails silently. If all teachers fail (exceptions), raise `TeacherConfigError("All teachers failed for cascade on domain '{domain}'")` with exception details in the message.

      **Domain mapping for routing:** The `domain` parameter maps specialist niches to benchmark table keys:
      - `"code"` → `"coding"`
      - `"medical"` → `"medical"`
      - `"qa_technical"` → `"qa_technical"`
      - `"encyclopedic"` → `"encyclopedic"`
      - `"patents"` → `"patents"`
      - For unknown domains: use the first available benchmark table key as fallback

    **Integration into TeacherClient (distill/teacher.py):**
    - In `__init__`: instantiate `self._cascade = TeacherCascade(self, teacher_benchmark, level1_model, confidence_threshold)` — passing `self` as the teacher_client reference
    - Add method `generate_with_cascade(self, messages, domain: str = "encyclopedic", **kwargs)`:
      - Calls `self._cascade.execute(messages, domain, **kwargs)`
      - Logs cascade result details
      - Returns the `CascadeResult.final_content` wrapped in the same response wrapper format as `generate()`
    - Keep the existing `generate(self, model_name, messages, **kwargs)` method unchanged (for direct model calls without cascade)
    - Keep `generate_with_logprobs(self, model_name, messages, **kwargs)` unchanged (used by cascade internally)

    **Logprobs confidence computation helper:**
    - Add a module-level function `compute_logprob_confidence(response) -> float` in cascade.py:
      - Extracts logprobs from the response (format: `response.choices[0].logprobs.content[].token_logprob` for OpenAI-compatible)
      - Computes `mean_logprob = sum(token_logprobs) / len(token_logprobs)`
      - Returns `math.exp(mean_logprob)` as confidence score (0.0 to 1.0)
      - Returns 0.0 if no logprobs data available or on error
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/GeniusCogntiveSystem/GNUS-NEO-SWARM/gnus-poc && python -c "
from distill.cascade import TeacherCascade, CascadeResult, compute_logprob_confidence
from unittest.mock import MagicMock
import math

# Test 1: CascadeResult construction
r = CascadeResult(final_content='test', level1_confidence=0.85, level2_confidence=None, level1_model='fast', escalated=False, attempts=[])
assert r.escalated == False
d = r.to_dict()
assert d['final_content'] == 'test'

# Test 2: compute_logprob_confidence
mock_resp = MagicMock()
mock_resp.choices = [MagicMock()]
token1 = MagicMock(); token1.token_logprob = math.log(0.9)  # ~0.9 prob
token2 = MagicMock(); token2.token_logprob = math.log(0.8)  # ~0.8 prob
mock_resp.choices[0].logprobs.content = [token1, token2]
conf = compute_logprob_confidence(mock_resp)
assert 0.84 < conf < 0.86, f'Expected ~0.85, got {conf}'

# Test 3: Cascade with high confidence (no escalation)
mock_client = MagicMock()
mock_client.generate_with_logprobs.return_value = mock_resp
benchmark = {'coding': {'deepseek-v4-pro[1m]': 0.82, 'glm-5.2': 0.91}}
cascade = TeacherCascade(mock_client, benchmark, 'deepseek-v4-fast', 0.7)
result = cascade.execute([{'role': 'user', 'content': 'test'}], 'coding')
assert result.escalated == False
assert result.level1_model == 'deepseek-v4-fast'
mock_client.generate_with_logprobs.assert_called_once()  # Level 1 only

# Test 4: domain_map_for_niche
from distill.cascade import _DOMAIN_MAP
assert _DOMAIN_MAP['code'] == 'coding'
assert _DOMAIN_MAP['medical'] == 'medical'

print('Cascade tests passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `distill/cascade.py` exists with classes `TeacherCascade` and `CascadeResult`
    - Source: `grep -c "class TeacherCascade" distill/cascade.py` returns `1`
    - Source: `grep -c "def execute" distill/cascade.py` returns `1`
    - Source: `grep -c "compute_logprob_confidence" distill/cascade.py` returns `1`
    - Source: `grep -c "generate_with_cascade" distill/teacher.py` returns `1`
    - Source: `grep -c "TeacherCascade" distill/teacher.py` returns `>= 1`
    - Behavior: When Level 1 confidence >= threshold, Level 2 is NOT called, result has `escalated=False`
    - Behavior: When Level 1 confidence < threshold, the best benchmark-routed Level 2 teacher is called; if Level 2 confidence >= threshold, result has `escalated=True`
    - Behavior: If all Level 2 teachers have confidence below threshold, the best (highest confidence) result is returned (no silent failure)
    - Behavior: compute_logprob_confidence returns approximately the geometric mean of token probabilities (0.0 to 1.0)
    - CLI: `python distill/cascade.py` imports without error
  </acceptance_criteria>
  <done>TeacherCascade implements Level 1 (always Fast) → benchmark-routed Level 2 (if confidence < threshold), integrated into TeacherClient.generate_with_cascade()</done>
</task>

<task type="auto">
  <name>Task 2: Budget persistence to disk and half-open circuit breaker</name>
  <files>distill/teacher.py, distill/teacher_errors.py</files>
  <read_first>
    distill/teacher.py — current state after Plan 02 and Task 1 of this plan; contains `_check_budget`, `_check_circuit`, `_total_cost`, `_circuit_open`, `_consecutive_failures`.
    CONTEXT.md D-13, D-14 — budget persistence requirements.
    CONTEXT.md TEACH-05 — retry and circuit breaker requirements including half-open recovery.
  </read_first>
  <action>
    **Budget Persistence (D-13, D-14):**

    In `TeacherClient.__init__`:
    - Set budget state path: `self._budget_state_path = project_root / "artifacts" / ".budget_state.json"`
    - After loading config, call `self._load_budget_state()`:
      - If `self._budget_state_path.exists()`: read JSON, set `self._total_cost = data.get("cumulative_cost_usd", 0.0)`, set `self._budget_version = data.get("version", 1)`
      - If not exists: `self._total_cost = 0.0`, `self._budget_version = 1`
      - Log: `spdlog` style print or logger line: `f"Budget state loaded: ${self._total_cost:.4f} of ${self._budget_cap:.2f}"`

    - Add method `_save_budget_state(self)`:
      - Write JSON to `self._budget_state_path` containing: `{"cumulative_cost_usd": round(self._total_cost, 6), "budget_cap_usd": self._budget_cap, "last_updated": datetime.now(timezone.utc).isoformat(), "version": self._budget_version}`
      - Create parent directories if needed
      - Call this method after every `_log_cost()` call (i.e., after every successful API call that adds cost)

    - In `generate()`: after `self._total_cost += cost` and `self._log_cost(...)`, call `self._save_budget_state()`

    - In `_check_budget()`: unchanged logic — compares `self._total_cost >= self._budget_cap`, raises BudgetExceededError. The persistence layer ensures this is accurate across runs.

    - Add `--reset-budget` support: Add a class method or flag `reset_budget(self)`: sets `self._total_cost = 0.0`, calls `self._save_budget_state()`, prints reset confirmation. The pipeline runner (Plan 03) will invoke this when `--reset-budget` CLI flag is passed. For now, add the method; CLI integration is tested via direct call.

    **Half-Open Circuit Breaker (TEACH-05):**

    Enhance `_check_circuit()` and `_call_api()`:

    - In `__init__`: store `self._circuit_opened_at = None` (datetime when circuit opened), `self._circuit_failure_threshold` from config (default 5), `self._circuit_recovery_timeout` from config (default 60 seconds), `self._circuit_half_open = False`

    - Rewrite `_check_circuit(self)`:
      ```
      if not self._circuit_open:
          return  # circuit closed, allow calls
      if self._circuit_opened_at is None:
          return  # should not happen, but safe
      elapsed = (current_time - self._circuit_opened_at).total_seconds()
      if elapsed >= self._circuit_recovery_timeout:
          self._circuit_half_open = True
          return  # allow one probe request
      # Circuit is open and timeout not elapsed
      raise CircuitBreakerOpenError(f"Circuit breaker open. {self._circuit_recovery_timeout - elapsed:.0f}s remaining.")
      ```

    - In `_call_api()`: update the circuit breaker trip logic:
      - After each failure that increments `self._consecutive_failures`:
        - If `self._consecutive_failures >= self._circuit_failure_threshold` and not `self._circuit_open`:
          - Set `self._circuit_open = True`
          - Set `self._circuit_opened_at = datetime.now(timezone.utc)`
          - Set `self._circuit_half_open = False`
          - Raise `CircuitBreakerOpenError(...)` (existing behavior, now with timestamp)
        - If circuit is half-open and the probe fails:
          - Re-open circuit: set `self._circuit_half_open = False`, reset `self._circuit_opened_at = datetime.now(timezone.utc)` (restart recovery timer)
          - Raise `CircuitBreakerOpenError("Half-open probe failed. Circuit re-opened.")`

    - On successful call (after response received):
      - Reset `self._consecutive_failures = 0`
      - If `self._circuit_half_open`:
        - Set `self._circuit_open = False`
        - Set `self._circuit_half_open = False`
        - Set `self._circuit_opened_at = None`
        - Log: `"Circuit breaker closed — half-open probe succeeded"`

    **Retry with exponential backoff (TEACH-05, refine existing):**
    - Existing retry loop in `_call_api()` already implements `delay = backoff_base * (2 ** attempt)`. Verify it's correct.
    - Ensure 429 responses are retried (existing behavior — `_is_retryable` checks `status_code == 429`)
    - Ensure 400/401/403/404 are NOT retried (existing behavior — `_is_retryable` checks `HTTP_NON_RETRYABLE`)
    - Ensure 5xx responses ARE retried (catch-all for Exception with status_code not in HTTP_NON_RETRYABLE)

    Update `distill/teacher_errors.py` if needed — no new error classes required; existing `BudgetExceededError` and `CircuitBreakerOpenError` are sufficient.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/GeniusCogntiveSystem/GNUS-NEO-SWARM/gnus-poc && python -c "
import json, tempfile
from pathlib import Path
from unittest.mock import MagicMock, patch
import yaml

# Test 1: Budget state persistence
with tempfile.TemporaryDirectory() as td:
    root = Path(td)
    config_dir = root / 'config'
    config_dir.mkdir()
    artifacts = root / 'artifacts'
    artifacts.mkdir()

    config = {
        'endpoints': {'litellm': {'url': 'http://127.0.0.1:4000', 'apiType': 'openai'}},
        'models': {'deepseek-v4-fast': {'endpoint': 'litellm', 'model_id': 'deepseek-v4-fast'}},
        'teacher': {
            'level1': 'deepseek-v4-fast', 'confidence_threshold': 0.7,
            'max_retries': 3, 'backoff_base_seconds': 2.0, 'budget_cap_usd': 5.0,
            'circuit_breaker_failure_threshold': 5, 'circuit_breaker_recovery_timeout': 60
        },
        'teacher_benchmark': {'coding': {'deepseek-v4-fast': 0.8}},
        'pipeline': {'specialists': ['code']},
    }
    (config_dir / 'pipeline.yaml').write_text(yaml.dump(config))

    # Mock env + backends to construct TeacherClient
    import os; os.environ['LITELLM_API_KEY'] = 'sk-test'
    with patch('distill.teacher.OpenAIBackend'), patch('distill.teacher.AnthropicBackend'):
        from distill.teacher import TeacherClient
        client = TeacherClient(config_path=config_dir / 'pipeline.yaml', project_root=root)
        
        # Verify budget state path
        assert client._budget_state_path == root / 'artifacts' / '.budget_state.json'
        assert client._total_cost == 0.0  # fresh state
        
        # Simulate cost accumulation and save
        client._total_cost = 1.50
        client._save_budget_state()
        assert client._budget_state_path.exists()
        data = json.loads(client._budget_state_path.read_text())
        assert data['cumulative_cost_usd'] == 1.50
        
        # Test reload
        client2 = TeacherClient(config_path=config_dir / 'pipeline.yaml', project_root=root)
        assert client2._total_cost == 1.50  # loaded from disk

    print('Budget persistence tests passed')

# Test 2: Half-open circuit breaker logic
from distill.teacher_errors import CircuitBreakerOpenError
from datetime import datetime, timezone, timedelta

# Simulate circuit state machine
class MockClock:
    def __init__(self):
        self._now = datetime(2026, 1, 1, tzinfo=timezone.utc)
    def now(self):
        return self._now
    def advance(self, seconds):
        self._now += timedelta(seconds=seconds)

clock = MockClock()
circuit_open = False
circuit_opened_at = None
circuit_half_open = False
failure_threshold = 2  # open after 2 failures
recovery_timeout = 10   # 10 seconds

failures = 0
# Simulate 2 failures
failures += 1  # failure 1
failures += 1  # failure 2
if failures >= failure_threshold:
    circuit_open = True
    circuit_opened_at = clock.now()
assert circuit_open == True

# Immediately after opening, should NOT allow probe
elapsed = (clock.now() - circuit_opened_at).total_seconds()
assert elapsed < recovery_timeout

# Advance past recovery timeout
clock.advance(15)
elapsed = (clock.now() - circuit_opened_at).total_seconds()
assert elapsed >= recovery_timeout
circuit_half_open = True  # allow probe

# Probe succeeds
circuit_open = False
circuit_half_open = False
circuit_opened_at = None
failures = 0
assert circuit_open == False

# Probe fails
circuit_open = True
circuit_half_open = False
circuit_opened_at = clock.now()
failures = 1
assert circuit_open == True

print('Circuit breaker half-open logic verified')
"</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `TeacherClient.__init__` loads cumulative spend from `artifacts/.budget_state.json` if it exists; fresh start has `_total_cost = 0.0`
    - Behavior: After each successful API call, `_total_cost` increases AND `.budget_state.json` is updated with new cumulative total
    - Behavior: When `_total_cost >= _budget_cap`, `_check_budget()` raises `BudgetExceededError` — applied across runs via disk persistence
    - Behavior: `client.reset_budget()` sets `_total_cost = 0.0` and writes updated state file
    - Behavior: Circuit breaker opens after `circuit_breaker_failure_threshold` consecutive failures, recording `_circuit_opened_at` timestamp
    - Behavior: After `circuit_breaker_recovery_timeout` seconds, circuit transitions to half-open (allows one probe request)
    - Behavior: Half-open probe that succeeds fully closes the circuit (resets all state); failing probe re-opens the circuit with fresh recovery timer
    - Behavior: 429 and 5xx errors are retried with exponential backoff; 400/401/403/404 are not retried
    - Source: `grep -c "budget_state" distill/teacher.py` returns `>= 2`
    - Source: `grep -c "_circuit_opened_at" distill/teacher.py` returns `>= 1`
    - Source: `grep -c "half.open\|half_open\|circuit_half_open" distill/teacher.py` returns `>= 1`
    - Source: `grep -c "_load_budget_state" distill/teacher.py` returns `>= 1`
    - Source: `grep -c "_save_budget_state" distill/teacher.py` returns `>= 1`
  </acceptance_criteria>
  <done>Budget state persists to disk across runs; circuit breaker has half-open recovery with configurable failure threshold and recovery timeout</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TeacherClient → Budget state file | Cumulative spend read/written to JSON on disk |
| TeacherClient → Multi-teacher cascade | Model selection from untrusted benchmark table in YAML |
| Circuit breaker state → API calls | Circuit state controls whether API calls proceed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | .budget_state.json | accept | Budget state file can be manually edited; the budget is a soft limit for cost control, not a security boundary. Document this in comments. |
| T-04-02 | Denial of Service | Circuit breaker | mitigate | Half-open recovery prevents permanent circuit lockout; recovery timeout is configurable |
| T-04-03 | Elevation of Privilege | TeacherCascade | mitigate | Benchmark table keys validated against models registry; unknown model names in benchmark raise TeacherConfigError |
| T-04-04 | Information Disclosure | Cascade logging | mitigate | Cascade attempts logged but response content is truncated to prevent PII leakage in logs |
| T-04-SC | Tampering | pip installs | mitigate | No new pip packages added in this plan |
</threat_model>

<verification>
- `python -c "from distill.cascade import TeacherCascade, CascadeResult; print('cascade import OK')"` exits 0
- `python -c "from distill.teacher import TeacherClient; print('teacher import OK')"` exits 0
- Budget state file is created at `artifacts/.budget_state.json` after first successful API call
- Circuit breaker half-open probe succeeds after recovery timeout, re-opens on failure
- Level 1 always runs; Level 2 only when confidence < threshold
</verification>

<success_criteria>
1. `TeacherCascade.execute()` runs Level 1 (Fast) always; escalates to benchmark-routed Level 2 only when logprobs confidence < threshold
2. Budget state persists to `artifacts/.budget_state.json` and is loaded on TeacherClient startup
3. `reset_budget()` zeros cumulative spend and updates persisted state
4. Circuit breaker opens after N consecutive failures, enters half-open after recovery timeout, closes on successful probe
5. Retry with exponential backoff recovers from 429 and 5xx; 4xx (except 429) are not retried
6. TeacherClient has both `generate(model_name, messages)` (direct) and `generate_with_cascade(messages, domain)` (cascade) entry points
</success_criteria>

<output>
Create `.planning/phases/01-pipeline-hardening/01-04-SUMMARY.md` when done
</output>
