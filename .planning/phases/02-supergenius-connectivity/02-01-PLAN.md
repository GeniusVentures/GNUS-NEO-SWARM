---
phase: 02-supergenius-connectivity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/network/sg_client/SuperGeniusClient.hpp
  - src/network/sg_client/SuperGeniusClient.cpp
  - src/network/sg_client/SGChannelManager.hpp
  - src/network/sg_client/SGJobSubmitter.hpp
  - src/network/sg_client/SGResultCollector.hpp
  - src/network/sg_client/SGMessageAuthenticator.hpp
  - src/genius_node.cpp
  - src/api/GeniusAPIServer.hpp
autonomous: true
requirements:
  - SG-01
  - SG-03
---

<objective>
Create the SuperGeniusClient component skeleton (6 files in src/network/sg_client/) and add CLI flags for SuperGenius endpoint configuration. This establishes the new component structure and operator-facing interface.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-supergenius-connectivity/02-CONTEXT.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/research/ARCHITECTURE.md
@src/core/sgprocessing/SGProcessingBridge.hpp
@src/genius_node.cpp
@src/api/GeniusAPIServer.hpp
@src/common/Error.hpp
@src/security/NodeIdentity.hpp
</context>

<tasks>

<task type="auto">
  <name>task 1: create SuperGeniusClient component skeleton</name>
  <files>
    src/network/sg_client/SuperGeniusClient.hpp
    src/network/sg_client/SuperGeniusClient.cpp
  </files>
  <action>
Create the SuperGeniusClient public interface in src/network/sg_client/:
- SuperGeniusClient.hpp — class with Config struct, Initialize(identity), Connect(), SubmitJob(json), Disconnect()
- SuperGeniusClient.cpp — stub implementations returning Error::NotImplemented
- Config struct with: endpoint_ (default "localhost:50051"), channel_timeout_ (30s), result_timeout_ (300s)
- Initialize() accepts const NodeIdentity& for message signing
- Connect() returns outcome::result<void>
- SubmitJob(json) returns outcome::result<vector<uint8_t>> matching the SGProcessingBridge return type
- Follow existing patterns: outcome::result<T>, namespace sgns::neoswarm::network, C++17, Allman braces
- Use PIMPL idiom (struct Impl) to hide heavy gRPC includes from headers
</action>
  <verify><automated>Files exist: src/network/sg_client/SuperGeniusClient.hpp, src/network/sg_client/SuperGeniusClient.cpp</automated></verify>
  <acceptance_criteria>
  - SuperGeniusClient class declared with Config, Initialize, Connect, SubmitJob, Disconnect
  - All methods return outcome::result (stub returns NotImplemented)
  - PIMPL pattern used to hide gRPC dependencies
  - Namespace: sgns::neoswarm::network
  </acceptance_criteria>
</task>

<task type="auto">
  <name>task 2: create internal component header stubs</name>
  <files>
    src/network/sg_client/SGChannelManager.hpp
    src/network/sg_client/SGJobSubmitter.hpp
    src/network/sg_client/SGResultCollector.hpp
    src/network/sg_client/SGMessageAuthenticator.hpp
  </files>
  <action>
Create stub header files for the four internal sub-components:
- SGChannelManager.hpp — gRPC channel lifecycle (create, keepalive, reconnect)
- SGJobSubmitter.hpp — publish signed Task messages to grid channel
- SGResultCollector.hpp — subscribe to result channel, timeout-bounded collection
- SGMessageAuthenticator.hpp — sign payloads with NodeIdentity

All stub — declare classes with minimal interfaces matching the architecture diagram. Implementation files created in later tasks.
</action>
  <verify><automated>All 4 header files exist in src/network/sg_client/</automated></verify>
  <acceptance_criteria>
  - Four stub header files created with class declarations
  - Namespace: sgns::neoswarm::network
  - Include guards follow NEOSWARM_NETWORK_SG_* pattern
  </acceptance_criteria>
</task>

<task type="auto">
  <name>task 3: add SG endpoint and TLS CLI flags</name>
  <files>
    src/genius_node.cpp
    src/api/GeniusAPIServer.hpp
  </files>
  <action>
Add three new CLI flags to src/genius_node.cpp:
- --sg-endpoint <host:port> (default: "localhost:50051")
- --sg-tls-ca <path> (TLS CA certificate bundle)
- --sg-tls-cert <path> (TLS client certificate)

Update Args struct with: sg_endpoint_, sg_tls_ca_, sg_tls_cert_ fields.
Update ParseArgs() to handle the new flags.
Update PrintHelp() to document new flags.
Add matching fields to GeniusAPIServer::Config: sg_endpoint_, sg_tls_ca_, sg_tls_cert_.
Wire Args fields into GeniusAPIServer::Config during initialization in main().
</action>
  <verify><automated>grep -c "sg-endpoint" src/genius_node.cpp && grep -c "sg_tls_ca_" src/api/GeniusAPIServer.hpp</automated></verify>
  <acceptance_criteria>
  - neo-swarm --sg-endpoint localhost:50051 parses correctly
  - --sg-tls-ca and --sg-tls-cert flags available (optional)
  - GeniusAPIServer::Config has sg_endpoint_, sg_tls_ca_, sg_tls_cert_ fields
  - PrintHelp shows all new flags
  </acceptance_criteria>
</task>

</tasks>
