---
phase: 01-foundation-bug-fixes
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - gnus-poc/config/pipeline.yaml
  - gnus-poc/config/specialists/medical.yaml
  - gnus-poc/config/specialists/code.yaml
  - gnus-poc/config/specialists/qa_technical.yaml
  - gnus-poc/config/specialists/encyclopedic.yaml
  - gnus-poc/config/specialists/patents.yaml
  - gnus-poc/config/experiments/example_experiment.yaml
  - gnus-poc/tests/__init__.py
  - gnus-poc/tests/conftest.py
  - gnus-poc/tests/test_chat_template.py
  - .gitignore
  - gnus-poc/requirements.txt
  - gnus-poc/pyproject.toml
autonomous: true
requirements:
  - FOUND-04

must_haves:
  truths:
    - "YAML config hierarchy loads correctly: pipeline.yaml → per-specialist overrides → experiment overrides"
    - "${DEEPSEEK_API_KEY} is resolved from environment at config load time using python-dotenv + os.path.expandvars()"
    - "The .gitignore covers all new directories (artifacts/, data/synthetic/, __pycache__/, *.pyc, .venv/, .env)"
    - "A pytest smoke test validates that the chat template fix produces correct Qwen3 tokens (not Qwen2.5 <|im_start|> tokens)"
    - "pytest conftest.py provides reusable fixtures: temp directories, mock model paths"
  artifacts:
    - path: "gnus-poc/config/pipeline.yaml"
      provides: "Global YAML config with env-var interpolation for API keys"
      contains: "${DEEPSEEK_API_KEY}"
    - path: "gnus-poc/config/specialists/medical.yaml"
      provides: "Per-specialist config with niche-specific overrides"
      contains: "lora_rank"
    - path: "gnus-poc/tests/conftest.py"
      provides: "Shared pytest fixtures for all future tests"
      min_lines: 20
    - path: "gnus-poc/tests/test_chat_template.py"
      provides: "Smoke test proving chat template fix works"
      min_lines: 20
  key_links:
    - from: "gnus-poc/tests/test_chat_template.py"
      to: "gnus-poc/training/tokenizer_utils.py"
      via: "import"
      pattern: "from training\\.tokenizer_utils import"
    - from: ".gitignore"
      to: "gnus-poc/artifacts/"
      via: "gitignore pattern"
      pattern: "^artifacts/"
    - from: "gnus-poc/config/pipeline.yaml"
      to: "gnus-poc/.env"
      via: "os.path.expandvars configuration loader (future Phase 3)"
      pattern: "\\$\\{DEEPSEEK_API_KEY\\}"
---

<objective>
Establish the YAML configuration hierarchy, pytest test skeleton, .gitignore rules, and Python project metadata. Per FOUND-04, create the three-tier YAML config system (global → specialist → experiment override) with `${ENV_VAR}` interpolation. Add a minimal pytest skeleton with a smoke test validating the Phase 1 chat template fix. Update .gitignore to cover all new directories and Python artifacts. Create requirements.txt and pyproject.toml for dependency management.

Purpose: The config hierarchy is consumed by every subsequent phase (Phase 2 API client, Phase 3 training config, Phase 4 eval, Phase 5 distillation, Phase 6 orchestration). The test skeleton gives every future plan a place to add tests. The .gitignore prevents accidental commits of large binary artifacts.

Output: 7 YAML config files, 3 test files, updated .gitignore, requirements.txt, pyproject.toml.
</objective>

<execution_context>
@/Users/khurley/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/khurley/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 1 success criteria #4: YAML config hierarchy)
@.planning/REQUIREMENTS.md (FOUND-04: YAML config hierarchy with env-var interpolation)
@.planning/research/ARCHITECTURE.md (Section 6: Configuration Management — full YAML examples for pipeline.yaml, specialists/medical.yaml, experiments/example_experiment.yaml; Section 9.6: gitignore anti-pattern)
@.planning/research/SUMMARY.md (Stack: PyYAML, python-dotenv already installed; Phase 1 deliverables list)
@.planning/phases/01-CONTEXT.md (Implementation Decisions: secrets management, .env file, testing, .gitignore)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create YAML config hierarchy with env-var interpolation</name>
  <files>
    gnus-poc/config/pipeline.yaml
    gnus-poc/config/specialists/medical.yaml
    gnus-poc/config/specialists/code.yaml
    gnus-poc/config/specialists/qa_technical.yaml
    gnus-poc/config/specialists/encyclopedic.yaml
    gnus-poc/config/specialists/patents.yaml
    gnus-poc/config/experiments/example_experiment.yaml
  </files>
  <action>
    Create the three-tier YAML config hierarchy per ARCHITECTURE.md Section 6. Use the exact YAML structure from ARCHITECTURE.md with adjustments for Phase 1 scope:

    **gnus-poc/config/pipeline.yaml** — Global configuration:
    ```yaml
    # GNUS-POC Pipeline Configuration — Global defaults
    # Override per specialist in config/specialists/<niche>.yaml
    # Override per experiment in config/experiments/<exp>.yaml
    # Secrets via ${ENV_VAR} resolved at load time from .env

    pipeline:
      specialists: ["medical", "qa_technical", "code", "encyclopedic", "patents"]

    paths:
      data_dir: "data/specialists"
      models_dir: "models/specialists_mlx"
      artifacts_dir: "artifacts"

    teacher:
      model: "deepseek-v4-pro"
      api_key_env: "DEEPSEEK_API_KEY"          # Resolved from .env at load time
      base_url: "https://api.deepseek.com"
      max_tokens: 4096
      temperature: 0.7
      max_retries: 3
      backoff_base_seconds: 2.0
      budget_cap_usd: 50.0                     # Hard dollar limit — stops all API calls when exceeded

    training:
      base_model: "mlx-community/Qwen3-30B-A3B-Instruct-2507-bf16"
      batch_size: 4
      iterations: 1000
      learning_rate: 1e-5
      lora_rank: 16
      lora_dropout: 0.05
      lora_scale: 20.0
      num_layers: 16
      val_batches: 25
      steps_per_report: 50
      steps_per_eval: 200
      save_every: 200
      fine_tune_type: "lora"
      optimizer: "adamw"
      seed: 42

    evaluation:
      test_split: "test"
      metrics: ["accuracy", "perplexity", "latency"]

    fp4_export:
      target_bits: 4
      block_size: 64
    ```

    **gnus-poc/config/specialists/medical.yaml** — Per-specialist config:
    - Use ARCHITECTURE.md Section 6 medical.yaml structure
    - Include: name, base_model, niche_sources (PubMed Abstracts, PubMed Central, NIH ExPorter), system_prompt, synthetic_prompts list, training overrides (lora_rank: 16, iterations: 1000)

    **gnus-poc/config/specialists/code.yaml**:
    - base_model: "mlx-community/Qwen3-Coder-30B-A3B-Instruct-bf16" (different from default!)
    - niche_sources: GitHub, StackOverflow, Programming Language Documentation
    - system_prompt: "You are a programming and code documentation specialist."
    - training: lora_rank: 16, iterations: 1000

    **gnus-poc/config/specialists/qa_technical.yaml**:
    - base_model: default (30B-A3B-Instruct)
    - niche_sources: StackExchange, Technical Documentation, Q&A Forums
    - system_prompt: "You are a technical Q&A specialist."
    - training: lora_rank: 16, iterations: 1000

    **gnus-poc/config/specialists/encyclopedic.yaml**:
    - base_model: default
    - niche_sources: Wikipedia, Encyclopedic References
    - system_prompt: "You are an encyclopedic knowledge specialist."
    - training: lora_rank: 16, iterations: 1000

    **gnus-poc/config/specialists/patents.yaml**:
    - base_model: default
    - niche_sources: USPTO, Patent Applications, Technical Innovation Disclosures
    - system_prompt: "You are a patent and technical innovation specialist."
    - training: lora_rank: 16, iterations: 1000

    **gnus-poc/config/experiments/example_experiment.yaml**:
    - Use ARCHITECTURE.md Section 6 example_experiment.yaml exactly
    - id: "exp_lora_rank_32", description, overrides for medical and code (lora_rank: 32)

    Also create gnus-poc/config/__init__.py with docstring: `"""GNUS-POC configuration — YAML hierarchy loader."""` (the actual loader is deferred to Phase 3; this is a placeholder).

    Every YAML file MUST use `.yaml` extension (not .yml) for consistency. Every file starts with a comment describing its role in the hierarchy.

    Env-var interpolation: `${DEEPSEEK_API_KEY}` appears as a literal string in pipeline.yaml. The actual resolution (python-dotenv + os.path.expandvars()) will be implemented by the config loader in Phase 3. For Phase 1, this is a schema contract — the placeholder string documents the expected env var name.
  </action>
  <verify>
    <automated>python3 -c "
import yaml, sys, os

files = [
    'gnus-poc/config/pipeline.yaml',
    'gnus-poc/config/specialists/medical.yaml',
    'gnus-poc/config/specialists/code.yaml',
    'gnus-poc/config/specialists/qa_technical.yaml',
    'gnus-poc/config/specialists/encyclopedic.yaml',
    'gnus-poc/config/specialists/patents.yaml',
    'gnus-poc/config/experiments/example_experiment.yaml',
]
errors = []
for f in files:
    try:
        with open(f) as fh:
            data = yaml.safe_load(fh)
        assert data is not None, f'{f}: empty or invalid YAML'
        print(f'  OK: {f} — valid YAML ({len(str(data))} bytes)')
    except Exception as e:
        errors.append(f'{f}: {e}')

# Verify pipeline.yaml has env-var placeholders
with open('gnus-poc/config/pipeline.yaml') as f:
    pipeline = f.read()
assert '\${DEEPSEEK_API_KEY}' in pipeline, 'MISSING: \${DEEPSEEK_API_KEY} placeholder'
assert 'budget_cap_usd' in pipeline, 'MISSING: budget_cap_usd'
print('  OK: pipeline.yaml contains \${DEEPSEEK_API_KEY} and budget_cap_usd')

# Verify code.yaml has different base model
with open('gnus-poc/config/specialists/code.yaml') as f:
    code = f.read()
assert 'Coder' in code, 'MISSING: code specialist should use Qwen3-Coder model'

# Verify experiment override has lora_rank: 32
with open('gnus-poc/config/experiments/example_experiment.yaml') as f:
    exp = f.read()
assert 'lora_rank: 32' in exp or 'lora_rank: 32' in yaml.dump(yaml.safe_load(exp)), 'MISSING: experiment override lora_rank=32'

if errors:
    for e in errors:
        print(f'ERROR: {e}')
    sys.exit(1)
print('All 7 YAML configs valid and complete')
"</automated>
  </verify>
  <done>All 7 YAML config files exist, pass yaml.safe_load validation; pipeline.yaml contains ${DEEPSEEK_API_KEY} placeholder and budget_cap_usd; code.yaml uses Qwen3-Coder base model; example_experiment.yaml overrides lora_rank to 32.</done>
</task>

<task type="auto">
  <name>Task 2: Create pytest skeleton and chat template smoke test</name>
  <files>
    gnus-poc/tests/__init__.py
    gnus-poc/tests/conftest.py
    gnus-poc/tests/test_chat_template.py
  </files>
  <action>
    Create gnus-poc/tests/ as the test root. Three files per CONTEXT.md "Testing" section:

    **gnus-poc/tests/__init__.py**:
    ```python
    """GNUS-POC test suite — unit, integration, and smoke tests."""
    ```

    **gnus-poc/tests/conftest.py** — Shared pytest fixtures:
    - `tmp_project_root(tmp_path)`: Create a temporary directory mimicking the gnus-poc structure with empty data/ and models/ directories. Returns the Path.
    - `mock_model_path()`: Return a string path to a lightweight HuggingFace model suitable for testing (use "Qwen/Qwen2.5-0.5B-Instruct" — a small 0.5B model that has a valid chat template and is fast to download once). Mark with `@pytest.fixture(scope="session")` so it's loaded once.
    - `sample_messages()`: Return a list of message dicts: `[{"role": "system", "content": "You are a test assistant."}, {"role": "user", "content": "Hello, world!"}]`
    - `real_tokenizer(mock_model_path)`: Load the actual HuggingFace tokenizer from mock_model_path using `AutoTokenizer.from_pretrained()`. Session-scoped. Returns the tokenizer object.

    **gnus-poc/tests/test_chat_template.py** — Smoke test for FOUND-01 fix:
    Test 1 — `test_tokenizer_utils_imports`:
    - Import `load_tokenizer` and `format_chat` from `training.tokenizer_utils`
    - Assert both are callable

    Test 2 — `test_format_chat_produces_chat_template`:
    - Use `real_tokenizer` fixture (already loaded)
    - Call `format_chat(sample_messages, real_tokenizer)`
    - Assert the result is a non-empty string
    - Assert the result does NOT contain `<|im_start|>` (this is the Qwen2.5 format that was causing the bug)
    - Assert the result DOES contain the user message text "Hello, world!"

    Test 3 — `test_format_chat_matches_tokenizer_template`:
    - Use `real_tokenizer` fixture
    - Call `format_chat(sample_messages, real_tokenizer)` → result A
    - Call `real_tokenizer.apply_chat_template(sample_messages, tokenize=False, add_generation_prompt=False)` → result B
    - Assert result A == result B (tokenizer_utils.format_chat should be a thin wrapper matching the tokenizer's native output)

    Test 4 — `test_load_tokenizer_returns_valid_tokenizer`:
    - Call `load_tokenizer(mock_model_path)`
    - Assert the returned object has `apply_chat_template` method
    - Assert calling `apply_chat_template` on it returns a string

    All tests should use `import sys; sys.path.insert(0, str(Path(__file__).resolve().parent.parent))` (or equivalent) so that `from training.tokenizer_utils import ...` resolves. The conftest.py should handle this once.

    **IMPORTANT:** The tokenizer_utils smoke test requires downloading a model from HuggingFace (~1GB for Qwen2.5-0.5B). The first `pytest` run will download this. Use `@pytest.mark.slow` for tests that require network/model download, and document that `pytest -m "not slow"` skips them for quick iteration.
  </action>
  <verify>
    <automated>python3 -c "
import ast, sys, os

# Verify conftest.py has required fixtures
with open('gnus-poc/tests/conftest.py') as f:
    conftest = f.read()
fixtures = ['tmp_project_root', 'mock_model_path', 'sample_messages', 'real_tokenizer']
for fix in fixtures:
    assert f'def {fix}' in conftest, f'MISSING fixture: {fix}'
print('OK: All 4 conftest fixtures present')

# Verify test_chat_template.py has required tests
with open('gnus-poc/tests/test_chat_template.py') as f:
    tests = f.read()
test_funcs = ['test_tokenizer_utils_imports', 'test_format_chat_produces_chat_template', 'test_format_chat_matches_tokenizer_template', 'test_load_tokenizer_returns_valid_tokenizer']
for tf in test_funcs:
    assert f'def {tf}' in tests, f'MISSING test: {tf}'
print('OK: All 4 test functions present')

# Verify no <|im_start|> in test expectations (should assert ABSENCE of old format)
assert '<|im_start|>' not in tests or 'not in' in tests, 'Test should assert ABSENCE of <|im_start|> format'
print('OK: test_chat_template.py validates absence of Qwen2.5 format')

# Verify pytest can collect tests (syntax check only — no model download)
result = os.system('cd gnus-poc && python3 -m pytest tests/test_chat_template.py --collect-only -q 2>&1 | head -20')
print('Pytest collection check completed')
"</automated>
    <human-check>After plan execution, run `cd gnus-poc && python3 -m pytest tests/test_chat_template.py -v` and confirm all 4 tests pass. The first run will download Qwen2.5-0.5B (~1GB). Expected output: 4 passed.</human-check>
  </verify>
  <done>tests/__init__.py, conftest.py with 4 fixtures, test_chat_template.py with 4 test functions; all tests discoverable by pytest; tests validate format_chat matches tokenizer native output and excludes <|im_start|>.</done>
</task>

<task type="auto">
  <name>Task 3: Update .gitignore, create requirements.txt and pyproject.toml</name>
  <files>
    .gitignore
    gnus-poc/requirements.txt
    gnus-poc/pyproject.toml
  </files>
  <action>
    **A) Update .gitignore** at repo root — append the following patterns to the existing file (preserve existing entries for .DS_Store, .env, gnus-poc/data/analysis, etc.):

    ```
    # Python artifacts
    __pycache__/
    *.pyc
    *.pyo
    *.egg-info/
    .venv/
    dist/
    build/
    *.egg

    # gnus-poc artifacts (new directories from Phase 1)
    gnus-poc/artifacts/
    gnus-poc/data/synthetic/
    gnus-poc/tests/__pycache__/

    # IDE
    .idea/
    .vscode/
    *.swp
    *.swo
    ```

    The existing entries `.DS_Store`, `.env`, `gnus-poc/data/analysis`, `gnus-poc/data/specialists`, `gnus-poc/models/specialists_mlx` MUST be preserved. Only append new patterns, do not modify existing ones.

    **B) Create gnus-poc/requirements.txt** — list all Python dependencies for the gnus-poc pipeline. Include:
    - All existing dependencies (from RESEARCH.md Stack section): mlx, mlx-lm, mlx-metal, torch, transformers, datasets, openai, scikit-learn, numpy, rich, tenacity, python-dotenv, PyYAML, nltk, tqdm
    - New Phase 1+ dependencies: mlflow, lm-eval, pytest, pytest-mock, pytest-cov, pytest-timeout
    - Comment each group with a header: `# MLX training`, `# Data processing`, `# API & config`, `# Evaluation & tracking`, `# Testing`
    - Pin versions if known from RESEARCH.md (e.g., `mlflow==3.12.0`, `lm_eval[hf]==0.4.12`)

    **C) Create gnus-poc/pyproject.toml** — per ARCHITECTURE.md Section 8.1 ("Flat modules, not a pip package — pyproject.toml for deps/tooling only"):
    ```toml
    [build-system]
    requires = ["setuptools>=68.0"]
    build-backend = "setuptools.build_meta"

    [project]
    name = "gnus-poc"
    version = "0.1.0"
    description = "GNUS-POC ELM Training & Distillation Pipeline (Milestone v1.1)"
    requires-python = ">=3.10"
    readme = "README.md"
    license = {text = "MIT"}
    authors = [
        {name = "GNUS.AI"}
    ]

    [tool.pytest.ini_options]
    testpaths = ["tests"]
    markers = [
        "slow: tests that require model download or GPU (deselect with '-m \"not slow\"')",
        "integration: tests that require external services or GPU",
    ]
    pythonpath = ["."]

    [tool.ruff]
    line-length = 120
    target-version = "py310"
    ```

    Note: The `[tool.pytest.ini_options]` section is critical — it sets `pythonpath = ["."]` so that `from training.tokenizer_utils import ...` resolves in tests without manual sys.path manipulation.

    After creating both files, verify with `pip install -r gnus-poc/requirements.txt --dry-run` (if pip supports it) or just verify file syntax.
  </action>
  <verify>
    <automated>
# 1. Verify .gitignore has all required patterns
python3 -c "
import sys
with open('.gitignore') as f:
    gi = f.read()
patterns = ['__pycache__/', '*.pyc', 'artifacts/', 'data/synthetic/', '.venv/', 'dist/', 'build/']
missing = [p for p in patterns if p not in gi]
if missing:
    print(f'ERROR: Missing .gitignore patterns: {missing}')
    sys.exit(1)
print('OK: .gitignore has all required patterns')

# 2. Verify .gitignore preserved existing entries (no regressions)
existing = ['.DS_Store', '.env', 'gnus-poc/data/analysis', 'gnus-poc/data/specialists', 'gnus-poc/models/specialists_mlx']
lost = [e for e in existing if e not in gi]
if lost:
    print(f'ERROR: Existing .gitignore entries lost: {lost}')
    sys.exit(1)
print('OK: All existing .gitignore entries preserved')
"

# 3. Verify requirements.txt has key packages
python3 -c "
with open('gnus-poc/requirements.txt') as f:
    reqs = f.read()
deps = ['mlx', 'mlx-lm', 'transformers', 'datasets', 'openai', 'PyYAML', 'python-dotenv', 'pytest', 'mlflow']
missing = [d for d in deps if d not in reqs]
if missing:
    print(f'WARNING: Packages not found in requirements.txt: {missing}')
else:
    print('OK: requirements.txt has all key packages')
"

# 4. Verify pyproject.toml is valid TOML and has pytest config
python3 -c "
import sys
try:
    import tomllib  # Python 3.11+
except ImportError:
    import tomli as tomllib  # fallback
with open('gnus-poc/pyproject.toml', 'rb') as f:
    config = tomllib.load(f)
assert 'project' in config, 'Missing [project] section'
assert 'tool' in config and 'pytest' in config['tool'], 'Missing [tool.pytest] section'
assert config['tool']['pytest']['ini_options']['testpaths'] == ['tests'], 'testpaths not set to tests'
print('OK: pyproject.toml valid, pytest configured')
print(f'  testpaths: {config[\"tool\"][\"pytest\"][\"ini_options\"][\"testpaths\"]}')
"</automated>
  </verify>
  <done>.gitignore updated with 12+ new patterns while preserving all 5 existing entries; requirements.txt lists all deps with comments; pyproject.toml valid TOML with pytest configuration and pythonpath set.</done>
</task>

</tasks>

<verification>
<automated>
# End-to-end config + test verification
python3 -c "
import yaml, sys
from pathlib import Path

# 1. Verify all YAML files parse
config_files = list(Path('gnus-poc/config').rglob('*.yaml'))
for cf in config_files:
    with open(cf) as f:
        data = yaml.safe_load(f)
    print(f'  OK: {cf} — valid YAML')

# 2. Verify pytest can discover tests
import subprocess
result = subprocess.run(
    ['python3', '-m', 'pytest', 'gnus-poc/tests/', '--collect-only', '-q'],
    capture_output=True, text=True, cwd='gnus-poc'
)
if result.returncode != 0:
    print(f'Pytest collection failed: {result.stderr[:500]}')
else:
    test_count = result.stdout.strip().split('\n')[-1] if result.stdout.strip() else 'unknown'
    print(f'  OK: pytest discovered tests: {test_count}')

# 3. Verify .gitignore covers new dirs
with open('.gitignore') as f:
    gi = f.read()
for d in ['artifacts/', 'data/synthetic/', '__pycache__/', '*.pyc']:
    assert d in gi, f'MISSING from .gitignore: {d}'

print('ALL PHASE 1 CONFIG VERIFICATIONS PASSED')
"
</automated>
</verification>

<success_criteria>
- [ ] 7 YAML config files created and valid (pipeline.yaml + 5 specialists + 1 experiment)
- [ ] pipeline.yaml contains `${DEEPSEEK_API_KEY}` placeholder and `budget_cap_usd`
- [ ] code.yaml uses Qwen3-Coder base model (different from default)
- [ ] tests/conftest.py has 4 shared fixtures (tmp_project_root, mock_model_path, sample_messages, real_tokenizer)
- [ ] tests/test_chat_template.py has 4 test functions validating tokenizer_utils
- [ ] All tests discoverable via `pytest --collect-only`
- [ ] .gitignore updated with Python, IDE, and new-directory patterns; existing entries preserved
- [ ] requirements.txt lists all dependencies with version pins and section comments
- [ ] pyproject.toml configures pytest with testpaths=["tests"] and pythonpath=["."]
</success_criteria>

<output>
Create `.planning/phases/01-foundation-bug-fixes/01-03-SUMMARY.md` when done
</output>
