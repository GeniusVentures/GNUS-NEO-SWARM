---
phase: 01-foundation-bug-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - gnus-poc/training/__init__.py
  - gnus-poc/training/train_specialists_mlx.py
  - gnus-poc/training/train_specialists.py
  - gnus-poc/data/scripts/__init__.py
  - gnus-poc/data/scripts/analyze_common_pile.py
  - gnus-poc/data/scripts/extract_source_niches.py
  - gnus-poc/data/scripts/prepare_datasets.py
  - gnus-poc/pipeline/__init__.py
  - gnus-poc/distill/__init__.py
  - gnus-poc/eval/__init__.py
  - gnus-poc/quantize/__init__.py
  - gnus-poc/config/specialists/.gitkeep
  - gnus-poc/config/experiments/.gitkeep
  - gnus-poc/artifacts/.gitkeep
autonomous: true
requirements:
  - FOUND-03

must_haves:
  truths:
    - "All existing scripts run from their new module locations without import errors, broken relative paths, or missing dependencies"
    - "All six new module directories exist with valid __init__.py files making them importable Python packages"
    - "Stale/duplicate files (train_specialists-old.py, duplicate train_specialists.py) are deleted"
    - "Relative paths in moved scripts resolve to absolute paths based on script location, not CWD"
  artifacts:
    - path: "gnus-poc/training/__init__.py"
      provides: "Package init for training module"
      min_lines: 1
    - path: "gnus-poc/training/train_specialists_mlx.py"
      provides: "Primary 30B-A3B LoRA training script (moved from models/)"
      min_lines: 200
    - path: "gnus-poc/data/scripts/__init__.py"
      provides: "Package init for data scripts module"
      min_lines: 1
    - path: "gnus-poc/data/scripts/prepare_datasets.py"
      provides: "Dataset preparation script (moved from data/)"
      min_lines: 200
    - path: "gnus-poc/pipeline/__init__.py"
      provides: "Package init for pipeline orchestrator (stub)"
      min_lines: 1
  key_links:
    - from: "gnus-poc/data/scripts/prepare_datasets.py"
      to: "gnus-poc/data/analysis/"
      via: "Path(__file__).resolve().parent.parent"
      pattern: "Path\\(__file__\\)\\.resolve\\(\\)\\.parent"
    - from: "gnus-poc/training/train_specialists_mlx.py"
      to: "gnus-poc/data/specialists/"
      via: "Path(__file__).resolve().parent.parent"
      pattern: "Path\\(__file__\\)\\.resolve\\(\\)\\.parent"
---

<objective>
Restructure gnus-poc from flat layout to organized module layout per ARCHITECTURE.md Phase A. Move all existing scripts to their new canonical locations, fix every relative path to use `Path(__file__).resolve().parent` so scripts work from any CWD. Delete stale/duplicate files. Per FOUND-03.

Purpose: Establish the directory tree that all subsequent phases (bug fixes, config, new modules) build upon. Without this, Plan 01-01 and 01-03 have nowhere to put their output.

Output:
- 6 new module directories with __init__.py (training, pipeline, distill, eval, quantize, data/scripts)
- 3 placeholder dirs (config/specialists, config/experiments, artifacts)
- 5 scripts moved to new locations with path fixes applied
- 2 files deleted (train_specialists-old.py, models/train_specialists.py duplicate)
</objective>

<execution_context>
@/Users/khurley/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/khurley/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/ARCHITECTURE.md (Section 2: Proposed Directory Tree; Section 5.1: Integration Points; Section 9.1: Anti-pattern — hardcoded paths)
@.planning/research/PITFALLS.md (Pitfall #11: non-reproducible training runs from hardcoded relative paths; Pitfall #17: no versioning for datasets or adapters)
@.planning/phases/01-CONTEXT.md (Directory restructure decision)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create directory tree and __init__.py files</name>
  <files>
    gnus-poc/training/__init__.py
    gnus-poc/data/scripts/__init__.py
    gnus-poc/pipeline/__init__.py
    gnus-poc/distill/__init__.py
    gnus-poc/eval/__init__.py
    gnus-poc/quantize/__init__.py
    gnus-poc/config/specialists/.gitkeep
    gnus-poc/config/experiments/.gitkeep
    gnus-poc/artifacts/.gitkeep
  </files>
  <action>
    Create the following directories (with intermediate parents as needed) under gnus-poc/:
    - training/
    - data/scripts/
    - pipeline/
    - distill/
    - eval/
    - quantize/
    - config/specialists/
    - config/experiments/
    - artifacts/

    In each of the six module directories (training, data/scripts, pipeline, distill, eval, quantize), create an __init__.py file containing only a docstring with the module name and purpose:
    - `"""GNUS-POC training module — LoRA fine-tuning for specialist models."""` (training)
    - `"""GNUS-POC data scripts — niche discovery, source extraction, dataset preparation."""` (data/scripts)
    - `"""GNUS-POC pipeline orchestration — stage runner and experiment tracker."""` (pipeline)
    - `"""GNUS-POC distillation — teacher API client and knowledge distillation."""` (distill)
    - `"""GNUS-POC evaluation — per-specialist evaluator and benchmarker."""` (eval)
    - `"""GNUS-POC quantization — FP4 binary export for C++ engine."""` (quantize)

    Create placeholder .gitkeep files in config/specialists/, config/experiments/, and artifacts/ (empty dirs won't be tracked by git otherwise). These directories are for future YAML configs and runtime artifacts — not yet populated in Phase 1.

    Do NOT create config/pipeline.yaml, tests/, or requirements.txt — those belong to Plan 01-03.
  </action>
  <verify>
    <automated>python3 -c "
import os
dirs = ['training','data/scripts','pipeline','distill','eval','quantize','config/specialists','config/experiments','artifacts']
for d in dirs:
    path = f'gnus-poc/{d}'
    assert os.path.isdir(path), f'Missing directory: {path}'
modules = ['training','data/scripts','pipeline','distill','eval','quantize']
for m in modules:
    path = f'gnus-poc/{m}/__init__.py'
    assert os.path.isfile(path), f'Missing __init__.py: {path}'
print('All directories and __init__.py files verified')
"</automated>
  </verify>
  <done>All 9 directories exist; all 6 module __init__.py files exist with correct docstrings; 3 .gitkeep files exist for empty dirs.</done>
</task>

<task type="auto">
  <name>Task 2: Move scripts and fix relative paths</name>
  <files>
    gnus-poc/training/train_specialists_mlx.py
    gnus-poc/training/train_specialists.py
    gnus-poc/data/scripts/analyze_common_pile.py
    gnus-poc/data/scripts/extract_source_niches.py
    gnus-poc/data/scripts/prepare_datasets.py
  </files>
  <action>
    MOVE the following scripts to their new locations (use git mv to preserve history):
    1. gnus-poc/models/train_specialists_mlx.py → gnus-poc/training/train_specialists_mlx.py
    2. gnus-poc/models/train_specialists.py → gnus-poc/training/train_specialists.py
    3. gnus-poc/data/analyze_common_pile.py → gnus-poc/data/scripts/analyze_common_pile.py
    4. gnus-poc/data/extract_source_niches.py → gnus-poc/data/scripts/extract_source_niches.py
    5. gnus-poc/data/prepare_datasets.py → gnus-poc/data/scripts/prepare_datasets.py

    Then FIX ALL relative paths in each moved script per Pitfall #11. The pattern: replace every bare relative path like `"data/specialists"`, `"data/analysis"`, `"models/specialists_mlx"` with absolute resolution using `Path(__file__).resolve().parent`.

    For scripts under gnus-poc/data/scripts/ (two levels deep from gnus-poc root), use `PROJECT_ROOT = Path(__file__).resolve().parent.parent.parent`. For scripts under gnus-poc/training/ (one level deep), use `PROJECT_ROOT = Path(__file__).resolve().parent.parent`.

    Specific path fixes per file:

    **gnus-poc/data/scripts/analyze_common_pile.py** (line 32):
    - Replace `OUTPUT_DIR = "data/analysis"` with:
      ```python
      from pathlib import Path
      PROJECT_ROOT = Path(__file__).resolve().parent.parent.parent
      OUTPUT_DIR = str(PROJECT_ROOT / "data" / "analysis")
      ```
    - Keep `os.makedirs(OUTPUT_DIR, exist_ok=True)` unchanged (line 33).

    **gnus-poc/data/scripts/extract_source_niches.py**: (inspect the actual paths; apply same PROJECT_ROOT pattern)

    **gnus-poc/data/scripts/prepare_datasets.py** (lines 19-20, 27):
    - Replace `OUTPUT_DIR = 'data/specialists'` with `OUTPUT_DIR = str(PROJECT_ROOT / 'data' / 'specialists')`
    - Replace hardcoded `'data/analysis/source_based_niches.json'` on line 27 with `str(PROJECT_ROOT / 'data' / 'analysis' / 'source_based_niches.json')`

    **gnus-poc/training/train_specialists_mlx.py** (lines 45-46):
    - Replace `DATA_DIR = "data/specialists"` with `DATA_DIR = str(PROJECT_ROOT / "data" / "specialists")`
    - Replace `OUTPUT_DIR = "models/specialists_mlx"` with `OUTPUT_DIR = str(PROJECT_ROOT / "models" / "specialists_mlx")`

    **gnus-poc/training/train_specialists.py** (if it has paths): Apply same PROJECT_ROOT pattern.

    Also implement versioned output directories per Pitfall #17 — a lightweight convention for Phase 1:
    - In `prepare_datasets.py`, append a date-based version to the output niche dir: `niche_dir = f"{OUTPUT_DIR}/{niche_name}_v{datetime.now().strftime('%Y%m%d%H%M')}"`
    - In `train_specialists_mlx.py`, write the dataset version hash (SHA256 of concatenated JSONL) into `training_metadata.json` under key `dataset_hash`.

    After all files are moved AND paths are fixed, verify with `git status` that the old files show as deleted and new files show as added.
  </action>
  <verify>
    <automated>python3 -c "
import ast, sys
from pathlib import Path

files = [
    'gnus-poc/training/train_specialists_mlx.py',
    'gnus-poc/data/scripts/analyze_common_pile.py',
    'gnus-poc/data/scripts/prepare_datasets.py',
]
for f in files:
    with open(f) as fh:
        tree = ast.parse(fh.read())
    # Check that no bare string assignments use relative paths like 'data/' or 'models/'
    for node in ast.walk(tree):
        if isinstance(node, ast.Assign):
            for target in node.targets:
                if isinstance(target, ast.Name) and 'DIR' in target.id.upper():
                    if isinstance(node.value, ast.Constant) and isinstance(node.value.value, str):
                        val = node.value.value
                        if val.startswith('data/') or val.startswith('models/'):
                            print(f'ERROR: Bare relative path in {f}: {target.id} = {val!r}')
                            sys.exit(1)
print('All paths verified — no bare relative paths remain')
"
# Also verify moved files exist and originals don't
test -f gnus-poc/training/train_specialists_mlx.py && echo "OK: train_specialists_mlx.py moved"
test ! -f gnus-poc/models/train_specialists_mlx.py && echo "OK: origin removed"
test -f gnus-poc/data/scripts/prepare_datasets.py && echo "OK: prepare_datasets.py moved"
test ! -f gnus-poc/data/prepare_datasets.py && echo "OK: origin removed"</automated>
  </verify>
  <done>All 5 scripts exist in new locations; old locations empty; all path references use PROJECT_ROOT resolution; versioned output convention implemented in prepare_datasets.py; dataset_hash field added to training metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Delete stale and duplicate files</name>
  <files>
    gnus-poc/models/train_specialists-old.py
  </files>
  <action>
    Delete the following files (use `git rm` to remove from tracking):
    1. gnus-poc/models/train_specialists-old.py — exact duplicate of the MLX training script, explicitly listed as "delete" in CONTEXT.md and ARCHITECTURE.md Section 5.1.

    After Task 2's git mv operations, the original `models/train_specialists.py` will already be moved to `training/train_specialists.py`. The `train_specialists-old.py` is the only file needing outright deletion.

    Verify: No `.py` files remain in gnus-poc/models/ (only the specialists_mlx/ artifact directory should exist there — trained safetensors are NOT deleted). The gnus-poc/data/ directory should contain only the scripts/ subdirectory and the analysis/ and specialists/ artifact directories (gitignored).
  </action>
  <verify>
    <automated># Verify stale file is deleted
test ! -f gnus-poc/models/train_specialists-old.py && echo "OK: stale file deleted"
# Verify no orphan .py files in models/
ls gnus-poc/models/*.py 2>&1 | grep -q "No such file" && echo "OK: no .py files remain in models/"
# Verify data/ structure is clean
test -d gnus-poc/data/scripts && echo "OK: data/scripts/ exists"
test -d gnus-poc/data/analysis && echo "OK: data/analysis/ preserved"</automated>
  </verify>
  <done>train_specialists-old.py deleted; no Python files remain in gnus-poc/models/; data/ directory has only scripts/, analysis/, specialists/ subdirectories.</done>
</task>

</tasks>

<verification>
<automated>
# End-to-end: verify every moved script can be parsed as valid Python
python3 -c "
import py_compile, sys
files = [
    'gnus-poc/training/train_specialists_mlx.py',
    'gnus-poc/training/train_specialists.py',
    'gnus-poc/data/scripts/analyze_common_pile.py',
    'gnus-poc/data/scripts/extract_source_niches.py',
    'gnus-poc/data/scripts/prepare_datasets.py',
]
errors = []
for f in files:
    try:
        py_compile.compile(f, doraise=True)
        print(f'  OK: {f}')
    except py_compile.PyCompileError as e:
        errors.append(f'{f}: {e}')
if errors:
    print('COMPILATION ERRORS:')
    for e in errors:
        print(f'  {e}')
    sys.exit(1)
print('All moved scripts compile cleanly')
"
</automated>
</verification>

<success_criteria>
- [ ] All 9 new directories exist under gnus-poc/
- [ ] All 6 module __init__.py files exist
- [ ] All 5 scripts moved to new locations via git mv
- [ ] All relative paths in moved scripts use Path(__file__).resolve() convention
- [ ] train_specialists-old.py deleted
- [ ] All moved scripts are syntactically valid Python (py_compile passes)
- [ ] Original model directory contains only specialists_mlx/ artifacts (no .py files)
- [ ] versioned output convention present in prepare_datasets.py
</success_criteria>

<output>
Create `.planning/phases/01-foundation-bug-fixes/01-02-SUMMARY.md` when done
</output>
