---
context: default
status: in_progress
last_updated: 2026-06-02T03:38:15Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Codex companion review must run on one branch at a time — parallel `git checkout` causes working directory conflicts. Run reviews sequentially with `--base develop --scope branch`.
- [ ] CONSTRAINT: Codex review without `--base develop --scope branch` only reviews uncommitted working tree changes (empty for clean branches). Always specify base and scope.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Parallel branch checkout for reviews | Running codex review on multiple branches simultaneously causes git checkout conflicts | blocking | Run reviews sequentially, one branch at a time |
| Review without --base flag | Codex reviews working tree diff by default, missing all committed changes | advisory | Always use `--base develop --scope branch` |

<current_state>
Splitting `feature/neo-swarm-cpp-engine` (56 C++ engine commits) into 5 PR branches based on develop. All 5 branches created, pushed, and draft PRs opened (#37-#41). Codex reviews started but interrupted — need to complete reviews sequentially and fix findings.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Merge develop into feature/neo-swarm-cpp-engine — Done (resolved .planning/, .gitignore, C++ file conflicts)
- Task 2: Create 5 PR branches via cherry-pick — Done (pr/foundation, pr/phase1-security, pr/phase2-supergenius, pr/phase3-6-hardening, pr/mnn-vulkan-libp2p)
- Task 3: Strip transient .planning/phases/ from PR branches — Done (PR2-4 cleaned)
- Task 4: Push all PR branches to origin — Done
- Task 5: Create draft PRs (#37-#41) — Done
- Task 6: Run Codex reviews on each PR — In progress (1 of 5 completed)

</completed_work>

<remaining_work>

- Task 6: Complete Codex reviews on remaining 4 PRs (pr/foundation, pr/phase2-supergenius, pr/phase3-6-hardening, pr/mnn-vulkan-libp2p)
- Task 7: Fix issues found by Codex reviews on each PR branch
- Task 8: Mark PRs as ready for review (remove draft status)

</remaining_work>

<decisions_made>

- Used cherry-pick with auto-conflict-resolution (checkout feature branch version) to create PR branches from develop
- Merged develop into feature branch first to minimize conflicts during cherry-pick
- Kept Phases 3-6 together in one PR (small, 6 commits)
- MNN Vulkan + libp2p as standalone PR (1 commit, distinct concern)
- Folded GSD planning docs into foundation PR
- All PRs target `develop` as base branch

</decisions_made>

<blockers>

- Codex review on pr/phase1-security completed with findings (2 P1 Python issues on develop, 4 P2 issues) — need to determine which are pre-existing vs. introduced by this PR
- Codex review on pr/foundation ran 5+ minutes without completing (117 files) — may need to scope review more tightly

</blockers>

## Required Reading (in order)

1. `.planning/INBOX-TRIAGE.md` — current inbox state and PR status
2. `.planning/ROADMAP.md` — phase dependency graph and critical path

## Infrastructure State

- 5 draft PRs open on GitHub: #37-#41
- All PR branches pushed to origin
- Currently on `pr/foundation` branch
- `feature/neo-swarm-cpp-engine` has develop merged in (commit 3e7c499)

<context>
Was splitting a large feature branch (56 commits, C++ engine work) into reviewable PR chunks by phase. All branches created and pushed. Codex bot reviews initiated but interrupted — need to run them one at a time with `--base develop --scope branch` flag, then fix findings on each PR branch before marking PRs ready.

The Codex companion review script found issues in the Phase 1 Security review — 2 P1s (pipeline stages marked complete without running, dataset path mismatch) and 4 P2s (config overrides, logprob forwarding, BLEU/ROUGE min-vs-max, perplexity evaluation). Some of these may be pre-existing on develop rather than introduced by the PR.
</context>

<next_action>
Run Codex reviews sequentially on each PR branch:
1. `git checkout pr/foundation && node codex-companion.mjs review --base develop --scope branch`
2. `git checkout pr/phase2-supergenius && ...`
3. `git checkout pr/phase3-6-hardening && ...`
4. `git checkout pr/mnn-vulkan-libp2p && ...`
5. Re-run pr/phase1-security review with correct flags

Then fix findings on each branch and push updates.
</next_action>
