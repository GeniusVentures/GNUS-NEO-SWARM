---
context: milestone
phase: v1.1
task: all
total_tasks: 7
status: complete
last_updated: 2026-05-27T20:15:00Z
---

# Continue Here — Milestone v1.1: ELM Training & Distillation Pipeline

## Current State

Milestone v1.1 is **complete as a POC** — 7 phases built, 23/23 requirements covered, 75/76 tests passing. PR #36 shipped to GitHub. Milestone-level review written (MILESTONE-REVIEWS.md).

The pipeline delivers: teacher API client → synthetic data gen → dedup → training configs → evaluation → distillation loss → orchestration skeleton → FP4 Ultra v0.2 binary export.

## Completed Work

- **Phase 1**: Fixed chat template mismatch (Qwen2.5 format on Qwen3 models), fixed skip-on-existing, directory restructure, YAML configs
- **Phase 2**: TeacherClient with $5 budget cap, retry/backoff, circuit breaker. SyntheticDataGenerator with quality filtering
- **Phase 3**: TrainingConfig dataclass, memory estimator, cross-niche MinHash deduplication
- **Phase 4**: SpecialistEvaluator (perplexity/BLEU/ROUGE/latency), Benchmarker, ExperimentTracker
- **Phase 5**: Logit-based distillation with temperature-scaled KL divergence, temperature sweep
- **Phase 6**: PipelineRunner DAG skeleton, CLI entry point (`orchestrate.py`)
- **Phase 7**: FP4 Ultra v0.2 spec exporter (64x64 macroblocks, FP4_AFFINE + T158_AFFINE), ManifestBuilder, round-trip tests

## 3 HIGH Integration Gaps (from milestone review)

| # | Gap | What's needed |
|---|-----|---------------|
| 1 | **TrainingConfig not wired** | `train_specialists_mlx.py` still uses old `OVERRIDES` dict. Wire `TrainingConfig.from_yaml()` into the training entry point. |
| 2 | **Distillation loss unintegrated** | `Distiller.compute_distillation_loss()` works but MLX-LM's `train_model()` doesn't support custom loss functions. Either find MLX-LM custom loss API or implement post-training fine-tuning step. |
| 3 | **PipelineRunner is skeleton** | `_run_stage()` prints commands instead of executing them. Add `subprocess.run()` with error handling per stage. |

## Required Reading

1. `.planning/phases/01-foundation-bug-fixes/MILESTONE-REVIEWS.md` — full milestone review with per-phase assessment and consensus concerns
2. `.planning/ROADMAP.md` — phase dependency chain and success criteria
3. `.planning/REQUIREMENTS.md` — 23 REQ-IDs with traceability
4. `gnus-poc/training/train_specialists_mlx.py` — the actual training entry point (still uses old OVERRIDES dict)
5. `docs/fp4_ultra_spec_v0_2_adaptive_ascii.md` — FP4 binary format spec for Phase 7 reference
6. `gnus-poc/.env` — API key location (gitignored, not committed)

## Key Decisions

- DeepSeek v4 pro API (MIT license, explicit distillation permission) — API key via `DEEPSEEK_API_KEY` env var
- FP4 Ultra v0.2 spec for binary export (64x64 macroblocks, 2048-byte payload, FP4/T158 modes)
- $5.00 budget cap on API calls with hard circuit breaker
- qLoRA default (4-bit base quantization) for 64GB Mac Studio safety
- Per-niche tokenizer caching (Qwen3-Coder has different template from Qwen3-Instruct)

## Infrastructure State

- `.env` exists at `gnus-poc/.env` with `DEEPSEEK_API_KEY` (gitignored, not committed)
- `.venv` at `gnus-poc/.venv` with all dependencies (MLX, openai, transformers, datasets, etc.)
- 5 trained specialists at `gnus-poc/models/specialists_mlx/` (pre-Phase-1, may have chat template issues)
- PR #36: https://github.com/GeniusVentures/GNUS-NEO-SWARM/pull/36 (pending review/merge)

## Pending Human Actions

- Review and merge PR #36
- Verify DEEPSEEK_API_KEY is valid at https://platform.deepseek.com/api_keys

## Next Action

```
/gsd-resume-work
```

Then address the 3 integration gaps — start with wiring `TrainingConfig` into `train_specialists_mlx.py` (highest impact, lowest risk).
