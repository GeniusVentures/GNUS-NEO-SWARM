---
status: resolved
trigger: "GNUS-NEO-SWARM fails to compile when built via the GeniusCognitiveSystem outer CMake project: mnn_inference_engine.cpp(22,10): error C1083: Cannot open include file: 'MNN/llm/llm.hpp': No such file or directory"
created: 2026-08-18
updated: 2026-08-19T01:30:00Z
---

## Symptoms

**Expected behavior:** `cmake --build build/Windows/Release --target neoswarm_core --config Release` (run from `W:\gnus\GeniusCognitiveSystem`) compiles `neoswarm_core` cleanly, now that CMake configure succeeds end-to-end (fixed earlier this session — see Context below).

**Actual behavior:** Compilation fails on the very first NEO-SWARM source file it touches:
```
W:\gnus\GeniusCognitiveSystem\GNUS-NEO-SWARM\src\core\engine\mnn_inference_engine.cpp(22,10): error C1083: Cannot open include file: 'MNN/llm/llm.hpp': No such file or directory
```

**Error messages:** Exact compiler output above. No other errors seen yet — build stops at this first compile unit.

**Timeline:** First real compile attempt against this outer build ever (GNUS-NEO-SWARM's own CMake configure had been broken by a separate, unrelated `get_third_party_dir()` thirdparty-path-resolution issue for the entire session until just now; that issue was fixed this session, which is what got us far enough to hit this new compile error for the first time).

**Reproduction:**
```
cd W:\gnus\GeniusCognitiveSystem
cmake --build build/Windows/Release --target neoswarm_core --config Release
```

## Context (carried over from originating session — treat as high-confidence prior findings, not fresh hypotheses to re-derive from scratch)

This session (a `/gsd-execute-phase 4 --ws neoswarm` run, now well past phase execution into ad-hoc CMake infra fixes) already:

1. Fixed GeniusCognitiveSystem's CMake configure by pointing `THIRDPARTY_DIR` at `W:\gnus\GeniusNetwork\thirdparty` and porting ~25 missing `find_package()` blocks (Vulkan, SuperGenius, GeniusSDK, MNN, Snappy, RocksDB, libp2p, ipfs-*, wallet-core, zkLLVM/crypto3, etc.) from `GeniusNetwork/GeniusSDK/cmake/CommonBuildParameters.cmake` into `GeniusCognitiveSystem/cmake/CommonBuildParameters.cmake`. Configure now succeeds.
2. During this same session's execution of neoswarm workstream **Phase 4, plan 04-03** (`.planning/workstreams/neoswarm/phases/04-sgprocessing-integration/04-03-SUMMARY.md`), the executor independently discovered and confirmed: the vendored MNN thirdparty build actually linked in this environment (`W:\gnus\GeniusNetwork\thirdparty\build\Windows\Release\MNN`) was built with `MNN_BUILD_LLM=OFF` — no `MNN/llm/` subdirectory exists under its installed `include/`, confirmed by direct `ls`. The *source* for MNN's LLM transformer engine does exist, at `W:\gnus\GeniusNetwork\thirdparty\MNN\transformers\llm\engine\include\llm\llm.hpp` — it's just never compiled into the actually-linked build output.
3. To work around this, plan 04-03 added a narrow, self-contained compile-time guard `SGPROC_HAS_MNN_LLM` in `W:\gnus\GeniusNetwork\SuperGenius\SGProcessingManager\src\processors\CMakeLists.txt` (auto-detects whether the linked MNN has LLM support) with exactly 2 `#ifdef SGPROC_HAS_MNN_LLM` sites (`ProcessingManager.hpp` include, `ProcessingManager.cpp` registration line) — when false, the `MNN_Llm` processor source file and its test target are simply excluded from the build rather than failing to compile.
4. **NEO-SWARM's own `mnn_inference_engine.hpp`/`.cpp` has no equivalent guard.** It unconditionally does `#include <MNN/llm/llm.hpp>` (line 22 of the .cpp, per this error) for `MNN::Transformer::Llm`, used by `MNNInferenceEngine::InferViaMnnLlm()`. This function was explicitly preserved *unchanged* by plan 04-04 (decision D-06 — `InferViaMnnLlm` and `InferViaSGProcessing` are the two supported inference paths, kept as-is) — plan 04-04 only deleted the *other*, hand-rolled `InferViaStandardInterpreter` path. Nobody had actually verified `InferViaMnnLlm`'s own `#include` resolves in this environment before now, because GNUS-NEO-SWARM's build was blocked by issue #1 for this entire session until moments ago.

**This is the same underlying environment fact (`MNN_BUILD_LLM=OFF` in the linked vendored MNN) surfacing a second time, in a second, unguarded call site.**

## Current Focus

reasoning_checkpoint:
  hypothesis: "The linked vendored MNN build was actually configured and compiled WITH MNN_BUILD_LLM=ON (llm.lib built successfully) — the prior session's Context-section claim of MNN_BUILD_LLM=OFF was WRONG. The true root cause is a header-install-path bug in vendored MNN's own CMakeLists.txt: the correct `DESTINATION include/MNN/llm` install rule only exists in the Apple-only branch (CMakeLists.txt:1041-1048, inside the `ELSE()` of the platform if/elseif/else at line 972/1005/1014); the `ELSEIF(NOT APPLE)` branch that actually runs on this Windows build (lines 1005-1013) has no LLM header install rule at all. The only thing that installs the LLM headers anywhere on non-Apple platforms is a separate, independent `INSTALL(DIRECTORY .../engine/include/ DESTINATION include FILES_MATCHING PATTERN *.hpp)` in transformers/llm/engine/CMakeLists.txt:76, which drops them at `include/llm/llm.hpp` (sibling of `include/MNN/`) instead of `include/MNN/llm/llm.hpp` (nested, which is what `#include <MNN/llm/llm.hpp>` requires)."
  confirming_evidence:
    - "MNN-build/CMakeCache.txt (the actual ExternalProject configure cache for the linked build) shows MNN_BUILD_LLM:BOOL=ON and MNN_BUILD_LLM_OMNI:BOOL=ON — contradicts prior session's OFF claim."
    - "MNN-stamp/Release/{MNN-configure,MNN-build,MNN-install,MNN-done} all present -- full ExternalProject pipeline completed without error."
    - "llm.dir/Release/llm.lib, llm_demo.exe, llm_bench.exe, quantize_llm.exe all exist in the build tree -- LLM engine compiled successfully."
    - "include/llm/llm.hpp EXISTS on disk (confirmed via find). include/MNN/llm/ does NOT exist. include/MNN/, include/audio/, include/cv/, include/llm/ are siblings under the same include root used by MNN::MNN's include dirs (matches SGProcessingManager's own MNN_INCLUDE_DIR/MNN/llm/llm.hpp check idiom)."
    - "cmake_install.cmake (generated) contains exactly one llm-related install line -- the buggy DESTINATION include one from engine/CMakeLists.txt:76. No DESTINATION include/MNN/llm line was generated, confirming the Apple-only branch (CMakeLists.txt:1041-1048) never fires on this platform."
  falsification_test: "If include/MNN/llm/llm.hpp existed on disk right now, this hypothesis would be false (and the compile error would have some other cause). It does not exist; include/llm/llm.hpp does. This is direct filesystem observation, not inference."
  fix_rationale: "Fix addresses the actual root cause (wrong install destination for an already-built header) rather than a symptom. No MNN rebuild is required -- llm.lib is already compiled; only the install step needs to place headers at the path the codebase's #include already expects. This also transparently unblocks SGProcessingManager's existing SGPROC_HAS_MNN_LLM EXISTS-based guard (same path check) without any further changes there, giving both workstreams real LLM capability instead of just compiling around its absence."
  blind_spots: "Have not verified llm.lib itself links cleanly against neoswarm_core/SGProcessors on Windows (only confirmed the header-install-path bug and that llm.lib exists) -- there could be a second, separate issue once the include resolves (e.g. missing OMNI transitive deps like sentencepiece/opencv/audio at link time, since MNN_BUILD_LLM_OMNI=ON pulls those in). Have not checked whether MNN_BUILD_OPENCV/MNN_BUILD_AUDIO headers (cv/, audio/) have the identical Apple-only install-branch bug (likely yes, by the same code structure, but out of scope for this specific header error). Have not yet run the actual fix + rebuild to observe a clean compile."

hypothesis: (superseded by reasoning_checkpoint above) See corrected root cause.

test: Confirmed via direct filesystem/build-log inspection (see confirming_evidence) -- no build/compile experiment was needed, this was fully resolvable by reading the generated CMake install script and comparing to the built artifacts.

expecting: N/A -- hypothesis confirmed via direct evidence, not a predictive experiment.

next_action: (Stage 1 complete — see Stage 2 section below for current work)

## Evidence

- timestamp: 2026-08-18T22:58:00Z
  finding: "Reproduced independently: `cmake --build build/Windows/Release --target neoswarm_core --config Release` from `W:\\gnus\\GeniusCognitiveSystem` fails with `error C1083: Cannot open include file: 'MNN/llm/llm.hpp': No such file or directory` at `mnn_inference_engine.cpp(22,10)`. Prior to this, `neoswarm_common` target built successfully (so the failure is specific to this file/include, not a broader configure/link problem)."
- timestamp: 2026-08-18T22:58:00Z
  finding: "Confirmed via `ls`: `W:\\gnus\\GeniusNetwork\\thirdparty\\build\\Windows\\Release\\MNN\\include\\MNN\\` has no `llm/` subdirectory. `W:\\gnus\\GeniusNetwork\\thirdparty\\MNN\\transformers\\llm\\engine\\include\\llm\\llm.hpp` (source, not installed/built) does exist — same finding plan 04-03's executor already made independently for SGProcessingManager's own MNN_Llm processor."
- timestamp: 2026-08-19T00:15:00Z
  checked: "W:\\gnus\\GeniusNetwork\\thirdparty\\build\\Windows\\Release\\MNN\\src\\MNN-build\\CMakeCache.txt (the ACTUAL configure cache for the ExternalProject build that produces the linked MNN, as distinct from an unrelated stray W:\\gnus\\GeniusNetwork\\thirdparty\\MNN\\.build\\ directory which also exists but is not what's linked)"
  found: "MNN_BUILD_LLM:BOOL=ON and MNN_BUILD_LLM_OMNI:BOOL=ON. All ExternalProject stamps (MNN-configure, MNN-build, MNN-install, MNN-done) present and completed same day. llm.dir/Release/llm.lib, llm_demo.exe, llm_bench.exe, quantize_llm.exe all present in the build tree."
  implication: "The prior session's core claim — 'vendored MNN was built with MNN_BUILD_LLM=OFF' — is WRONG. MNN_BUILD_LLM was ON and the LLM engine compiled successfully. The header-resolution failure has a different cause: an install-path bug, not a missing feature."
- timestamp: 2026-08-19T00:20:00Z
  checked: "W:\\gnus\\GeniusNetwork\\thirdparty\\build\\Windows\\Release\\MNN\\src\\MNN-build\\cmake_install.cmake (generated install script) and W:\\gnus\\GeniusNetwork\\thirdparty\\MNN\\CMakeLists.txt lines 970-1069 (platform-branched header install logic) and W:\\gnus\\GeniusNetwork\\thirdparty\\MNN\\transformers\\llm\\engine\\CMakeLists.txt:76"
  found: "MNN CMakeLists.txt has an `IF(Android) ELSEIF(NOT APPLE) ELSE(=Apple)` branch (line 972/1005/1014) for header installs. The correct `INSTALL(FILES ${MNN_LLM_HDRS} DESTINATION include/MNN/llm)` rule exists ONLY inside the Apple-only ELSE() branch (lines 1041-1048). The ELSEIF(NOT APPLE) branch that actually executes on this Windows build (lines 1005-1013) installs only MNN_PUB_HDRS/MNN_EXPR_PUB_HDRS — no llm/cv/audio header install at all. The ONLY llm header install that fires on non-Apple platforms is transformers/llm/engine/CMakeLists.txt:76: `INSTALL(DIRECTORY ${CMAKE_CURRENT_LIST_DIR}/include/ DESTINATION include FILES_MATCHING PATTERN *.hpp)`, which (because the source path ends in a trailing slash) copies the engine's include/ *contents* — including its llm/ subfolder — directly under `<prefix>/include/`, producing `<prefix>/include/llm/llm.hpp` (sibling of `include/MNN/`) instead of `<prefix>/include/MNN/llm/llm.hpp` (nested). Generated cmake_install.cmake confirms only this one llm-related install line exists — the Apple-branch rule was never even generated on this platform."
  implication: "Root cause is an upstream/vendored MNN CMakeLists.txt gap: LLM (and likely cv/audio/diffusion) header installs are only wired up for Apple. On Windows/Linux the llm engine's own sub-CMakeLists installs to the wrong destination. Fix does not require rebuilding MNN — only re-running its install step after correcting one DESTINATION path (or adding the missing non-Apple install rule)."
- timestamp: 2026-08-19T00:25:00Z
  checked: "find W:\\gnus\\GeniusNetwork\\thirdparty\\build\\Windows\\Release\\MNN\\include for llm.hpp; git submodule status for thirdparty/MNN; existing patch conventions for other vendored thirdparty libs (protobuf has checked-in .patch files)"
  found: "W:\\gnus\\GeniusNetwork\\thirdparty\\build\\Windows\\Release\\MNN\\include\\llm\\llm.hpp exists on disk right now (confirms the install-path theory directly — the header is not missing, it's misplaced). thirdparty/MNN is a clean git submodule pinned at f84312b (branch MNN_Ultra), not currently patched. This repo already patches other vendored thirdparty libs directly (e.g. thirdparty/protobuf/*.patch), so patching MNN's CMakeLists.txt would be consistent with existing project convention, not a novel practice."
  implication: "A one-line DESTINATION fix in transformers/llm/engine/CMakeLists.txt:76 (or adding the missing non-Apple branch entries in the main CMakeLists.txt) followed by re-running MNN's install step (not a full rebuild -- llm.lib is already compiled) would make <MNN/llm/llm.hpp> resolve correctly for BOTH neoswarm_core (this bug) and SGProcessingManager's SGPROC_HAS_MNN_LLM guard (04-03) simultaneously, since both check the same MNN_INCLUDE_DIR/MNN/llm/llm.hpp path."

## Eliminated

- hypothesis: "Vendored MNN was built with MNN_BUILD_LLM=OFF, so the LLM engine was never compiled and a real header genuinely doesn't exist anywhere in the build output (prior session's Context section claim)."
  evidence: "MNN-build/CMakeCache.txt shows MNN_BUILD_LLM:BOOL=ON; llm.lib, llm_demo.exe, llm_bench.exe, quantize_llm.exe all exist in the build tree; the header itself exists on disk at include/llm/llm.hpp (just not at the include/MNN/llm/llm.hpp path the #include expects). The build succeeded end-to-end with LLM support -- only the header install destination is wrong."
  timestamp: 2026-08-19T00:15:00Z

## Resolution (Stage 1 — header include path)

root_cause: "CONFIRMED, and CORRECTED once more after user-provided evidence. Vendored MNN was built with MNN_BUILD_LLM=ON and llm.lib compiled successfully on BOTH Windows and macOS (user independently confirmed via their own Mac build: llm.hpp lands at include/llm/llm.hpp there too, not include/MNN/llm/llm.hpp). This means the earlier theory that MNN's Apple-only CMakeLists branch installs to the 'correct' nested path was ALSO wrong / never actually exercised as described — empirically, on every platform tested, the real installed path is include/llm/llm.hpp (sibling of include/MNN/), not include/MNN/llm/llm.hpp. Cross-checked against SGProcessingManager's own 04-03-SUMMARY.md (Deviation 1): that plan's RESEARCH.md/PLAN.md claim that '<MNN/llm/llm.hpp> is confirmed already used and building successfully in NEO-SWARM' was ITSELF false/unverified — the 04-03 executor found the same wrong-path problem independently and the SGPROC_HAS_MNN_LLM guard correctly excluded processing_processor_mnn_llm.cpp from the build for this reason (never actually compiled against a real MNN.lib in this environment prior to today). So this is the same wrong assumption debunked twice, in two unguarded/guarded call sites, and NEITHER a build-flag issue NOR a vendored-MNN-CMakeLists bug -- it's simply that NEO-SWARM's and SGProcessingManager's own code assumed a header path that was never real anywhere."
fix: "APPLIED. Corrected the wrong include path in the actual consuming code (not vendored MNN) — 5 files: (1) GNUS-NEO-SWARM/src/core/engine/mnn_inference_engine.cpp:22 `#include <MNN/llm/llm.hpp>` -> `#include <llm/llm.hpp>`; (2) GNUS-NEO-SWARM/test/benchmark/bench_mnn_llm.cpp:32 same fix; (3) SuperGenius/SGProcessingManager/src/processors/CMakeLists.txt:29 `EXISTS \"${MNN_INCLUDE_DIR}/MNN/llm/llm.hpp\"` -> `EXISTS \"${MNN_INCLUDE_DIR}/llm/llm.hpp\"` plus corrected stale MNN_BUILD_LLM=OFF comments/messages (this flips SGPROC_HAS_MNN_LLM to TRUE, so processing_processor_mnn_llm.cpp now actually compiles for the first time); (4) SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_llm.cpp:14 same include fix; (5) SuperGenius/SGProcessingManager/include/processors/processing_processor_mnn_llm.hpp:9 doc comment reference fix. No vendored MNN patch, no rebuild, no compile-time guard needed in NEO-SWARM."
verification: "Compile-time verified: mnn_inference_engine.cpp and processing_processor_mnn_llm.cpp both now compile past the include line during a full `cmake --build build/Windows/Release --parallel 8 --config Release` (previously failed at C1083 before this fix). Stage 1 is fully resolved -- see Stage 2 below for new issues this full build surfaced, which are a DIFFERENT (link-stage) problem."
files_changed:
  - GNUS-NEO-SWARM/src/core/engine/mnn_inference_engine.cpp
  - GNUS-NEO-SWARM/test/benchmark/bench_mnn_llm.cpp
  - SuperGenius/SGProcessingManager/src/processors/CMakeLists.txt
  - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_llm.cpp
  - SuperGenius/SGProcessingManager/include/processors/processing_processor_mnn_llm.hpp

## Stage 2 — NEW investigation (link-stage failures, post Stage-1 fix)

A full outer build (`cmake --build build/Windows/Release --parallel 8 --config Release` from `W:\gnus\GeniusCognitiveSystem`) now compiles past Stage 1's error but fails at LINK stage. Several DISTINCT failure categories surfaced simultaneously; user confirmed (2026-08-19) to continue THIS session for the MNN-linkage issue only, and leave 3 unrelated pre-existing bugs for separate handling.

### In scope for this session

hypothesis: "One root cause manifesting as two symptoms: `find_package(MNN CONFIG REQUIRED)` (GeniusCognitiveSystem/cmake/CommonBuildParameters.cmake:166) only defines the namespaced imported target `MNN::MNN` (confirmed: MNNTargets.cmake:59 `add_library(MNN::MNN STATIC IMPORTED)`; MNNTargets-release.cmake:12 `IMPORTED_LOCATION_RELEASE \"${_IMPORT_PREFIX}/lib/MNN.lib\"` — the correct, fully-merged 143MB lib, confirmed via grep to contain the createLLM/destroy/response symbols). But GNUS-NEO-SWARM's own CMakeLists.txt files reference the BARE, non-namespaced `MNN` instead: test/benchmark/CMakeLists.txt:7-8 (`target_link_libraries(bench_mnn_llm PRIVATE MNN ...)`, unconditional, no if(TARGET MNN) guard) and src/core/CMakeLists.txt:34-35 (`if(TARGET MNN) target_link_libraries(neoswarm_core PUBLIC MNN) endif()`). No literal `add_library(MNN ALIAS ...)` or bare `MNN` target/alias was found anywhere in GeniusCognitiveSystem's own CMake files (confirmed via grep), so it's unconfirmed whether a bare `MNN` target exists at all in the full dependency graph (possibly from SuperGenius/GeniusSDK cmake modules, not yet searched) or whether these references simply silently fail/fall through."

symptom_1: "test/benchmark/bench_mnn_llm.vcxproj: LNK2019 unresolved external symbol for MNN::Transformer::Llm::createLLM/destroy/response, despite these symbols being CONFIRMED present in MNN.lib."
symptom_2: "neo-swarm.vcxproj, Genius-MOS-ELM-FFI.vcxproj, test_elm.vcxproj: LNK2005 'already defined in MNN.lib(...)' duplicate-symbol errors for OpCommonUtils/Session/Utils objects — self-referential duplicate, suggesting MNN.lib (or two different resolutions of it) gets passed to the linker twice for these targets."

test: CONFIRMED via direct filesystem/CMakeCache/vcxproj/PATH-env inspection (see Evidence below) -- no build experiment needed to find root cause, only to verify the fix.

expecting: N/A -- confirmed via direct evidence, not a predictive experiment.

next_action: (Stage 2 investigation complete -- fix applied, proceeding to build verification of bench_mnn_llm/neoswarm_core/neo-swarm/test_elm/Genius-MOS-ELM-FFI.)

reasoning_checkpoint:
  hypothesis: "GNUS-NEO-SWARM's OWN cmake/CommonBuildParameters.cmake:221-222 unconditionally defines a redundant, non-namespaced `MNN` IMPORTED target via `find_library(MNN_LIBRARY MNN PATHS \"${THIRDPARTY_BUILD_DIR}/MNN/lib\" REQUIRED)` (no NO_DEFAULT_PATH). Because NO_DEFAULT_PATH is missing, find_library searches the environment PATH (and other system default locations) BEFORE the given PATHS argument. This machine has `C:\\MNNTools` (a personal/unrelated official-MNN-tools install containing its own standalone MNN.lib, 93MB, no LLM engine) on PATH, so find_library resolves MNN_LIBRARY to `C:/MNNTools/MNN.lib` instead of the project's correct vendored `${THIRDPARTY_BUILD_DIR}/MNN/lib/MNN.lib` (143MB, LLM-enabled, the one MNN::MNN correctly points to). GNUS-NEO-SWARM's own consumers (test/benchmark/CMakeLists.txt:7-8 unconditional; src/core/CMakeLists.txt:34-35 via if(TARGET MNN)) link this wrong bare `MNN` target. bench_mnn_llm links ONLY bare MNN -> only gets the LLM-less stray lib -> LNK2019 (symptom_1). neoswarm_core links bare MNN PUBLIC, propagating the stray lib into neo-swarm.exe/test_elm.exe/Genius-MOS-ELM-FFI -- which ALSO separately receive the CORRECT MNN::MNN transitively through SuperGenius's exported CMake config (sgns::genius_node / GeniusSDK chain, real imported targets with usage requirements) -> both the stray and correct MNN.lib (two different files, same base filename) land on the same link line -> LNK2005 'already defined in MNN.lib(...)' (symptom_2), which LOOKS self-referential only because MSVC's LNK2005 message shows just the base filename, not the full path. ONE root cause explains both symptoms."
  confirming_evidence:
    - "Exhaustive grep of SuperGenius and GeniusSDK's own CMakeLists.txt/*.cmake files (W:\\gnus\\GeniusNetwork\\SuperGenius, W:\\gnus\\GeniusNetwork\\GeniusSDK) found ZERO bare `MNN` target/alias/find_library(MNN...) definitions anywhere -- every reference in those trees uses the namespaced `MNN::MNN` consistently (SuperGenius/src/processing/CMakeLists.txt:56, SuperGenius/example/mnn_chunkprocess/CMakeLists.txt:44, SGProcessingManager/src/processors/CMakeLists.txt:114). This eliminates the 'bare MNN defined elsewhere in the dependency graph' branch of the original Stage-2 hypothesis."
    - "MNNConfig.cmake / MNNTargets.cmake (the vendored MNN's own exported CMake package, at thirdparty/build/Windows/Release/MNN/lib/cmake/MNN/) only ever does `add_library(MNN::MNN STATIC IMPORTED)` -- confirms find_package(MNN CONFIG) can NEVER produce a bare `MNN` target on its own."
    - "GNUS-NEO-SWARM/cmake/CommonBuildParameters.cmake:221-222 DOES define a bare `MNN` target independently: `find_library(MNN_LIBRARY MNN PATHS \"${THIRDPARTY_BUILD_DIR}/MNN/lib\" REQUIRED)` + `add_library(MNN UNKNOWN IMPORTED)` -- this is the ONLY bare-MNN definition found anywhere in the full dependency graph, and it lives entirely within GNUS-NEO-SWARM's own file."
    - "W:\\gnus\\GeniusCognitiveSystem\\build\\Windows\\Release\\CMakeCache.txt: `MNN_LIBRARY:FILEPATH=C:/MNNTools/MNN.lib` -- direct confirmation of the actual (wrong) resolved value."
    - "`C:\\MNNTools\\` exists on disk and literally contains `MNN.lib` (93MB) alongside MNNConvert.exe/GetMNNInfo.exe/etc. -- an official MNN prebuilt-tools distribution, unrelated to this project's vendored thirdparty MNN (143MB, confirmed in Stage 2's earlier evidence to contain the LLM symbols)."
    - "`[Environment]::GetEnvironmentVariable('PATH','User')` on this machine includes `C:\\MNNTools` -- directly confirms the PATH-pollution mechanism, not just a stale-cache theory."
    - "Actual generated vcxproj AdditionalDependencies (read directly, not inferred): bench_mnn_llm.vcxproj lists ONLY `C:\\MNNTools\\MNN.lib` (no correct MNN.lib at all) -> matches LNK2019 exactly (missing LLM symbols). neo-swarm.vcxproj and test_elm.vcxproj BOTH list `C:\\MNNTools\\MNN.lib` AND `W:\\gnus\\GeniusNetwork\\thirdparty\\build\\Windows\\Release\\MNN\\lib\\MNN.lib` side by side -> matches LNK2005 exactly (two distinct files, both literally named MNN.lib, both defining OpCommonUtils/Session/Utils). Genius-MOS-ELM-FFI.vcxproj (Debug config, same underlying CMake logic) shows the identical pattern."
    - "GeniusCognitiveSystem/cmake/CommonBuildParameters.cmake:498 `add_subdirectory(GNUS-NEO-SWARM ...)` happens strictly AFTER its own `find_package(MNN CONFIG REQUIRED)` (~line 166) -- confirmed via line-number ordering in the same file -- so MNN::MNN reliably exists by the time GNUS-NEO-SWARM's own CommonBuildParameters.cmake runs when nested. GNUS-NEO-SWARM's own pre-existing `_MISSING_DEPS` stub-creation loop (lines 398-425) already relies on this exact ordering guarantee for its own `if(NOT TARGET MNN::MNN) add_library(MNN::MNN INTERFACE IMPORTED) endif()` stub-skip logic -- i.e. the codebase already assumes/depends on MNN::MNN being available by that point when nested, confirming this is a safe, precedented assumption to build the fix on."
    - "GNUS-NEO-SWARM/cmake/CommonBuildParameters.cmake:213-215 already has an established, working precedent for exactly this fix shape: `if(TARGET libsecp256k1::secp256k1 AND NOT TARGET secp256k1) add_library(secp256k1 ALIAS libsecp256k1::secp256k1) endif()` -- reusing a namespaced target under a bare-name ALIAS when available, immediately above the MNN block in the same file."
  falsification_test: "If a bare `MNN` target/alias/find_library had been found anywhere in SuperGenius or GeniusSDK's own cmake files, this hypothesis would be false (would need to look for a DIFFERENT conflicting definition in the shared/vendored graph, which would require a checkpoint per the task's stop condition). None was found -- confirmed via exhaustive grep. If C:\\MNNTools had NOT been on PATH, the find_library call would have fallen through to its PATHS argument and found the correct file, and this hypothesis would be false (would need to explain the wrong resolution some other way) -- it register on PATH, this is direct environment observation, not inference."
  fix_rationale: "Fix targets the actual root cause -- the redundant, unguarded, PATH-vulnerable `find_library`+`add_library(MNN UNKNOWN IMPORTED)` block that GNUS-NEO-SWARM defines independently of (and now conflicting with) the correct, already-available MNN::MNN -- rather than a symptom. Mirrors the file's own existing libsecp256k1 alias precedent: when nested inside GeniusCognitiveSystem (MNN::MNN already exists), reuse it via `add_library(MNN ALIAS MNN::MNN)` instead of re-resolving independently; this requires ZERO changes to the two consuming files (src/core/CMakeLists.txt, test/benchmark/CMakeLists.txt), since they already reference bare `MNN` and the alias makes that reference correct. Standalone (non-nested) builds are preserved: when MNN::MNN does NOT exist, falls back to the original find_library-based path, now with NO_DEFAULT_PATH added (fixing the same PATH-vulnerability at its root for that mode too, since it's the literal missing flag that caused this whole bug)."
  blind_spots: "Have not yet re-run the actual build to observe the LNK2019/LNK2005 errors disappear (verification in progress next). Have not exhaustively verified there are no OTHER machines/CI environments where this fix's `if(TARGET MNN::MNN)` branch selection could behave differently (e.g. a CI environment where GNUS-NEO-SWARM is built nested but for some reason MNN::MNN isn't yet defined at add_subdirectory time) -- but this exact ordering/assumption is already relied upon by the pre-existing _MISSING_DEPS stub logic in the same file, so this is a pre-existing, accepted assumption, not a new risk introduced by this fix. Have not investigated why/when C:\\MNNTools ended up on this developer's PATH (irrelevant to the code fix, but worth the user knowing it's a personal environment artifact, not a project requirement)."

### Explicitly OUT of scope for this session (separate, unrelated pre-existing bugs — user confirmed to leave alone, handle separately)

- `test_network`/`test_sg_connectivity`/`test_sg_client`/`test_elm`: LNK2019 unresolved `init_unit_test_suite` from `libboost_unit_test_framework...(unit_test_main.obj)` — Boost.Test library/macro mismatch, unrelated to MNN.
- `GNUS-NEO-SWARM/test/common/test_types.cpp:165`: `error C7555` — designated initializers require C++20, violates this project's C++17-only rule (CLAUDE.md).
- `GNUS-NEO-SWARM/src/storage/gcs_global_db.cpp:17`: `error C1083: Cannot open include file: 'GeniusSDK.hpp'` — missing include path for neoswarm_storage target, unrelated to MNN.

## Resolution (Stage 2)

root_cause: "CONFIRMED. GNUS-NEO-SWARM/cmake/CommonBuildParameters.cmake:220-227 independently defined its own bare, non-namespaced `MNN` IMPORTED target via `find_library(MNN_LIBRARY MNN PATHS \"${THIRDPARTY_BUILD_DIR}/MNN/lib\" REQUIRED)` -- missing NO_DEFAULT_PATH, so find_library searched the environment PATH (and other system defaults) BEFORE the given PATHS. This developer machine has `C:\\MNNTools` (an unrelated personal install of MNN's official prebuilt tools, containing its own standalone 93MB MNN.lib with no LLM engine) on PATH, so MNN_LIBRARY resolved to `C:/MNNTools/MNN.lib` instead of the project's correct vendored `${THIRDPARTY_BUILD_DIR}/MNN/lib/MNN.lib` (143MB, LLM-enabled -- the same file MNN::MNN correctly points to via the outer GeniusCognitiveSystem project's find_package(MNN CONFIG REQUIRED)). No bare `MNN` target/alias/find_library exists anywhere in SuperGenius or GeniusSDK's own cmake files (exhaustively grepped and eliminated) -- this was entirely self-contained within GNUS-NEO-SWARM's own file. Single root cause explained BOTH symptoms: bench_mnn_llm links ONLY bare MNN (test/benchmark/CMakeLists.txt, unconditional) -> only ever got the LLM-less stray lib -> LNK2019 unresolved createLLM/destroy/response. neoswarm_core links bare MNN PUBLIC (src/core/CMakeLists.txt, via if(TARGET MNN)) -> propagated the stray lib into neo-swarm.exe/test_elm.exe/Genius-MOS-ELM-FFI, which ALSO separately received the CORRECT MNN::MNN transitively through SuperGenius's exported CMake package config (sgns::genius_node/GeniusSDK chain, real CMake imported targets with usage requirements) -> both the stray and correct MNN.lib (two different physical files, identical base filename) landed on the same link line simultaneously -> LNK2005 'already defined in MNN.lib(...)', which only looked self-referential because MSVC's LNK2005 diagnostic shows just the base filename, not the full path. Confirmed via direct evidence at every step: CMakeCache.txt showing the stray resolved path, PATH env var directly confirmed to contain C:\\MNNTools, and the actual generated .vcxproj AdditionalDependencies lists read directly (not inferred) showing exactly this dual-linkage pattern for neo-swarm/test_elm/Genius-MOS-ELM-FFI and single-wrong-linkage for bench_mnn_llm."
fix: "APPLIED, entirely within GNUS-NEO-SWARM/cmake/CommonBuildParameters.cmake (no shared/vendored files touched, no consumer CMakeLists.txt files touched). Replaced the unconditional find_library+add_library(MNN UNKNOWN IMPORTED) block (lines 220-227) with an if(TARGET MNN::MNN)/else() branch: (1) when nested inside GeniusCognitiveSystem, MNN::MNN already exists (correctly resolved) by the time add_subdirectory(GNUS-NEO-SWARM) runs -- confirmed via line-number ordering in the outer file (find_package(MNN CONFIG REQUIRED) at ~line 166, add_subdirectory(GNUS-NEO-SWARM) at line 498) and via the codebase's own pre-existing _MISSING_DEPS stub-skip logic already relying on this exact ordering guarantee -- so the fix reuses it via `add_library(MNN ALIAS MNN::MNN)`, mirroring the file's own existing libsecp256k1::secp256k1 -> secp256k1 alias precedent two blocks above; (2) when NOT nested (true standalone GNUS-NEO-SWARM build), falls back to the original find_library-based resolution, now with NO_DEFAULT_PATH added to fix the same PATH-vulnerability at its root for that mode too. This required zero changes to test/benchmark/CMakeLists.txt or src/core/CMakeLists.txt -- both already reference bare `MNN`, and the alias makes that reference resolve correctly."
verification: "Self-verified: rebuilt all four affected targets individually via `cmake --build build/Windows/Release --target <t> --config Release` from W:\\gnus\\GeniusCognitiveSystem. CMake reconfigure log showed 'MNN: reusing MNN::MNN (nested build)' confirming the new branch executed. bench_mnn_llm.exe: builds and links cleanly (LNK2019 gone). neo-swarm.exe: builds and links cleanly (LNK2005 gone). Genius-MOS-ELM-FFI.dll: builds and links cleanly (LNK2005 gone). test_elm.exe: MNN-related LNK2005 gone; only the pre-existing, explicitly out-of-scope Boost.Test LNK2019 (init_unit_test_suite) remains. Directly re-inspected the regenerated .vcxproj AdditionalDependencies for all three (neo-swarm, bench_mnn_llm, test_elm): `C:\\MNNTools\\MNN.lib` no longer appears anywhere; each lists the correct `W:\\gnus\\GeniusNetwork\\thirdparty\\build\\Windows\\Release\\MNN\\lib\\MNN.lib` exactly once.

HUMAN-VERIFIED (2026-08-18): User independently re-ran a full `cmake --build build/Windows/Release --parallel 8 --config Release` (not just the 4 targeted targets) and confirmed zero MNN-related LNK2019/LNK2005 errors remain anywhere in the full build log -- total error line count dropped from 293 to 31. Remaining failures are exactly the pre-agreed out-of-scope set: Boost.Test `init_unit_test_suite` unresolved across 6 test targets, the C++20 designated-initializer violation in test_common_types.cpp, the missing GeniusSDK.hpp include in gcs_global_db.cpp, plus one not explicitly enumerated before but confirmed present in the very first (pre-fix) build log too: test_genius_elm_ffi.vcxproj has 5x `error C2491: definition of dllimport function not allowed` in genius_elm_chat_completions.cpp -- confirmed pre-existing, not introduced by this fix, and out of scope for this session. Debug session CONFIRMED RESOLVED end-to-end."
files_changed:
  - GNUS-NEO-SWARM/cmake/CommonBuildParameters.cmake
  - GNUS-NEO-SWARM/src/core/engine/mnn_inference_engine.cpp (Stage 1)
  - GNUS-NEO-SWARM/test/benchmark/bench_mnn_llm.cpp (Stage 1)
  - SuperGenius/SGProcessingManager/src/processors/CMakeLists.txt (Stage 1)
  - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_llm.cpp (Stage 1)
  - SuperGenius/SGProcessingManager/include/processors/processing_processor_mnn_llm.hpp (Stage 1)
