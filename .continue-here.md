# Continue Here — Phase 7 PR #105 Follow-up (WIP)

**Date:** 2026-07-24
**Branch:** `wip/phase7-pr105-followup` (based on `pr/phase7-wave2-role-domain-elms` @ f2e9b6c, rebased on origin/develop)
**PR:** https://github.com/GeniusVentures/GNUS-NEO-SWARM/pull/105 (DRAFT)

## Where things stand

PR #105 carries the Phase 7 Wave 2 follow-up fixes (single commit `f2e9b6c`):
- ELM fail-close: `Error::NotLoaded = 21` (Phase 8 Memory codes took 18–20), role_elm/domain_elm not-loaded paths return it; `error.cpp` message cases for NotLoaded + Phase 8 Memory codes
- GeniusSDK API drift: lightweight `GeniusNodeConfig` (Addr/Cut/TokenValueInGNUS/TokenID/BaseWritePath), `GeniusSDKInit(base_path, dev_config_json)` 2-arg via nlohmann::json (escaped), `ProcessingManager::Process` 4-arg, removed `m_sgBasePort`/`--sg-base-port`
- Thirdparty link resolution: `find_package(libp2p/ipfs-bitswap-cpp/ipfs-lite-cpp)` guarded by `if(EXISTS ... Config.cmake)` with `_MISSING_DEPS` stubs as fallback; `neoswarm_core` links imported targets instead of raw `*.a` globs (zero duplicate-library warnings)
- build/ submodule @ `82baed1` on cmaketemplate main (whole-archive per-OS settings — ALREADY MERGED to cmaketemplate main)

`/gsd:code-review --fix` ran on this change set: 9 findings (2 Critical, 4 Warning, 3 Info) — **all fixed, build clean, 18/18 ctest suites pass.** Fixes are amended into `f2e9b6c`. Review details: `.planning/workstreams/neoswarm/phases/07-expert-language-models-router/07-REVIEW.md` (Follow-up Review section, committed on this WIP branch).

## Remaining work (next person)

1. **PR #105**: push WIP branch is just for handoff — the real content is on `pr/phase7-wave2-role-domain-elms`. Mark PR #105 ready for review (or wait for kenya). Codex bot will flag on PR creation — check its comments.
2. **Reply to + resolve PR #92's 7 review threads** (henriqueaklein) pointing at PR #105 — PR #92 is MERGED so threads can't be resolved there; note in #105.
3. **PR #94 (Wave 3)**: 6 review threads — 5 outdated "isn't this an error?" (likely already fixed by develop's fail-close), 1 live: `grounding_elm.cpp:52` "This is the same thing."
4. **PR #95 (Wave 4)**: 1 thread — `elm_chain_builder.cpp:97` "Will you have any error path? If not... no reason to use outcome"
5. **PR #96 (Wave 5)**: 0 unresolved threads — check mergeability; likely already merged as part of Waves 3–6 landing on develop (verify; Wave 6 test/docs PR #102 merged 07-24).
6. **PR #69** (CI workflow, CHANGES_REQUESTED) — not Phase 7, not yet triaged.

## Key context / gotchas

- **Workstream layout**: planning root for this C++ work is `GNUS-NEO-SWARM/.planning/workstreams/neoswarm/` (phases 07, 08 live there). The `poc` workstream (phases 01–04) is the Python track. Workspace root `.planning/` bifurcated — do NOT write phase artifacts to root.
- **Phase 8 (GAML memory)** is in flight in parallel (PR #101 merged Wave 1; `pr/phase8-wave2-memory-storage` branch exists on remote).
- **Error enum**: Phase 8 took 18–20; next free value after NotLoaded=21 is 22.
- **Build**: `cd build/OSX/Debug && cmake .. -G Ninja && ninja`. NEVER run cmake from repo root. Stale cache → delete CMakeCache.txt + CMakeFiles, fresh configure (do NOT FORCE-cache).
- **test_genius_elm_ffi**: develop has `GeniusElmShutdown()` (int return) + fixture TearDown — do not re-add a void version; the duplicate was dropped during rebase.
- **test_chain_builder** is commented out in test/CMakeLists.txt:82 on develop (pre-existing).
- **Build submodule (cmaketemplate)**: integration branch is `main` (no develop branch exists there).
