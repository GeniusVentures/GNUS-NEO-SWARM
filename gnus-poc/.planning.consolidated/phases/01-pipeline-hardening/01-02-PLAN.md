---
phase: 01-pipeline-hardening
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - distill/backends/__init__.py
  - distill/backends/base.py
  - distill/backends/openai_backend.py
  - distill/backends/anthropic_backend.py
  - distill/teacher.py
  - distill/teacher_errors.py
  - tests/test_teacher.py
autonomous: true
requirements: [TEACH-02]

must_haves:
  truths:
    - "TeacherClient accepts a model name and dispatches to the correct backend (OpenAI or Anthropic) based on the model's endpoint `apiType`"
    - "Both the OpenAI backend (using `openai` SDK `chat.completions.create`) and the Anthropic backend (using `anthropic` SDK `messages.create`) are fully implemented and callable"
    - "All backends produce a uniform response wrapper so callers (`generate()`) receive the same interface regardless of backend"
    - "Existing cost tracking, retry, circuit breaker, and budget enforcement apply uniformly across both backends"
  artifacts:
    - path: "distill/backends/base.py"
      provides: "Abstract backend interface defining the contract all backends must implement"
      exports: ["TeacherBackend"]
      min_lines: 30
    - path: "distill/backends/openai_backend.py"
      provides: "OpenAI API backend extracted from current TeacherClient, using openai SDK"
      exports: ["OpenAIBackend"]
      min_lines: 60
    - path: "distill/backends/anthropic_backend.py"
      provides: "Anthropic API backend using anthropic SDK messages.create()"
      exports: ["AnthropicBackend"]
      min_lines: 60
    - path: "distill/teacher.py"
      provides: "Refactored TeacherClient dispatching to backend based on apiType from config endpoints"
      min_lines: 120
  key_links:
    - from: "distill/teacher.py TeacherClient._resolve_backend()"
      to: "distill/backends/base.py TeacherBackend"
      via: "apiType field from config endpoints → backend class dispatch"
      pattern: "apiType.*openai.*OpenAIBackend|apiType.*anthropic.*AnthropicBackend"
    - from: "distill/backends/openai_backend.py OpenAIBackend.generate()"
      to: "openai SDK chat.completions.create()"
      via: "OpenAI client instance configured from endpoint config"
      pattern: "client\\.chat\\.completions\\.create"
    - from: "distill/backends/anthropic_backend.py AnthropicBackend.generate()"
      to: "anthropic SDK messages.create()"
      via: "Anthropic client instance configured from endpoint config"
      pattern: "client\\.messages\\.create"
---

<objective>
Refactor the current single-backend (OpenAI-only) TeacherClient into a multi-backend architecture supporting both OpenAI and Anthropic API backends, dispatched per-call based on the model's endpoint `apiType` configuration.

Purpose: Enable dual-backend API support per D-05/D-06 so the pipeline can route teacher calls through either the OpenAI SDK or Anthropic SDK depending on the configured endpoint. Both backends must be fully implemented — not stubbed.
Output: `distill/backends/` module (base interface, OpenAI backend, Anthropic backend), refactored `distill/teacher.py`, updated `tests/test_teacher.py`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-pipeline-hardening/01-CONTEXT.md

<interfaces>
<!-- Current TeacherClient public API (must preserve compatibility) -->
From distill/teacher.py (lines 23-180):
```python
class TeacherClient:
    def __init__(self, config_path: Optional[Path] = None, project_root: Optional[Path] = None):
        # Loads config, creates OpenAI client, sets model/max_tokens/temperature/budget
    def generate(self, messages, **kwargs) -> Response:
        # Calls _call_api, resets failure count, logs cost, returns response
    def generate_with_logprobs(self, messages, **kwargs) -> Response:
        # Sets logprobs=True, top_logprobs=20, calls generate()

    # Properties: total_cost, budget_cap, circuit_open, call_count
    # Properties consumed by synthetic.py and distillation.py

    # Internal methods to extract to backend:
    def _call_api(self, messages, **kwargs) -> Response:
        # Retry loop, circuit check, budget check, API call via self._client.chat.completions.create
    def _estimate_cost(self, prompt_tokens, completion_tokens) -> float:
        # Cost calculation
    def _log_cost(self, prompt_tokens, completion_tokens, cost):
        # JSONL log
    def _log_error(self, error_type, status_code, detail):
        # JSONL log
    def _is_retryable(self, exception) -> bool:
        # Status code classification
    def _check_circuit(self):
        # Circuit breaker guard
    def _check_budget(self):
        # Budget cap guard
```

<!-- Current error types -->
From distill/teacher_errors.py:
```python
class TeacherConfigError(Exception): pass
class BudgetExceededError(Exception): pass
class CircuitBreakerOpenError(Exception): pass
class SyntheticDataError(Exception): pass
```

<!-- Existing test patterns -->
From tests/test_teacher.py:
- Tests use `patch("distill.teacher.OpenAI")` mock — will need updating for new module path
- `make_mock_completion()` creates mock response with choices[0].message.content and usage
- Fixtures: `config_path` (creates temp yaml with teacher block), `mock_openai` (patches OpenAI), `client` (constructs TeacherClient)
- Tests cover: init config loading, missing API key, successful generation, circuit breaker, budget exceeded, cost logging, error logging, non-retryable 400

<!-- New config schema (from Plan 01) already available -->
Config structure TeacherClient reads:
```yaml
endpoints:
  litellm:
    url: "http://127.0.0.1:4000"
    apiType: openai

models:
  deepseek-v4-fast:
    endpoint: litellm
    model_id: "deepseek-v4-fast"
  deepseek-v4-pro[1m]:
    endpoint: litellm
    model_id: "deepseek-v4-pro[1m]"
  glm-5.2:
    endpoint: litellm
    model_id: "glm-5.2"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend interface and extract OpenAI backend</name>
  <files>distill/backends/__init__.py, distill/backends/base.py, distill/backends/openai_backend.py</files>
  <read_first>
    distill/teacher.py — full current implementation; the `_call_api`, `_estimate_cost`, `_log_cost`, `_log_error`, `_is_retryable` methods contain the logic to extract into backends.
    distill/teacher_errors.py — existing error classes that backends must import and raise.
  </read_first>
  <action>
    Create `distill/backends/__init__.py` exporting: `TeacherBackend`, `OpenAIBackend`, `AnthropicBackend`.

    Create `distill/backends/base.py` with abstract base class `TeacherBackend`:
    - Constructor: `__init__(self, endpoint_config: dict, model_id: str, api_key: str)` — endpoint_config is the resolved `endpoints[<name>]` dict from config; model_id is the literal model identifier from `models` config
    - Abstract method: `generate(self, messages: list, max_tokens: int, temperature: float, **kwargs) -> dict` — returns a uniform response dict (not raw SDK response) with keys: `content` (str), `prompt_tokens` (int), `completion_tokens` (int), `raw_response` (original SDK response object)
    - Abstract property: `backend_type(self) -> str` — returns `"openai"` or `"anthropic"`
    - Concrete method: `estimate_cost(self, prompt_tokens: int, completion_tokens: int) -> float` — returns `(prompt_tokens * 0.27 + completion_tokens * 1.10) / 1_000_000` (default implementation, subclasses may override)

    Create `distill/backends/openai_backend.py` implementing `TeacherBackend`:
    - `__init__`: creates `openai.OpenAI(api_key=api_key, base_url=endpoint_config["url"])` — stores as `self._client`
    - `generate()`: calls `self._client.chat.completions.create(model=self._model_id, messages=messages, max_tokens=max_tokens, temperature=temperature, **kwargs)`. Extracts `response.choices[0].message.content`, `response.usage.prompt_tokens`, `response.usage.completion_tokens`. Returns uniform dict with `raw_response` set to the original OpenAI response object.
    - `backend_type` property returns `"openai"`
    - Handle `openai.APIError` and related exceptions by re-raising — do not catch here; retry logic stays in TeacherClient

    EXTRACT (do not copy-paste entire methods; move the logic):
    - Cost estimation formula from `TeacherClient._estimate_cost` → `TeacherBackend.estimate_cost` (default implementation)
    - The OpenAI SDK `chat.completions.create` call and response parsing from `TeacherClient._call_api` → `OpenAIBackend.generate`
  </action>
  <verify>
    <automated>python -c "
from distill.backends.base import TeacherBackend
from distill.backends.openai_backend import OpenAIBackend
# Verify interface
assert hasattr(TeacherBackend, 'generate')
assert hasattr(TeacherBackend, 'estimate_cost')
assert hasattr(TeacherBackend, 'backend_type')
# Verify OpenAI backend instantiable
import os; os.environ['TEST_KEY'] = 'sk-test'
backend = OpenAIBackend({'url': 'http://127.0.0.1:4000', 'apiType': 'openai'}, 'test-model', 'sk-test')
assert backend.backend_type == 'openai'
assert backend._model_id == 'test-model'
print('Backend interface and OpenAI backend created successfully')
"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `distill/backends/base.py` exists with class `TeacherBackend` having abstract method `generate` and concrete method `estimate_cost`
    - Source: `distill/backends/openai_backend.py` exists with class `OpenAIBackend(TeacherBackend)` having `generate()` that calls `self._client.chat.completions.create`
    - Source: `distill/backends/__init__.py` exports `TeacherBackend`, `OpenAIBackend`
    - Source: `grep -c "chat\.completions\.create" distill/backends/openai_backend.py` returns `1`
    - Source: `grep -c "class TeacherBackend" distill/backends/base.py` returns `1`
    - Source: `grep -c "def generate" distill/backends/base.py` returns `1`
    - CLI: `python -c "from distill.backends import TeacherBackend, OpenAIBackend; print('imports OK')"` exits 0
  </acceptance_criteria>
  <done>Backend interface defined, OpenAI backend extracted into its own module implementing the interface</done>
</task>

<task type="auto">
  <name>Task 2: Implement Anthropic backend</name>
  <files>distill/backends/anthropic_backend.py</files>
  <read_first>
    distill/backends/base.py — the TeacherBackend interface you must implement.
    distill/backends/openai_backend.py — reference implementation for patterns (response dict format, constructor shape).
  </read_first>
  <action>
    Create `distill/backends/anthropic_backend.py` implementing `TeacherBackend`:

    Constructor `__init__(self, endpoint_config: dict, model_id: str, api_key: str)`:
    - Creates `anthropic.Anthropic(api_key=api_key, base_url=endpoint_config.get("url"))` — note: Anthropic SDK base_url is only set if a non-default URL is configured; if endpoint_config["url"] is a LiteLLM proxy URL, pass it; if it's the default Anthropic API, omit base_url
    - Stores client as `self._client`

    Method `generate(self, messages, max_tokens: int, temperature: float, **kwargs) -> dict`:
    - Messages format conversion: OpenAI format uses `{"role": "system", "content": "..."}`, `{"role": "user", "content": "..."}`, `{"role": "assistant", "content": "..."}`. Anthropic SDK expects: `system` as a top-level parameter (string or list), and `messages` as a list of `{"role": "user", "content": "..."}` / `{"role": "assistant", "content": "..."}` blocks.
    - Extract system messages: If messages list contains `role: "system"`, extract `content` of the FIRST system message and pass as `system=str(content)` to `messages.create()`. If multiple system messages exist, join with newline.
    - Convert remaining messages: filter out system messages, keep user/assistant roles. Map `role` values as-is (Anthropic accepts "user" and "assistant").
    - Call `self._client.messages.create(model=self._model_id, system=system_prompt, messages=converted_messages, max_tokens=max_tokens, temperature=temperature, **kwargs)`
    - Response extraction: Anthropic response has `response.content[0].text` (string content), `response.usage.input_tokens` (prompt tokens), `response.usage.output_tokens` (completion tokens)
    - Return uniform dict: `{"content": str(content), "prompt_tokens": int(usage.input_tokens), "completion_tokens": int(usage.output_tokens), "raw_response": response}`
    - If `kwargs` contains extended thinking params (`thinking={"type": "enabled", "budget_tokens": N}`), pass them through to `messages.create()`

    Property `backend_type` returns `"anthropic"`

    Cost estimation: Override `estimate_cost` for Anthropic-specific pricing. Use the default formula from `TeacherBackend` as a base but note that Anthropic pricing differs by model. For Phase 1, keep the same cost formula as default (the pipeline config's budget_cap_usd handles the actual limit). Mark this with a TODO comment for per-model pricing in a future phase.
  </action>
  <verify>
    <automated>python -c "
from distill.backends.anthropic_backend import AnthropicBackend
from distill.backends.base import TeacherBackend
import os; os.environ['ANTHROPIC_TEST_KEY'] = 'sk-ant-test'
backend = AnthropicBackend({'url': 'http://127.0.0.1:4000', 'apiType': 'anthropic'}, 'claude-sonnet-4-20250514', 'sk-ant-test')
assert backend.backend_type == 'anthropic'
assert backend._model_id == 'claude-sonnet-4-20250514'
assert issubclass(AnthropicBackend, TeacherBackend)
# Test message conversion (unit, no API call)
conv = backend._convert_messages([{'role': 'system', 'content': 'You are helpful.'}, {'role': 'user', 'content': 'Hi'}])
assert conv['system'] == 'You are helpful.'
assert len(conv['messages']) == 1
assert conv['messages'][0]['role'] == 'user'
assert conv['messages'][0]['content'] == 'Hi'
print('Anthropic backend created and message conversion verified')
"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `distill/backends/anthropic_backend.py` exists with class `AnthropicBackend(TeacherBackend)`
    - Source: `grep -c "client\.messages\.create" distill/backends/anthropic_backend.py` returns `1` (not `client.messages.create` literally, but the anthropic SDK call pattern)
    - Source: `grep -c "class AnthropicBackend" distill/backends/anthropic_backend.py` returns `1`
    - Source: `grep -c "system.*system_prompt\|system.*content" distill/backends/anthropic_backend.py` returns `>= 1` (system message extraction)
    - Source: `grep -c "input_tokens\|output_tokens" distill/backends/anthropic_backend.py` returns `>= 1` (usage parsing)
    - CLI: `python -c "from distill.backends import AnthropicBackend; print('import OK')"` exits 0
    - Behavior: `_convert_messages()` correctly separates system message from user/assistant messages
  </acceptance_criteria>
  <done>AnthropicBackend implements TeacherBackend, converts OpenAI-format messages to Anthropic format, extracts content and usage from response</done>
</task>

<task type="auto">
  <name>Task 3: Refactor TeacherClient to dispatch via apiType and update tests</name>
  <files>distill/teacher.py, distill/teacher_errors.py, tests/test_teacher.py</files>
  <read_first>
    distill/teacher.py — full current implementation (lines 1-180); you will refactor __init__ and _call_api to use backends, preserving _check_circuit, _check_budget, generate, generate_with_logprobs, and properties.
    distill/backends/base.py — TeacherBackend interface.
    distill/backends/openai_backend.py — OpenAIBackend for openai apiType.
    distill/backends/anthropic_backend.py — AnthropicBackend for anthropic apiType.
    tests/test_teacher.py — current test file; all `patch("distill.teacher.OpenAI")` calls must be updated to patch backend construction instead.
  </read_first>
  <action>
    Refactor `distill/teacher.py` — `TeacherClient` class:

    **`__init__` changes:**
    - Load config from `config/pipeline.yaml` as before (use yaml.safe_load directly or ConfigLoader from Plan 01; if ConfigLoader not yet available, load raw yaml and validate inline)
    - Read `endpoints` and `models` from config (not the old flat `teacher` block)
    - Read `teacher` section for: `level1`, `confidence_threshold` (for Plan 04), `max_retries`, `backoff_base_seconds`, `budget_cap_usd`, circuit breaker thresholds
    - Build backend registry: for each endpoint in config, create a backend instance. Map: `{endpoint_name: BackendInstance}`. Use `apiType` to select backend class: `"openai"` → `OpenAIBackend`, `"anthropic"` → `AnthropicBackend`
    - Store backend registry as `self._backends` dict
    - Store models config as `self._models`
    - API key resolution: for each model/endpoint, resolve `{MODEL_NAME_UPPER}_API_KEY` from env. For LiteLLM proxy (which fronts all models), use `LITELLM_API_KEY` env var. Document the env var convention.
    - Remove `self._client` (single OpenAI client) — calls now go through backends
    - Remove `self._model`, `self._max_tokens`, `self._temperature` as instance-level defaults — these become per-call parameters. Store default `_default_max_tokens` and `_default_temperature` from config for backward compat.

    **New internal method `_resolve_backend(self, model_name: str)`:**
    - Look up `model_name` in `self._models`
    - Get the `endpoint` reference from model config
    - Return `self._backends[endpoint_name]`
    - Raise `TeacherConfigError(f"Unknown model '{model_name}'. Available: {list(self._models.keys())}")` if model not found
    - Raise `TeacherConfigError(f"Endpoint '{endpoint_name}' not found for model '{model_name}'")` if endpoint not in backends

    **Refactored `_call_api(self, model_name: str, messages, **kwargs)`:**
    - Accept `model_name` as first parameter (the model identifier from `models` config)
    - `_check_circuit()` and `_check_budget()` unchanged
    - Resolve backend: `backend = self._resolve_backend(model_name)`
    - In retry loop: call `backend.generate(messages, max_tokens=..., temperature=..., **kwargs)` instead of `self._client.chat.completions.create(...)`
    - Set `last_exception` from backend errors (catch generic Exception as before for retry)
    - After successful response: return the uniform dict
    - Response adaptation: `_call_api` now receives the uniform dict from backend. Wrap it to maintain backward compatibility: create a lightweight response wrapper object with `.choices[0].message.content`, `.usage.prompt_tokens`, `.usage.completion_tokens`. Use a simple `types.SimpleNamespace` or a private `_Response` class.

    **Updated `generate(self, model_name: str, messages, **kwargs)`:**
    - Signature now: `generate(self, model_name, messages, **kwargs)` — model_name is the key from `models` config
    - For backward compat: if `model_name` is not provided or None, use `self._config["teacher"]["level1"]` (the default Level 1 teacher)
    - Calls `self._call_api(model_name, messages, **kwargs)`
    - Resets `self._consecutive_failures = 0` on success
    - Logs cost via `self._log_cost()`
    - Returns the response wrapper object

    **Updated `generate_with_logprobs(self, model_name, messages, **kwargs)`:**
    - Same signature update as generate
    - Passes `logprobs=True, top_logprobs=20` to generate

    **Properties preserved unchanged:** `total_cost`, `budget_cap`, `circuit_open`, `call_count`

    **Remove from teacher.py (moved to backends):**
    - `_estimate_cost` (now in `TeacherBackend.estimate_cost`)
    - The `self._client.chat.completions.create(...)` call (now in `OpenAIBackend.generate`)
    - `NON_RETRYABLE_EXCEPTIONS` tuple (keep; still used for non-retryable guard)

    Add to `distill/teacher_errors.py`:
    - `class BackendNotFoundError(TeacherConfigError): pass` — raised when no backend found for model

    **Update `tests/test_teacher.py`:**
    - Remove `patch("distill.teacher.OpenAI")` — that import goes away
    - Replace with `patch("distill.teacher.OpenAIBackend")` and `patch("distill.teacher.AnthropicBackend")` to mock backend classes
    - Update `config_path` fixture to produce the new config format (endpoints + models, not flat teacher)
    - The `make_mock_completion` helper: update to return a backend-style uniform dict instead of an OpenAI response mock
    - Update `client` fixture to construct TeacherClient with new config format
    - `test_loads_config`: assert model registry is populated, not single model
    - `test_successful_generation`: pass model_name as first arg; mock backend.generate to return uniform dict
    - `test_circuit_breaker_opens_after_failures`: mock backend.generate to raise
    - `test_budget_exceeded_raises` / `test_budget_not_exceeded_below_cap`: unchanged logic
    - `test_logs_cost`: adapt to new response format
    - `test_non_retryable_400_error_raises_immediately`: mock backend to raise with status_code=400
    - Add new test: `test_resolve_backend_for_unknown_model_raises` — calling generate with "nonexistent-model" raises TeacherConfigError
    - Add new test: `test_dispatches_to_correct_backend_by_apiType` — verify OpenAIBackend called for openai endpoint, AnthropicBackend for anthropic endpoint
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/GeniusCogntiveSystem/GNUS-NEO-SWARM/gnus-poc && python -m pytest tests/test_teacher.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `TeacherClient.__init__` builds backend registry from config endpoints by apiType (OpenAIBackend for "openai", AnthropicBackend for "anthropic")
    - Behavior: `client.generate("deepseek-v4-fast", messages)` dispatches to the backend associated with that model's endpoint
    - Behavior: `client.generate("nonexistent-model", messages)` raises `TeacherConfigError` with a message containing the model name and available models
    - Behavior: the `generate()` method signature accepts `model_name` as first positional arg after `self`
    - Behavior: existing `generate_with_logprobs()` works with the new signature
    - Behavior: `client.total_cost`, `client.budget_cap`, `client.circuit_open`, `client.call_count` properties still work
    - Source: `grep -c "from.*backends.*import" distill/teacher.py` returns `>= 1`
    - Source: `grep -c "_resolve_backend" distill/teacher.py` returns `>= 1`
    - Source: `grep -c "OpenAIBackend" distill/teacher.py` returns `>= 1`
    - Source: `grep -c "AnthropicBackend" distill/teacher.py` returns `>= 1`
    - Source: `grep -c "class BackendNotFoundError" distill/teacher_errors.py` returns `1`
    - CLI: `python -m pytest tests/test_teacher.py -x -v` — all tests pass
  </acceptance_criteria>
  <done>TeacherClient dispatches via apiType to correct backend; all existing tests pass with updated config format; new tests cover backend dispatch and error cases</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TeacherClient → Backend | API key and messages cross from trusted code to external SDK |
| Backend → External API | Network calls to LiteLLM proxy or Anthropic API |
| Config → TeacherClient | Endpoint URLs and model identifiers from YAML config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | AnthropicBackend | mitigate | Validate response structure before extracting content; handle missing keys gracefully |
| T-02-02 | Information Disclosure | TeacherClient | mitigate | API keys loaded from `.env` only via `os.getenv()`; never logged; error messages sanitize keys |
| T-02-03 | Denial of Service | TeacherClient | mitigate | Circuit breaker and retry logic preserved from original code; applied uniformly across both backends |
| T-02-04 | Elevation of Privilege | Backend dispatch | mitigate | `apiType` values strictly validated against known backends; unknown types raise TeacherConfigError |
| T-02-SC | Tampering | pip installs | mitigate | `anthropic` SDK is a new dependency — slopcheck audit required before install |
</threat_model>

<verification>
- `python -m pytest tests/test_teacher.py -x -v` — all tests pass
- `python -c "from distill.teacher import TeacherClient; print('import OK')"` — no import errors
- `python -c "from distill.backends import TeacherBackend, OpenAIBackend, AnthropicBackend; print('backends OK')"` — all backends importable
</verification>

<success_criteria>
1. TeacherClient generates responses via OpenAI backend when endpoint `apiType` is `"openai"`
2. TeacherClient generates responses via Anthropic backend when endpoint `apiType` is `"anthropic"`
3. Both backends produce a uniform response dict with `content`, `prompt_tokens`, `completion_tokens`, `raw_response`
4. The `generate()` method signature accepts `model_name` as first positional arg; unknown model raises descriptive TeacherConfigError
5. All existing test_teacher.py tests pass after updating for new config format and backend architecture
6. New config format (endpoints + models) is fully supported in TeacherClient.__init__
</success_criteria>

<output>
Create `.planning/phases/01-pipeline-hardening/01-02-SUMMARY.md` when done
</output>
